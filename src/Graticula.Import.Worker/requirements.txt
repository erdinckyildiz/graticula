# Pinned, and pinned rather than ranged on purpose.
#
# ADR-016 §7 answered "may tools install dependencies at runtime" with no: the image carries a
# curated set we version and patch, because offline beats convenience for an air-gapped deployment.
# A range would make two builds of the same image different, which is the thing a curated set exists
# to prevent. A-049 is the assumption that this stays maintainable, and it is UNVALIDATED.
#
# Not listed and deliberately absent: GDAL itself. It comes from the base image
# (ghcr.io/osgeo/gdal), not from pip — Q-28's stricter form is that GDAL stays out of the managed
# dependency graph, and pyogrio links the system library rather than shipping its own.
pyogrio==0.11.1
geopandas==1.1.1
pyarrow==21.0.0
psycopg[binary]==3.2.9
shapely==2.1.2
