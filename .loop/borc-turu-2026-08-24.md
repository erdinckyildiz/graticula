# Borç turu — 2026-08-24

Çalışma listesi. Döngünün tek hafızası. Her tur bunu yeniden okur.
Durumlar: `bekliyor` / `kapandı` / `vakti-gelmemiş` / `tıkalı`.

Tohum 42 kayıttı; panonun KPI'ı 49 diyordu. Fark, tabloda OPEN görünmeyen
yedi "kısmen" satırıydı — D-06, D-17, D-23, D-30, D-32, D-61, D-65 — hepsi
`architecture-debt.md` üzerinden okundu, hepsinin ödenmemiş bir yarısı var,
hepsi listenin sonuna numara sırasıyla eklendi. 42 + 7 = 49.

| Kayıt | Durum | Sonuç |
|---|---|---|
| D-63 | tıkalı | Teşhis doğru ve bugün yeniden ölçüldü — sekiz push, hepsi 0 saniyelik `startup_failure`; tek `echo`'luk probe iki saniyede düştü. Depo izinleri tam açık, yani onarım depo dışında. Beklenen karar Q-135 olarak yazıldı: harcama limiti mi, depoyu herkese açmak mı. |
| D-115 | vakti-gelmemiş | Tetikleyici "bölünmeden doğan bir sonraki kusur"; 2026-08-23 sonrası açılan beş kaydın (D-144/145/146/147/148) hiçbiri yüzey bölünmesine dayanmıyor. Satırın iddia ettiği ScreenOwnershipTests var ve anlattığı üç iddiayı karşılıyor. Onarım zaten sahibinin kararını geri almak demek. |
| D-46 | tıkalı | Onuncu örnek bulundu ve kapatıldı — hem de satırın kendi çare gösterdiği testin içinde: iki CSP muhafızının sayfa listeleri elle tutuluyordu ve ayrışmıştı, `/studio/view.html` satır içi script için hiç bakılmıyordu. Sınıf düzeyinde onarıldı: sayfa kümesi artık `wwwroot`'tan okunuyor, iki yönden yanlışlandı. Satır kapanamıyor, çünkü tetikleyicisi her zaman doğru — §62'ye göre bu borç değil, kılık değiştirmiş kalıcı karar. Sınıflandırma kararı Q-136. |
| D-116 | vakti-gelmemiş (bu turun payı ödendi) | Tetikleyici ateşlendi: dört belge §66 kapıları için present-tense "0 of 9" diyordu, gerçek 7 of 9. Biri (ADR-030) canlı iddiaydı, üçü tarihlenmemiş anlık görüntü. Kayıt korunarak tarihlendirildi, ikinci mekanik kontrol (`a_gate_tally_that_disagrees_with_the_gates`) yazıldı ve iki yönden yanlışlandı. Satır açık kalıyor: soru/varsayım/ADR/test sayıları hâlâ kontrolsüz. |
| D-03 | kapandı | Tetikleyici on iki gün önce ateşlenmişti: kural 2026-08-12'de security.md §5'e yazılmış, hiçbir şey uygulamamıştı. Ölçüm: on altı koldan on dördü anonim çağırana sızdırıyordu — beşi motoru adıyla, ikisi deponun kendi metnini aynen, dördü `/admin` adresi. Tek anahtar iki cümle taşıyor, `admin:manageServer` ile seçiliyor; WFS/WMS yetenek belgelerinden de sağlayıcı adı çıkarıldı. Üç muhafız, biri yanlışlandı. |
| D-04 | vakti-gelmemiş | Tetikleyici "çok kiracılı dağıtımlar desteklenmeden önce". `src/` içinde tek "tenant" geçişi var ve o da `portalMode = "singletenant"` — sunucu kendini istemciye tek kiracılı ilan ediyor. v1-scope §7 gerekçesini *"one tenant model"* üzerine kuruyor. |
| D-05 | kapandı | **Özgün teşhis bayattı.** Ödenmedi, çözüldü: v1-scope 2026-08-14'te "D-05 closes" diye yazmış, register izlememişti. Satırın "Oracle ve SQL Server katmanları hâlâ özellik sunuyor" cümlesi yanlış — `src/` içinde tek sağlayıcı var. Ona canlı borç diye atıf yapan iki varsayım satırı da düzeltildi. Onarırken bulunan yeni çelişki D-149 olarak açıldı. |
| D-07 | tıkalı | Tetikleyici ateşlenmiş: `exceededTransferLimit` sayfalaması yayında, kesik sayfa ile kısa sayfa ayrımı artık istemciden fiilen bekleniyor. Sunucu yarısı `ResponseOutcome.Truncated` ile onarılmış; istemci yarısı Q-91'in protokol kararına bağlı ve o karar başkasının ArcGIS istemcisinin ne yaptığını gözlemlemeyi istiyor. Q-91 üç yönden keskinleştirildi, cevaplanmadı. |
| D-08 | kapandı | Açık kalma gerekçesi iki maddeydi, ikisi de ölçüldü. Ölçekleme: 250/1024/3832 ms — 1M/4M/16M, 16× aralıkta doğrusal, tavan ~120M satır; tek noktalık ekstrapolasyon artık eğim. Kilit beklemesi kuruldu: 30 s tavana karşı 30.30 s, engelsiz aynı okuma 296 ms — yüz kat, ve satırın kendi cümlesinin sayısı. Yol boyunca D-150 bulundu (55P03 → "veritabanı erişilemez"). |
| D-09 | kapandı | **Özgün teşhis yanlıştı — satır kendi kendisiyle çelişiyordu:** gövde "RESOLVED 2026-08-14" derken durum hücresi "OPEN" diyordu. Üç iddia da ölçümle doğrulandı: uç noktalar var, kimlik bilgisi süreç içinde mühürleniyor, kayıt yazmadan önce prob atıyor. Hücrenin işaret ettiği ADR-017 §3'teki gerçek eksik D-151 olarak ayrıldı. |
| D-10 | vakti-gelmemiş | **Satır bayattı:** `/generateToken` yokmuş gibi yazıyordu, oysa 2026-08-20'de gelmiş — `/rest` ve `/admin` altında dört rota, ortak `LoginService`, conformance testleri var. O yarı ödenmiş, satır düzeltildi. Kalan beş yüzeyin (OIDC/SAML/SCIM/API key/mTLS) tetikleyicisi "IdP'li ilk dağıtımdan önce" ve hiç dağıtım yok — Operations kapısı da aynı sebeple bekliyor. |
| D-11 | kapandı | **Özgün teşhis yanlıştı, üç kez:** durum hücresi "OPEN" ve gerekçesi "admin API yapılmadı" diyordu — D-09 bir saat önce tersini ölçmüştü. Özgün metnin dört iddiası da ölçüldü ve dördü de tersine dönmüş: 73 `RequireAsync` çağrısı, göçle ekilen roller, paylaşım kapsamıyla okuma, kalkmış başlangıç uyarısı. Bağlı olduğu Q-59 da 2026-08-14'te cevaplanmış. Sınıf onarımı: aynı kusuru yakalayan kontrol yazıldı ve yanlışlandı. |
| D-14 | tıkalı | Tetikleyicinin ikinci maddesi ateşlenmiş (rol iptali admin API'ye ulaştı, beş rota). Beşi de reddediyor ve test edilmiş — satırın haberi olmayan iyileşme. Teşhis doğru: `RequireSetup()` gerçekten yalnızca hiç kullanıcı yokken kuruluyor, yöneticisizlik Critical log + tam kurtarma cümlesi. Kalan durum artık yalnızca göç/geri yükleme/doğrudan veritabanı erişimiyle üretilebiliyor — ki sonuncusu kurtarma cümlesine zaten sahip, yani reddin dayandığı öncül kaydı. Karar Q-137. |
| D-16 | kapandı | Tetikleyici ateşlenmiş: `/sharing/rest` Portal yüzeyi yayınlıyor. Esri'nin yayımlanmış listesine karşı denetlendi — tele giden üç kimlik birebir doğru, içerideki onsekiz kimlik zaten kasten bizim (§6) ve hiçbiri Esri'nin yazımıyla eşleşmiyor. **Bir şey yanlış çıktı:** Esri'nin yerleşik rolü altı, bizde beş; Facilitator depoda hiç geçmiyor ve iki register "beş Portal rolü" yazmış. ADR-018 §5'e dördüncü `INFERRED` olarak eklendi, koşul 2 kapatıldı. |
| D-18 | vakti-gelmemiş | Tetikleyici "break-glass yolu tasarlandığında"; ADR-017 kendi 3. koşulunda *"the break-glass path is not built"* diyor ve A-051 `UNVALIDATED`. Şimdi ödenebilir yarı zaten ödenmiş: `/admin/health` `maySeeDetail = Allows(AdminManageServer)` ile ayrım yapıyor ve anonim çağırana gerekçesini söylüyor — D-03 kuralını, ben `ErrorResponse`'ta uygulamadan önce uyguluyormuş. Kalan yalnızca kesinti hâli. |
| D-19 | vakti-gelmemiş | Tetikleyici "ilk sürüm". Sıfır git etiketi, sıfır GitHub sürümü; Q-72 (kayıt defteri, etiket şeması, sürüm süreci) hâlâ açık. Ayrıca D-63 gereği yayınlayacak hiçbir iş akışı zaten koşmuyor. |
| D-20 | vakti-gelmemiş | Tetikleyici "Q-58 geldiğinde". Editor tracking kodda yok (üç yorum, üçü de ertelendiğini söylüyor) ve bağlı olduğu **Q-58c** açık. **Bir sonraki tur için not:** satır "Q-58" diyor ama Q-58a ve Q-58b cevaplanmış durumda; "Q-58 geldi mi" diye bakan biri yanlışlıkla kapatabilir. Tetikleyici ateşlenmediği için dosyaya dokunulmadı. |
| D-21 | vakti-gelmemiş | Tetikleyici "biri büyük bir katmanı senkronlamadan önce". `createReplica`/`synchronizeReplica`/`syncCapabilities` kodda sıfır kez geçiyor. Kalan yarı ArcGIS protokolünün kendi özelliği: `query` `resultOffset` alıyor, cursor almıyor. Kararı verilmiş fix — ADR-008 §4.5, opak token ile keyset — v1'in sunmadığı yüzeye ait; ArcGIS'te tutarlı anlık görüntünün cevabı sync ve o da yok. Yani karar eksik değil, yüzey eksik. |
| D-24 | vakti-gelmemiş (bu turun payı ödendi) | Düşürme tetikleyicisi ("desteklenen hiçbir sürüm şema 6 okumadığında") ADR-016'nın geri alma penceresiyle ilgili ve v1 daha çıkmadı. Ama satırın adını koyduğu tehlike — *ölü kolona ulaşabilen yazıcı* — bedelsiz kesilebilen iki yerde kesildi ve muhafız artık yazmaları da yakalıyor (okuma kontrolü yazıcıları hiç göremiyordu — D-24'ün kendi onarımının içinde D-46). 351 entegrasyon testi canlı veritabanına karşı geçti. Kalan tek yazıcı `is_hosted`, NOT NULL ve default'suz. |
| D-26 | vakti-gelmemiş | Tetikleyici "biri composite ilişki bildirip bir origin özelliği silmeden önce". `RelationshipEndpoints.cs:133` `Composite == true` için 501 döndürüyor ve `CompositeRelationshipTests` bunu tutuyor — yani kimse bildiremiyor, tetikleyici ateşlenemez. Yalan söyleyen bayrak tehlikesi 2026-08-23'te kapanmış; özellik yapılmamış. |
| D-29 | vakti-gelmemiş | Tetikleyici "depo herkese açılmadan önce"; depo private (D-63 turunda ölçüldü). **Bulgu:** bu tetikleyici, Q-135'in ikinci seçeneğiyle aynı olay — herkese açmak, CI'yı çalışır kılan adımla aynı anda D-29'un vadesini getiriyor, ki bu bir adım yanlış sıra. Q-135'e sıralama notu olarak yazıldı: "public" cevabı seçilirse sır taraması tek önkoşul değil, hâlâ-private depoda bir yeşil koşu da önkoşul. |
| D-33 | vakti-gelmemiş | D-24'ün ikizi, aynı üç kolon, aynı contract göçü. Tetikleyicisi açık ve kesin: "göç 11'i taşıyan sürümden sonraki sürüm" — sıfır git etiketi, sıfır sürüm. Bu, D-24'ün daha muğlak tetikleyicisini de yerine oturtuyor: düşürme sürümleri bekliyor. Satırın "kolonlar hâlâ publish'te yazılıyor" cümlesi bu turda benim değişikliğimle bayatladı, düzeltildi — üçten biri yazılıyor. |
| D-34 | vakti-gelmemiş | **Maliyet cümlesi yanlış çıktı, düzeltildi.** Satır "hosted katmanı yayından kaldırmak tablosunu düşürebilir" ve "API neye mal olduğunu söylemiyor" diyordu; ölçüm: `UnpublishLayerAsync` yalnızca kaydı siliyor, tablo düşürme yalnızca `drop=true` ile ve API bunu söylüyor — yanıt zaten *"the table was not touched"* notunu taşıyor. Geriye düz yokluk kalıyor (taşıma/yeniden adlandırma/sıralama/grup silme yok). Tetikleyici bir kullanım olayı ve gözlenebilir değil. |
| D-40 | vakti-gelmemiş | Tetikleyici "veritabanı olmayan ilk sağlayıcı tasarlanmadan önce". `src/` içinde tek sağlayıcı `Graticula.Providers.PostGis`; GeoParquet/FlatGeobuf/DuckDB kodda üç yorum geçişi, v1-scope ise DuckDB'yi kesilenler arasında sayıyor. Satırın durumu zaten "RECORDED, deliberately unpaid" ve tutarlı. |
| D-43 | kapandı | **Özgün durum yanlıştı:** "characterised but not diagnosed" diyordu, oysa 2026-08-18'de tam teşhis edilip `PostgresFixture.cs` yorumuna yazılmış — register haberdar olmamış (bu turun beşinci bayat satırı). Mekanizma aritmetik: `planet_osm_polygon` üzerinde indekssiz `order by osm_id`, 166,335 blok = 1,362 MB, disk 40.8 MB/s → 33.4 sn. Bugün bağımsız doğrulandı: tabloda hâlâ yalnızca mekânsal indeks var, ve zorunlu yeniden derlemenin ardından **351/351, 1dk38sn, sıfır hata**. Kalan `osm_id` indeksi sunucunun sahibi olmadığı bir tabloda — operatörün kararı. |
| D-50 | kapandı | Sonda artık tablonun kendi anahtarını (tipi ne olursa olsun) ve bütün tekil tamsayı adaylarını bildiriyor; tek tıklık öneri yapıca listenin ilki. Ölçüm dört tablo şekliyle: metin anahtarlı tablo eskiden yalnızca `legacy` diyordu — `ref` de `parcel` de görünmüyordu; şimdi `ref` + `{legacy,parcel}`. Dört test, yanlışlandı, 355 entegrasyon testi yeşil. |
| D-51 | tıkalı | Tetikleyici kalıcı bir kural ve ateşlenmiş. Kuralı taşıyan tek yer (sign-out) doğru yazılmıştı ama hiçbir şey tutmuyordu; artık kaynaktan okuyan bir muhafız tutuyor — hata yolu `sessionStorage`'a da belleğe de dokunamaz, çıkmak ve söylemek zorunda. Özgün kusur (`catch { /* already gone */ }`) geri konarak yanlışlandı. Kalan iki şey kod değil: iki-biçimli yapı satırın kendi gerekçesiyle makul, ve semptomun kapandığı onayı sahibinin. |
| D-58 | vakti-gelmemiş → tetikleyici ateşlendi, ölçüm eksik | *"Bir renderer olduğunda"* gerçekleşmiş: `Graticula.Render.Skia`, ADR-041, `MapServer/export`, WMS `GetMap`. Satırın kendi 1. rotası artık mümkün. **`preview.js` hâlâ tersini söylüyordu** — "bu sunucunun geometriyi piksele çeviren yolu yok" — düzeltildi, çünkü işi yapacak kişiyi durduran cümle tam olarak oydu. Değiştirme yapılmadı: dosyanın kendi standardı ölçüm ve kırk satırlık listede satır başına render'ın hiçbir sayısı yok; ölçmek sahibinin anahtarıyla çalışan bir sunucu istiyor. Borç artık "imkânsız" değil "ölçülmemiş". |
| D-68 | vakti-gelmemiş (öncülü yanlış çıktı) | **Satırın merkez iddiası ölçümle çürüdü.** Enstrüman `pg_backend_pid()` ve tek bağlantılık havuz: `CommandTimeout` ateşlendikten sonra aynı backend cevap veriyor (10981 → 10981), yani konnektör atılmıyor — Npgsql iptal isteği gönderiyor. Sunucu tarafı da bağlantıyı koruyor, yani karşılaştırmanın içinde fark yok. Geriye yalnızca tam-saniye çözünürlüğü kalıyor; komut fabrikasını değiştirmeyi gerektiren bağlantı israfı yok. Test iki yarıyı birlikte tutuyor. |
| D-79 | vakti-gelmemiş | Tetikleyici "ikinci bir çağıran ortaya çıkmadan önce". `PUT /admin/groups/{name}/settings`'i çağıran tek yer var: `console.js:1002`. Öteki iki geçiş bir yorum ve bir bağlantı; `tools/` altındaki betiklerin hiçbiri çağırmıyor. |
| D-88 | vakti-gelmemiş | Tetikleyici "ilk dağıtımdan önce". D-19 turunda ölçüldü: sıfır git etiketi, sıfır GitHub sürümü, hiç dağıtım yok. Üç parçası da (yapı-zamanı GDAL kontrolü, süreç kuralı, sürücü kümesi) aynı koşula bağlı. |
| D-94 | vakti-gelmemiş | Tetikleyici "aynı anda hem sunan hem içe aktaran ilk dağıtım, ya da bir yüklemenin istekleri yavaşlattığına dair ilk rapor". Hiç dağıtım yok ve hiç rapor yok; satır zaten "narrowed to the container" diyor. |
| D-95 | tıkalı | Tetikleyici bir girdi: taahhüt edilebilecek kadar küçük, kamuya açık bir geodatabase ve lisansının kaydı. Kimsede yok ve bulmak/vendorlamak bir veri-lisansı kararı. Eksik girdi Q-138 olarak üç somut cevap şekliyle yazıldı: vendorla, GDAL ile test-zamanında üret, ya da okuma yolunun doğrulanmadığını açıkça söyle. |
| D-99 | bekliyor | |
| D-107 | bekliyor | |
| D-108 | bekliyor | |
| D-120 | bekliyor | |
| D-124 | bekliyor | |
| D-127 | bekliyor | |
| D-128 | bekliyor | |
| D-144 | bekliyor | |
| D-145 | bekliyor | |
| D-06 | bekliyor | |
| D-17 | bekliyor | |
| D-23 | bekliyor | |
| D-30 | bekliyor | |
| D-32 | bekliyor | |
| D-61 | bekliyor | |
| D-65 | bekliyor | |
