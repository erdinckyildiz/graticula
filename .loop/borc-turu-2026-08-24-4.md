# Borç turu 4 — 2026-08-24

Sahibinin talimatı: *"tamamı bitene kadar durma."* Tur 1–3 her kaydı **işledi**;
çoğu tetikleyicisi ateşlenmediği için kapanmadı. Bu tur farklı bir soru soruyor:
**tetikleyici ateşlenmiş olsun olmasın, bugün ödenebilecek ne var?**

Kural değişmiyor: tetikleyicisi ateşlenmemiş bir borç kanıtsız kapatılmaz.
Değişen şey, "vadesi gelmemiş" cevabıyla yetinmemek — her satırda *şimdi
yapılabilecek bir parça var mı* diye bakmak, varsa yapmak.

| Kayıt | Durum | Sonuç |
|---|---|---|
| D-04 | bekliyor | |
| D-07 | bekliyor | |
| D-10 | bekliyor | |
| D-14 | bekliyor | |
| D-18 | bekliyor | |
| D-19 | bekliyor | |
| D-20 | vakti-gelmemiş (tetikleyici keskinleştirildi) | Satır "Q-58 geldiğinde" diyordu; Q-58 bölünmüş ve **Q-58a ile Q-58b cevaplanmış**. "Q-58 geldi mi" diye bakan biri iki yeşil cevap bulup satırı kapatabilirdi — oysa ihtiyaç duyduğu üçüncüsü, Q-58c (domains, subtypes, editor tracking), açık ve başlanmamış. Tetikleyici artık Q-58c diyor. |
| D-21 | bekliyor | |
| D-23 | bekliyor | |
| D-24 | kapandı | Üç ölü kolonun üçü de artık yazılmıyor. İkisi sabah bedelsiz kesilmişti; `is_hosted` NOT NULL ve default'suz olduğu için kesilemiyordu — **göç 34 ona bir varsayılan veriyor**, bu bir *expand*, `minimum_reader_version` kımıldamıyor ve sürüm-33 okuyucu etkilenmiyor. Muhafız artık üçünü de kapsıyor. **Satırın "varsayılan veren göç ile düşüren göç aynıdır" gerekçesi yanlıştı:** ikisi expand ve contract'ın zıt yarıları; expand bugün mümkün, contract sürüm bekliyor. 356 entegrasyon testi göç edilmiş depoda geçti. |
| D-26 | kapandı | Satırın maliyet cümlesi baştan sona *yalan söyleyen bayrak* hakkında ve çareyi kendisi söylüyor: *"uç nokta `composite: true`'yu onurlandırılana kadar reddetmeli, ve reddetmiyor."* — **Reddediyor** (501 + test). Geriye kalan **yapılmamış bir özellik**, ve §62 borcun tetikleyicisi olan bir taviz olduğunu söylüyor; hiçbir şeyin aksini iddia etmediği eksik yetenek taviz değil. ADR-013 §6a'ya taşındı ve o satır artık bayrağın yok sayıldığını söylemiyor. |
| D-29 | bekliyor | |
| D-30 | bekliyor | |
| D-32 | bekliyor | |
| D-33 | vakti-gelmemiş | Tetikleyicisi hâlâ "göç 11'i taşıyan sürümden sonraki sürüm" ve sıfır sürüm var. Ama satırın "üçten biri hâlâ publish'te yazılıyor" cümlesi bugün geçersiz kaldı: göç 34'ten sonra hiçbiri yazılmıyor. Geriye yalnızca contract — düşürme — kaldı. |
| D-34 | kapandı (önce bir yarısı bayattı) | *"Gruplar hiç silinemiyor"* artık yanlış: `POST/DELETE /admin/services/{name}/groups` var ve çocukları varken reddediyor, testi de var. Satırın ikinci yarısı gerçekti ve ödendi: unpublish yanıtı artık `moving` taşıyor — taşıma/yeniden adlandırma/sıralama yok, düzeltmek yeniden yayınlamak demek, ve **bedel indeks**: ADR-013 §4 indeksin asla yeniden kullanılmadığını söylüyor, yani istemcinin sakladığı adres geri gelmiyor. Bu, operatörün tahmin edemeyeceği kısım ve yeniden yayınlamadan *önce* söyleniyor. Kalan yetenekler (taşı/adlandır/sırala) yapılmamış özellik, taviz değil — D-26 ile aynı okuma. |
| D-40 | bekliyor | |
| D-46 | bekliyor | |
| D-51 | bekliyor | |
| D-58 | vakti-gelmemiş → ölçüldü, takas sahibinin | Eksik olan sayı artık var: aynı sunucuda, 5.000 poligonluk **kayıtlı** (hosted değil) bir katmanda — önizleme 17–23 ms / **139.5 kB**, render 70–76 ms / **1.8 kB**. Kırk satırda 5.6 MB'a karşı 72 kB, üstelik 70 ms katman başına bir kez, 139 kB her izleyici için her seferinde. Kayıtlı katman sorusu da cevaplandı. **Takas yapılmadı ve sebebi sayılar değil:** önizleme beş markup noktasından çıkıyor, `cover` üçünde farklı şekilde, ve sonuca bakılması gereken ekranın sahibi var. `benchmarks/thumbnail`. |
| D-61 | bekliyor | |
| D-63 | bekliyor | |
| D-65 | kapandı | İstenen not artık yorum değil **parametre**: `FirstServiceNameAsync` `whyOneIsEnough` alıyor, yani derleyici bir sonraki kişiye testinin hangi türden olduğunu derlemeden önce sorduruyor. İki süitte on altı çağrı yeri birer cümle taşıyor. Yorum isteğe bağlı olurdu, parametre değil — bu satırın anlattığı dört gün, çağrı yerinden evrensel iddiayı per-server iddiadan ayıramadığımız dört gündü. Derleyici dizenin bir şey söylediğini kontrol edemez, o yüzden çok kısa gerekçeyi reddeden muhafız yazıldı ve yanlışlandı. |
| D-68 | bekliyor | |
| D-79 | kapandı | Satırın kendi son cümlesi çareyi söylüyordu: *"istek kaydında zorunlu-alan sözleşmesi."* `GroupSettingsRequest`'in yedi üyesi de artık `required` — eksik gövde, nesne var olmadan reddediliyor. **`required`, not-null kontrolü değil, çünkü ayrım *yokluk*:** özeti temizlemek meşru bir eylem, yani boş bir değer ve yalnızca *eksik* olan hata. **Konsolun değişmesi gerekmedi** — tek çağıran zaten bütün nesneyi kuruyordu, tip artık bunu söylüyor. Yedi `[InlineData]`, her biri bir üyeyi çıkarıyor; yalnızca `title`'ı çıkaran tek test altısı isteğe bağlıyken geçerdi. Yanlışlandı. |
| D-88 | kapandı | Üç parçadan ikisi zaten ödenmişti; kalan ikincisi ancak **sorarak** cevaplanabilirdi. GDAL'ın `LICENSE.TXT`'i on dört bileşen sayıyor ve en az biri için *"MRF sürücüsü derlemeye dahilse geçerlidir"* diyor — yani hangi lisansların bağladığı sürücü kümesine bağlı, onu da yalnızca derleme bilir. Okuyucuya `drivers` işlemi eklendi. **Ölçüm: GDAL 3.13.1, 83 vektör, 213 raster** ve koşullu bölümlerin hepsi geçerli, **Esri'nin Apache-2.0'ı dahil**. Sonuç aynı (hepsi izin verici, copyleft yok) ama artık *"GDAL MIT'tir"* bulgu olarak yazılamaz. |
| D-94 | bekliyor | |
| D-95 | bekliyor | |
| D-99 | vakti-gelmemiş → tetikleyici yanlış okunmuştu | **"ADR-004 sonrası ilk kartografik iş" gelecekte değil:** ADR-041 kendi başlığında ADR-004'ü *un-defer* ediyor, ADR-033 de kabul edilmiş, renderer WMS ve MapServer sunuyor. Doğru okununca örnek geometri de ödendi: çip artık ArcGIS'in adlandırdığı şekli alıyor (`esriSFS` dolgu, `esriSLS` bant, `esriSMS` nokta) ve çizgide `color` konturun değil çizginin kendisi. Kalan: renk seçici ve stilin altındaki katmanın önizlemesi — bunlar bir editör, editör de sahibi olan bir ekran. |
| D-107 | bekliyor | |
| D-108 | bekliyor | |
| D-115 | bekliyor | |
| D-116 | kapandı | Panonun yayınladığı öteki sayılar da kontrole bağlandı, üstelik `status-page.py`'ın kendi aritmetiğiyle — ikinci bir gerçekleme kayabileceği için. **İlk koşumda altı yeniden-ifade buldu ve altısı da meşruydu**: belgeler altı düzenleme almak yerine kontrol üç daraltma aldı — geçmiş kaydı fiilleri, referans cümleleri, ve **ilk on beş satırında kendi tarihini damgalayan belge o tarihin kaydıdır**. Pencere satır değil cümle. Yanlışlandı. Soru/varsayım/test sayıları kasten dışarıda. |
| D-124 | bekliyor | |
| D-127 | kapandı | **Satır kendi hücresinin ortasında "CLOSED 2026-08-23" diyordu ve başında "OPEN" ile başlıyordu** — pano başa baktığı için bir gün boyunca açık sayılmış. Verdikt öne alındı. Aynı kusur D-65'te de vardı (D-17 ve D-120'yle birlikte dört örnek) ve artık `a_debt_row_that_disagrees_with_itself` bunu da yakalıyor: durum hücresinin içine gömülmüş bir verdikt, ilk cümlesi yalan olan bir hücredir. Yanlışlandı. |
| D-128 | bekliyor | |
| D-144 | bekliyor | |
| D-145 | bekliyor | |
