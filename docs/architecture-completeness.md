# Architecture Completeness Matrix

Tracks how far each architectural area has actually been taken (§64). The point
is to make gaps visible — an area with a decision but no failure review is not
finished, however confident the ADR sounds.

**Legend:** `—` not started · `WIP` in progress · `✓` complete · `n/a` not applicable

---

| Area | Decision | ADR | Prototype | Benchmark | Security review | Ops review | Failure review | Status |
|---|---|---|---|---|---|---|---|---|
| Core language | **.NET** | `ACCEPTED` | n/a | — | — | — | — | Decided on paper analysis and secondary criteria. **Not validated by a language benchmark** — stated in ADR-001 §6. Effort moved to absolute measurement (A-019) |
| Primary data architecture | **decided, amended three times in one day** | `ACCEPTED WITH CONDITIONS` | — | — | — | — | — | **Platform store is PostgreSQL only** (Q-70). Four engines → two (Q-51) → one, in a single day. PostgreSQL is now a hard dependency of every deployment because the datastore is mandatory (Q-69) and PostGIS-only (Q-32); A-009 reinstated, A-018 superseded, Q-22 reversed. SQL Server and Oracle remain **providers only** — not platform stores, not datastores, not tile sources. Conditions in ADR-002 §9. |
| Provider architecture — multi-dialect | **v1 is PostGIS only (Q-88); six dialects deferred** | — | — | — | — | — | — | **Six** SQL dialects on the feature path: PostGIS, SQL Server, Oracle, **MySQL 8, MariaDB** (two providers, not one — their spatial implementations diverged) and **DuckDB as the file-format query engine, not a registered source** (Q-81). Tiles remain hosted-PostGIS only (Q-67). **The binding problem is no longer pushdown — it is Q-20**: six geometry engines evaluating our predicates, disagreeing at the edges, which never-degrade-silently cannot catch because every engine claims `intersects`. A-043 |
| Hosted datastore (**mandatory**) | **decided: v1, PostGIS only, managed appliance, required** | — | — | — | — | — | — | **Q-69 made it mandatory**, which via Q-32 made PostgreSQL mandatory (Q-70). It is now the only tile source (Q-67) and the platform store lives beside it. Storage model in [data-model.md](data-model.md). Q-71 asks how server, datastore and job-worker images are packaged and version-matched |
| Data model — storage and layer modes | **decided** | n/a | — | — | — | — | — | four lifecycles separated; datastore is a provider we own, on any of three spatial engines; editing is out of our API |
| Connection discipline / quiesce | — | — | — | — | — | — | — | new obligation: we must not block a DBA's DDL. Lands on ADR-007 §5b and the admin API |
| Schema drift detection | — | — | — | — | — | — | — | improvement over ArcGIS, which requires manual restart. A-023 |
| Geometry engine | **decided** | `ACCEPTED WITH CONDITIONS` | prototype | **measured x3** | — | — | — | **Blocker B2a discharged 2026-08-13.** Alternative B, refined by measurement into **three tiers rather than two**: push down to the provider first, ours on flat arrays second, adopted NTS topology third — *the cheapest geometry operation is the one that never crosses the wire*. §6b gives three conditions for owning a primitive so the boundary is applied rather than admired. **Single-provider scope**; cross-engine consistency (A-043) deferred with a trigger that is the second provider, not a date. A-006 moved to `CONTESTED`. Conditions in §9a |
| Rendering engine | **built 2026-08-20** | **ACCEPTED** ([ADR-041](adr/ADR-041-the-map-renderer.md)) | n/a | — | — | — | — | `DEFERRED` — vector-first; only WMS-in-compatibility-layer remains |
| API architecture | **`REOPENED` — v1 is ArcGIS-first (Q-88)** | `REOPENED` | — | — | — | — | — | OGC API Features 1+2+3 native; capability report generated not hand-written. **Q-78 put full protocol parity in scope**: 29 faces over 10 engines — [protocol-surface.md](protocol-surface.md). Eight engines decided or in flight, two do not exist (Q-79). Six faces over the feature engine is now the hardest available test of ADR-005's protocol-neutral interface, which was asserted and never proven (A-026) |
| Plugin model | **`REOPENED`** | `REOPENED` | — | — | — | — | — | **The trigger fired, 2026-08-13, by the route ADR-006 predicted** — Q-17b puts a Python-based GPServer in scope, which is a third-party plugin system for geoprocessing. ADR-006 named geoprocessing as the first candidate because job workers already isolate. Providers, formats and API surfaces stay internal-only; only the geoprocessing axis reopened. Q-74 to Q-76 |
| Service runtime | **decided** | `ACCEPTED WITH CONDITIONS` | — | **partly measured** | — | — | — | **§4.1's worker sizing has no allocation term and needs one** — A-037 validated, ADR-007 §4.14. The ArcSOC question answered. Structure decided, numbers are conditions. Affinity routing (A-014) is the unproven part. Amended with context pinning (§4.12) as the dedicated-instance equivalent |
| Query engine | **decided** | `ACCEPTED WITH CONDITIONS` | — | — | — | — | — | structure decided, numbers not. Conditions in ADR-008 §10 |
| Raster engine | **built 2026-08-21, and the shape of it is not what this row predicted** | **ACCEPTED WITH CONDITIONS** ([ADR-043](adr/ADR-043-imageserver-and-the-raster-face.md), [ADR-044](adr/ADR-044-tiles-are-served-because-the-claim-had-to-be-true.md)) | — | — | — | — | — | **This row read `REOPENED` for a day after ImageServer shipped, which is [D-130](architecture-debt.md) happening again in the file that is supposed to catch it.** Q-77's Tier 1/Tier 2 line was drawn by ADR-043: reading a TIFF is Tier 2 behind `ICoverageReader`, and choosing an overview, warping and planning are Tier 1. `exportImage` and `identify` are served; **`identify` turned out not to be near-free in the way ADR-009 §0 assumed**, and neither were the other five — that assumption is ADR-043 §2. Tiles were left out of the first cut and put back in by ADR-044 eight days later, because ArcGIS Pro will not open an image service without them ([Q-134](open-questions.md)). **Raster function chains and dynamic mosaicking are still out**, and the register says so rather than implying it by silence. COG storage, conversion at registration and GDAL on job workers did not survive: imagery is registered where it lives and is never copied (ADR-043 §3.3) |
| Caching | **decided** | `ACCEPTED WITH CONDITIONS` | — | — | — | — | — | tiles are the real cache; L2 optional permanently; coherence is best-effort for registered data and documented as such |
| Job system | **decided** | `ACCEPTED WITH CONDITIONS` | — | — | — | — | — | queue in the platform store, no broker; job classes with reserved capacity; every job declares its re-run behaviour |
| Clustering | deferred | DEFERRED | n/a | n/a | — | — | — | `DEFERRED` (§79) |
| Build vs adopt policy | ✓ | n/a | n/a | n/a | — | — | — | ACTIVE |
| Provider architecture (§27) | **resolved (Q-52)** | — | — | — | — | — | — | **Q-52 answered 2026-08-13.** Serving providers are databases only — PostGIS, SQL Server, Oracle, MySQL, MariaDB — plus DuckDB as the *file-query engine* (Q-81), which is what lets a file be queried without becoming a provider. Import sources are Shapefile, GeoPackage, FileGDB, KML, GPX, WKT, GeoJSON, FlatGeobuf, GeoParquet, converted at registration. Output formats add GeoArrow and MVT. **§27's error is now explicit: a format is not a provider.** Warehouses still declined (Q-53) |
| Feature services (§28) | — | — | — | — | — | — | — | read **and write**. A-026 asks whether OGC API Features plus additive extensions covers §28; A-027 covers concurrency against writes that bypass us |
| Vector tiles (§33) | **scope decided (Q-67): hosted data only** | — | prototype | **measured x3** | — | — | — | [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md). A-019 passes; own clipper 63x and own simplifier 47x faster than the adopted equivalents, at equal output. Geometry is no longer the cost — allocation is: **80.9% GC pause at 18% CPU utilisation** under load (A-037, validated). Database pushdown of clip is structural not optional (A-021, finding 11: a z16 tile read 201,580 vertices to emit 2,080). **Q-67 removed the multi-dialect tile problem entirely** — every tile source is now PostGIS, so the measurement gap closed by decision rather than by evidence. Open: Q-68 (do we keep our own encoder) and Q-69 (is the datastore still optional) |
| Glyph & sprite serving | **glyphs shipped 2026-08-15; sprites are an empty stub** | `ACCEPTED WITH CONDITIONS` | — | — | — | — | — | [ADR-027](adr/ADR-027-glyphs-and-sprites.md). **The tile service could not draw a label** — the style had no `glyphs` key, so a client with a `text-field` rendered no text at all, and every tile test passed anyway because nothing in the sequence asks whether a style can be *drawn*. Signed-distance-field ranges are generated at build time from DejaVu Sans and served as files, which satisfies Q-15 by construction: 31 ranges, 7,720 glyphs, all reconstructed at IoU 1.000 against the font. **Sprites answer and are empty** — there is no icon library and no way to upload one, so the sheet exists only so a probing client does not get a 404. Open: no CJK, one typeface, and an administrator cannot add a font without rebuilding |
| Style document management | **shipped 2026-08-15** | `ACCEPTED WITH CONDITIONS` | — | — | — | — | — | [ADR-028](adr/ADR-028-style-documents.md). Storage and serving only, no evaluation, as scoped. One style per service, stored as **text so it comes back byte for byte** — a cartographer diffing the server's copy against their file should see nothing. Checked on write for the two failures worth preventing: a mistyped `source-layer`, which renders a blank map with no error anywhere, and any URL that leaves this server, because a style is a document handed to every viewer's browser. Open: one style per service is not enough (light/dark), and nothing revalidates a style when the service's layers change |
| Publishing (§38) | — | — | — | — | — | — | — | not started — owns runtime schema evolution (publishing with a smaller blast radius) and registration, which runs as an interactive-class job |
| Admin API (§39) | **shape decided** | `ACCEPTED WITH CONDITIONS` | — | — | **partial** | **partial** | — | [ADR-017](adr/ADR-017-admin-api.md), 2026-08-13. **Blocker B6 discharged.** Shape, not surface: separate versioned prefix, one async pattern (202 + job), runtime state through the API with only bootstrap in config, every mutation audited, capability report as a first-class resource. **Designed against F5's 2 AM scenarios rather than as a CRUD list**, which surfaced the allocation-and-GC-pause requirement. New finding: §6's **degraded mode** — the platform store lives in the mandatory datastore, so a datastore outage blinds the admin API exactly when it is needed, and a minimal supervisor-served surface must survive it. Full catalogue is Phase 1 |
| Compatibility layer (§51) | **scope set, amended twice in one day** | — | — | — | — | — | — | v1 is **WFS + WMTS-for-vector-tiles + full ArcGIS FeatureServer including edits + GeometryServer** (Q-17, Q-17a). **GeometryServer added and elevated to v1 core** — owner calls it crucial. Cheap to build, not cheap to operate: it publishes the general overlay run 1 measured at 438.6 ms, on caller-supplied geometry, with no special case to exploit (ADR-005 §3.3b, A-042). **GPServer in, Python-based, after the SDK** (Q-17b) — reopens ADR-006. **ImageServer in, decomposed per operation** (Q-17c) — reopens ADR-009. WMS and MapServer remain out of v1 (Q-47, ADR-004). Outside the core domain. |
| Feature service data model gaps | **identity, relationships, attachments decided** | `ACCEPTED WITH CONDITIONS` | — | — | **partial** | — | — | [ADR-013](adr/ADR-013-feature-service-data-model.md), 2026-08-13. Q-57, Q-58a, Q-58b answered; **all three ship in v1**. Identity is declared not inferred; relationships declared not reverse-engineered; attachments in the database, streamed, behind a separate bounded pool. Remaining: **Q-58c** — domains, subtypes, editor tracking. Opened A-040 and A-041. Originally: **Corroborated 2026-08-13**: a peer ships attachments and related records at its free tier ([reference-reading-log.md](research/reference-reading-log.md)), so these are table stakes for FeatureServer compatibility rather than refinements — though the argument does not need the corroboration, since ArcGIS clients call `queryRelatedRecords` and the attachment endpoints unconditionally |
| Format support | **enumerated (Q-52)** | — | — | — | — | — | — | 9 import, 5 output. **A-038 confirmed**: File Geodatabase has no managed .NET reader, so GDAL is not avoidable and A-016's job-worker placement stands. **GeoParquet now has three independent justifications** — Q-74's Python SDK boundary, Q-81's DuckDB engine, and this list — having had none a week ago |
| Migration tooling | **scope set** | — | — | — | — | — | — | inventory plus definition import, free (Q-16). Needs a home — probably its own component rather than part of the compatibility layer |
| Self-service publishing | **scope confirmed** | — | — | — | — | — | — | second user type and second publishing path. Neither GeoServer nor Honua has this. Q-59 to Q-62 open |
| AuthZ (§42) | **partial** | n/a | — | — | — | — | — | model decided in [security.md](security.md) §2–3. Multi-tenant resource isolation still open (D-04) |
| AuthN (§41) | **decided** | `ACCEPTED WITH CONDITIONS` | — | — | **partial** | — | — | [ADR-015](adr/ADR-015-authentication.md), 2026-08-13. **Blocker B4 discharged.** Three principals including anonymous-as-a-principal; **opaque server-side tokens rather than JWT**, which Q-70's mandatory PostgreSQL made cheap and which buys real revocation; local accounts first-class because air-gapped sites have no IdP; OIDC, SAML and SCIM free where Honua gates them at Pro and Enterprise. Two hard constraints shaped it: the identity must map to a **database role** for RLS delegation (A-047, five engines), and ArcGIS compatibility forces credentials into URLs (bounded, not fixed). Conditions in §9 |
| **Protocol conformance testing** | — | **none** | — | — | — | — | — | **Absent from the architecture, and Q-78 makes that untenable.** Honua publishes 1,117 passing tests across 13 suites. Claiming 29 protocol faces without conformance evidence is a weaker position than claiming six with it. OGC publishes the suites; this is a CI decision to be taken early, not discovered late |
| **Geocoding** (GeocodeServer) | **in scope (Q-84)** | — | — | — | — | — | — | **New subsystem, not an endpoint.** Address parsing (locale-specific — a Turkish-built product has an advantage here that Anglocentric open geocoders lack), matching and scoring, reverse and batch. **Reference data is the real question**, and it is GPServer's toolbox problem again: a geocoder with no street centrelines is an empty shell. Recommended: build the engine, customer brings the data — the only option that survives Q-15 air-gapped |
| **Observation store** (SensorThings, EDR) | **IN SCOPE — PENDING CONFIRMATION (Q-79)** | — | — | — | — | — | — | **New product surface, not a new endpoint** — a different domain model with its own storage, temporal indexing and write path. Q-79 asks whether it was chosen or swept in |
| **3D and terrain** (3D Tiles, Terrain-RGB) | **IN SCOPE — PENDING CONFIRMATION (Q-79)** | — | — | — | — | — | — | **No foundation in anything decided** — not the vector tile pipeline, not the raster engine. Q-79 |
| Observability (§46) | — | — | — | — | — | — | — | not started |
| Backpressure (§48) | — | — | — | — | — | — | — | shape set by ADR-007 §4.9. Fairness still depends on N4's per-source limit and D-04's per-tenant limits, neither of which exists |
| Runtime supervisor (§21) | **designed** | feeds ADR-007 | — | — | — | — | — | [runtime-supervisor.md](runtime-supervisor.md). Severe gap closed. Q-63 to Q-65 remain: routing placement, memory-growth detection, adoption protocol |
| User-uploaded content | **policy written** | feeds ADR-013 | — | — | **partial** | — | — | [security.md](security.md), 2026-08-13. First surface accepting arbitrary user bytes. Open: virus scanning posture, and whether an attachment inherits its feature's row-level security or carries its own grant |
| **TLS / certificates** | **decided** | `ACCEPTED WITH CONDITIONS` | — | — | **partial** | — | — | [ADR-014](adr/ADR-014-tls-and-certificates.md), 2026-08-13. **Severe gap closed, blocker B3 discharged, N8 closed.** Four surfaces; we terminate by default because *run our container* must not become *also run nginx*. Rotation without restart is architectural, not courteous — a restart evicts the warm state ADR-007 §4.4 protects (A-044). Expiry becomes a supervisor duty and **supplies one of F5's three missing 2 AM scenarios**. Also fixed §6's unwritten SSRF hole. Conditions in §9 |
| Failure scenarios (§59) | **walked** | n/a | — | — | — | — | ✓ | [failure-scenarios.md](failure-scenarios.md) |
| Geometry & CRS policy | **written** | n/a | — | — | — | — | — | [geometry-crs-policy.md](geometry-crs-policy.md). Per-engine claims need `VERIFY` |
| Resource governance (§49) | — | — | — | — | — | — | — | not started |
| Deployment profiles (§53) | **decided** | `ACCEPTED WITH CONDITIONS` | — | — | — | **partial** | — | [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md), 2026-08-13. **Blocker B5 discharged. Q-71, Q-13 and Q-76 answered; N9 closed.** Three images; version handshake **refuses rather than auto-migrates**; expand-and-contract; **rollback to exactly one version and only before contract**. Air-gapped delivery is a single verifiable bundle with nothing fetched at runtime, which is what decided Q-76 against pip. Developers run the same compose file — no second code path. Conditions in §10 |
| Licensing (§55) | WIP | n/a | n/a | n/a | — | — | n/a | see [DEPENDENCY-LICENSES.md](../DEPENDENCY-LICENSES.md) |
| Competitive position | **analysed** | n/a | — | — | — | — | — | [competitive-position.md](competitive-position.md). Q-49 attempted three times with no strong answer. GeoNode already does self-service publishing. What survives is a niche: the fully open ArcGIS Server exit path. **Needs validation with real GIS teams, which desk research cannot supply.** |

## Adversarial reviews (§85, §67)

| Round | Date | Findings | Status |
|---|---|---|---|
| 1 — adversarial | 2026-08-12 | 12 (3 severe) | [adversarial-review-1.md](reviews/adversarial-review-1.md) — all dispositions applied |
| 2 — fresh challenger (§67) | 2026-08-12 | 8 (3 severe) | [fresh-challenger-review-2.md](reviews/fresh-challenger-review-2.md) — **written by the same agent, which §67 forbids.** Findings are real; coverage is suspect. A genuine independent review is still owed. |
| 3 — three reviewers, no shared context | 2026-08-13 | **41 (17 severe)** | [independent-review-3-synthesis.md](reviews/independent-review-3-synthesis.md) — architecture, operations, process. Each had the repository and the master prompt and **no access to the reasoning that produced them**, which removed blind spots in reasoning but not in framing. **This does not discharge §67** and the process reviewer said so unprompted: *“Do not tick that box on the strength of this document.”* All three converged independently on one diagnosis: the record is falling behind the decisions |
| 4 — genuinely independent | — | — | **Still owed.** §67 requires a reviewer who did not participate. Rounds 1 and 2 were self-review; round 3 was commissioned and briefed by the author |
| **Contradiction sweep 1 (§63)** | 2026-08-13 | **11 (3 severe)** | [contradiction-sweep-1.md](reviews/contradiction-sweep-1.md) — blocker **B1 discharged**. S1 vector-first superseded by accumulation without ever being reversed; S2 in-scope capabilities whose ADR is `DEFERRED`; S3 **§82 applied to a list rather than per capability**, which is a binding rule that stopped being followed under scope pressure. Eight dispositions applied, three raised as Q-85 to Q-87 |

## Review gates (§66)

Each gate is run against the whole architecture, not per-ADR, and a failure
reopens the relevant decisions rather than being noted and passed over.

**Seven of nine run as of 2026-08-19, and the two blanks are the ones nobody here can fill.**
Correctness and Simplicity were run that day by independent reviewers — §67's *fresh challenger*, which
is what both rows had been waiting for and what the design reviews of the same week had shown works: a
reader who did not write the code found, in one pass each, a silent wrong answer on an advertised
capability and a subsystem whose reason to exist had been deleted by a later decision. **Operations needs
a deployment somebody operates and Extensibility has nothing to examine** (ADR-006 is `REOPENED` and no
plugin code exists), so those two stay blank and say why.

**Seven of nine were run, and the two blanks are deliberate.** A gate ticked without
evidence is worse than one left open: it converts an unknown into a false assurance,
and nothing afterwards re-examines it. The seven below were run because the evidence to
run them exists; the two blanks are named with what each is waiting for, and neither is
waiting on us.

**Five ran or re-ran on 2026-08-20**, against the four protocol faces added on
2026-08-19 and 2026-08-20, each by an independent reviewer who did not write the code —
which is §67's requirement and was the standing gap in every earlier round. **All five
failed.** Between them: twenty-two findings, sixteen repaired the same day, six recorded
as debt with the repayable form named. Two of the six are not defects but capabilities
scoped smaller than the ADR that describes them, and those are the ones worth reading:
a gate that only ever finds bugs is not looking at the architecture.

**What the day changed about the gates themselves.** Four of the five failures were
found by a question no single-face test can ask — *do two faces of this server agree*,
or *what does this face say while a dependency is gone*. The suite that had 1,564 tests
passing caught none of them. That is not an argument against the suite; it is the
argument for §66 existing beside it, and it is the first time this repository has
evidence for that rather than a policy.

| Gate | Run | Result |
|---|---|---|
| **Correctness** | **2026-08-20** | **FAIL, and all five findings are repaired.** [correctness-gate-2.md](reviews/correctness-gate-2.md). Re-run because the 2026-08-19 gate examined three protocol faces and there are now seven. **Every finding was a wrong answer with a 200 on it** and not one was caught by 1,564 passing tests: an axis rule that knew EPSG:4326 and not the class it belongs to, a negotiated CRS that changed the response header and not the coordinates, a MapServer document reporting a colour the server does not draw, an exact-instant `datetime` that could not match by construction, and a capability claimed in a document with no route behind it. **Four of the five needed a question no single-face test can ask** — do two faces agree about one layer — which is a tool that did not exist before this week. `GateFindingsTests` now carries one test per finding. Previous run 2026-08-19, also FAIL. |
| **Simplicity** | **2026-08-19** | **FAIL, narrowly, and not for the reason this row expected.** Run by an independent challenger — the *somebody other than the author* this row had been waiting for. **Its headline finding is that the architecture is markedly less overengineered than its own ADRs:** four external packages (Npgsql, NetTopologySuite, MaxRev.Gdal.Core, Konscious.Argon2), one deployable, one worker kind after ADR-037 reversed itself the day it was written, affinity routing and auto-pinning suspended by ADR-029 for want of the experiment that would justify them, L2 permanently optional, the MVT encoder correctly left in `benchmarks/`, tile encoding pushed into `ST_AsMVT`, and **no Redis, Kafka, Kubernetes, service mesh, event sourcing or CQRS** — §6's named challenge list is clean and the baseline really is one line. **What fails is that §6's question is never asked a second time.** Three findings share one shape: *a later decision removed an earlier subsystem's reason to exist and nothing went back to delete it.* **S1, disqualifying — repaired 2026-08-23** ([D-113](architecture-debt.md)): the parse is in the child process and the 860-line parser is deleted. The repair had to be measured rather than reasoned three times over, and one of those measurements is the reason it did not ship a correctness regression: GDAL's fast ring reading gave PostGIS 47 of 50 valid polygons where the deleted parser gave 50, and `OGR_ORGANIZE_POLYGONS=DEFAULT` restores it. **Original follows.** **S1, disqualifying:** ADR-037 §5a put GDAL in a child process on 2026-08-18 precisely to keep an untrusted-file parser out of the serving process, and **1,094 lines of our own shapefile parser still run inside it** under ADR-024, decided three days earlier against a rule that no longer holds — the newer format is isolated and the older one is not. **S2 — repaired 2026-08-23** ([D-114](architecture-debt.md)): the 92 lines of transverse Mercator are deleted and the UTM leg goes through PROJ, verified end to end in both directions to sub-metre. The row's own arithmetic was wrong — the engine was 92 lines of 963, not most of them, and the promised 6:1 reduction was never available because the notation work is 850 lines PostGIS has no function for. **What was removed is not measured in lines**: it is that two engines answered the same question and could differ silently. **Original follows.** **S2:** `GeoCoordinateString.cs` ships a hand-written transverse Mercator forward and inverse into Tier 1 — a second coordinate engine, which ADR-022 §4 refuses **by name** on the grounds that two engines *differ by metres and differ silently* — to close two operations that returned 404 in a feature-comparison table; §2c reclassifies it as *a closed-form series* and §4 is never revisited. **S3:** ADR-034's two-surface split is the sole cause of D-90, D-91, D-93 and D-98, and its own §5b makes the gate a privilege the API already enforces, which is Alternative A with one router. **Two costs found rather than recorded:** the connection budget answers a measured problem one layer too high — `LayerConnections` already bounds the *attachment* pool with two properties 77 lines below the query pool that sets neither, and the gate's own header names the case a request-counting bound cannot reach (D-110); and the repository holds **~46,000 lines of prose against ~31,000 of code** (33% of `src/` is comment; `IAdminCatalog.cs` is 73%), with the same facts restated in ADR bodies, inline remarks and debt rows, which is D-46's pattern in another medium. **The repair it asks for is process, not code:** `v1-scope.md` converted an observation into a cut exactly once and has not been used since — it is stale on the Python runtime and silent on the group axis's ~2,074 lines despite two later amendments — so **a reversal must trigger a sweep for what it just made unnecessary**, and nothing in CLAUDE.md §2 does. Findings are [D-113](architecture-debt.md), D-114, D-115 and D-116; re-run after the deletions, not before |
| **Performance** | **2026-08-20** | **FAIL, on a finding about behaviour rather than speed.** [performance-gate-2.md](reviews/performance-gate-2.md), run by an independent reviewer against the five new faces. **The connection budget refuses correctly on the two rendering faces and never refuses on the query faces** — concurrency 150, and 50 connections held open for 23 seconds, produced not one 503 — so the load shedding ADR-007 §4.8 describes protects the rare surface and is absent from the common one. Not repaired: a hold-time bound is a concurrency design change and needs its own ADR ([D-128](architecture-debt.md)). A second finding was repaired the same day — the refusal's documented `Retry-After` had never been sent. Three measurements answered questions that were open rather than finding defects: D-118's count costs 50–90% of a small page and 2–3% of a full dump; a full-precision map is at parity with the JSON of the same features rather than cheaper, for a fiftieth of the bytes; and WMS capabilities extrapolates to ~3.3 s cold at 1,000 services. **The previous run follows. 2026-08-15: FAIL, nothing repaired.** [performance-gate-1.md](reviews/performance-gate-1.md). Read together the four rounds say one thing — **the cost of this system is memory traffic and it is invisible at concurrency 1** (80.9% GC pause at 18% CPU; 404 MB for one z12 tile; 1.5× at concurrency 1 becoming 3.4× at 16). **F1, disqualifying: the feature query path has never been measured** — the word *query* appears in neither results document, and it runs the same WKB-to-object-graph mechanism the rounds indicted. A black-box probe run for the gate shows throughput plateauing at 5–7× for 24× concurrency, and says no more, because run-to-run variance reached 2.2×. **F2:** two of four rounds measured a component the architecture then decided not to have — though the encoder correctly never left `benchmarks/`. **F3:** the 30 s statement timeout and the per-provider connection budget still bound three subsystems apiece with nothing behind them. **F4: the harness has now been wrong three times**, and the third looked plausible. [D-30](architecture-debt.md). **Amended 2026-08-19: the disqualifying finding is repaired, and the gate is still FAIL.** F1 is answered — [benchmarks/feature-query](../benchmarks/feature-query/RESULTS.md) instruments the path in process (lookup, prepare, driver, decode, serialise; nothing timed unless the `query` logger is at Debug), and it found what four rounds of tile benchmarks could not have: the **per-request catalogue read is 1.8 ms flat and 70% of a one-feature query**, 2.6× the data query it precedes. §3b then measured concurrency and **the ceiling is not allocation** — 142–164 kB per request, flat, GC pause never above 2.5% — **nor CPU**, which contradicts the *memory traffic* headline the four rounds had agreed on. F4 gained a **fifth** wrong harness, this time the measurement setup itself (§3c), which is the finding rather than an embarrassment. **What keeps it FAIL: F3, and half of F3 was paid later the same day.** The 30-second statement timeout and the per-provider connection budget bounded three subsystems apiece with nothing behind them — that was [Q-04](open-questions.md), carried and blocking, and ADR-007 condition 3. **Q-04 is now answered by measurement and the bound is now enforced:** `(data sources + 1) × 100` potential per worker, `≈1.3 × concurrent requests` in practice, `8` at rest, and `ConnectionBudget` refuses past a configured ceiling instead of letting PostgreSQL do it with *too many clients already*. Condition 3 is discharged. ~~**What is left of F3 is the statement timeout**, which is still one number nobody has measured against a real workload~~ **Measured 2026-08-23** ([benchmarks/statement-timeout](../benchmarks/statement-timeout/RESULTS.md), [D-08](architecture-debt.md)), against a purpose-built million-row table because the existing corpus cannot falsify a thirty-second ceiling. **F3 is closed and the finding is not the number.** Nothing this server can be made to issue comes near thirty seconds: the most expensive statement reachable through any face is a full-extent render read at 330 ms, and the most expensive one a caller can steer is an unindexed sort at 221 ms. The two shapes that do approach the ceiling — a geodesic buffer over every row at 9.3 s, a spatial self-join at 5.8 s — **cannot be written through the query model**, which refuses function calls, subqueries and column-to-column comparison by name against the running server. That property was written into `AttributePredicate` for safety and has now been measured as a performance one. The timeout's real subject is a pathological plan and a lock wait — and F2 and F4 are unchanged. **The gate has not been re-run as a gate**, and it should not be recorded as one until it is: a row that reads its own repairs back as a pass is what §67 warns about. **That sentence still stands on 2026-08-23 and it is the reason this row is not a PASS.** Every finding it names is now closed — F1 by ADR-046, F2 the same day it was found, F3 by the million-row measurement above, F4 a nuance whose underlying defect is D-129 — and closing findings is not passing a gate. **The subject has also grown since**: admission control, a circuit breaker, a request log, four WFS repairs and a shapefile path that now crosses a process boundary all arrived after the last run, and none has been through this gate. What has been measured of them is narrow and is recorded where it belongs: [benchmarks/request-log](../benchmarks/request-log/RESULTS.md) says the four additions to the request path are not measurable on it, log on against log off, at three concurrencies. **Somebody other than the author has to run this one**, which is what the simplicity gate waited for and what made its finding worth having |
| **Failure** | **2026-08-20** | **FAIL, and six of ten findings repaired the same day.** [failure-gate-2.md](reviews/failure-gate-2.md), run by an independent reviewer against all seven faces — four of which had never been through this gate — with the database stopped and restarted six times. **The gate turns on a liveness probe that cost four seconds during an outage**: every route resolves a principal from the store first, including the probe that answers a constant, so an orchestrator with default settings would restart the container about thirty seconds in — a restart that does not reach the database and that empties the memory degraded serving runs on. **The outage would have removed its own mitigation.** Fixed and re-measured under a fresh outage: 10–22 ms. Second: a bad `srsName` returned HTTP 200 and a well-formed WFS document announcing a thousand features and carrying none; now refused before a byte is written. Also fixed: a caller's bad SRID reported as a database outage on four faces, one error envelope for seven protocols, truncation logged nowhere on WFS and OGC, a blind refusal that contradicted itself, and a portal wildcard that answered that a portal with twelve items was empty. ~~**Not repaired**~~ **Three of the four repaired 2026-08-23, each measured before and after.** Refusals during an outage went from 8.0 s to 10–21 ms — the circuit breaker ADR-007 §4.8 asked for as N3, and the eight seconds turned out to be two platform-store waits rather than one plus a data source ([D-131](architecture-debt.md)). 499 is written: an aborted request produced **no access line at all**, not a line saying 200, which is a worse defect than the row described ([D-132](architecture-debt.md)). And the two workers write about 10 kB a minute instead of 100, with a recovery line that closes the incident ([D-133](architecture-debt.md)). **Partly repaired:** ADR-026's degraded serving was narrower than it reads on two axes, and the second is closed — a described shape now survives an outage, so a layer document answers 200 at 100 seconds where it refused after thirty. **The first axis stands:** there is no degraded *listing* capability, so a folder listing serves 0 of 20 concurrent requests instantly during an outage where the authentication path serves 19 of 20 ([D-127](architecture-debt.md)). **Six of twelve scenarios executed, recorded rather than rounded up.** Recovery, readiness and malformed-input handling all held, the last of them well: 51 of 57 cases named exactly what was wrong. **The previous run follows. 2026-08-15: FAIL, and repaired in part.** [failure-gate-1.md](reviews/failure-gate-1.md): five of twelve scenarios executed against a running server. **`/healthz/ready` answered 200 while every query answered 503** during recovery — it probed the platform store and not the datastore, so a green probe stood in front of a red server. Fixed, and the probe is now conservative in the safe direction. **N1 and N2 are confirmed not built**: a platform-store outage is total rather than degrading, and a warm tile for a public layer is inside the blast radius — which is [Q-95](open-questions.md) with evidence attached. Seven scenarios were not run and are named rather than ticked |
| Operations | — | Needs a deployment somebody operates. A-051's break-glass path and D-18 are the known holes |
| **Security** | **2026-08-20** | **FAIL on one finding, repaired the same day.** [security-gate-2.md](reviews/security-gate-2.md). Re-run for the five faces added since the last one. **Session tokens were written to this server's own log in full**: the reviewer harvested a `root` token from the log file and replayed it against a private layer. The channel is ADR-015 §4's `?token=`, permitted under four required mitigations of which redaction is the first — and `Authentication`'s own remark still said the parameter was *not accepted yet*, so the code and its documentation disagreed in the direction of a guard that had been named as a precondition and skipped. `QueryRedaction` and `TokenIsNotLoggedTests` close ADR-015 condition 2. **What held is the larger half**: sharing on all five new faces, with private indistinguishable from nonexistent; injection on every path into `PredicateSql`; and the XML hardening that failed the last gate, tested with the exact D-122 vector. Previous run 2026-08-15, FAIL, repaired in part. |
| Extensibility | — | ADR-006 is `REOPENED` and no plugin code exists; the gate has nothing to examine |
| **Licensing** | **2026-08-15** | **PASS.** All six shipped and test dependencies verified from their own nuspecs — Npgsql (PostgreSQL licence), Argon2 (MIT), xunit ×2 (Apache-2.0), test SDK and TimeProvider.Testing (MIT). No GPL or AGPL anywhere. Found and corrected a register whose stated purpose contradicted Q-73. Transitive dependencies not checked — [D-06](architecture-debt.md) narrowed, not closed |
| **Consistency** | **2026-08-20** | **FAIL, and three of four findings repaired the same day.** [Contradiction sweep 3](reviews/contradiction-sweep-3.md), run by an independent reviewer against the four surfaces added on 2026-08-19 and 2026-08-20. Two behavioural findings — `GetLegendGraphic` accepted a style name `GetMap` refused, and a WFS error message named an implemented operation as missing — and two structural ones. **The largest is documentary**: ADR-004 read `DEFERRED` with §5 reading *Pending* while the renderer it defers was shipped and serving, and eight further documents restated the deferral as current. All nine corrected, and **the missing propagation check is written as of 2026-08-23**: `registers-check.py` fails the build when an ADR named as amended does not mention the ADR that amended it, and it caught one on its first run ([D-126](architecture-debt.md), closed, with the general form [D-130](architecture-debt.md) closed beside it). The fourth is not a defect — degraded serving through a platform-store outage is implemented on three of the seven faces that resolve a published service, and cannot be extended to the other four without a listing capability that does not exist ([D-127](architecture-debt.md)). **The previous run follows. 2026-08-15: FAIL, and repaired in part.** [Contradiction sweep 2](reviews/contradiction-sweep-2.md): twelve findings. The largest was that twelve of twenty-two ADRs still described a three-database product ([D-27](architecture-debt.md)) — **closed 2026-08-18** when the owner settled that the cut is a deferral rather than a removal, which supplied the per-paragraph rule the repair needed: all thirteen affected ADRs now open with the same scope note and the six present-tense claims are corrected. Four findings were new; four were already recorded at their point of decision; four are now fixed |

## Phase 0 exit criteria (§81)

**Assessment 2026-08-13:** [phase-0-exit-plan.md](phase-0-exit-plan.md). Four of
sixteen met, but the count is misleading in both directions. Several criteria
below cannot be met by Phase 0 at all — eight ADRs carry conditions that are
*numbers* only a running system produces. Five items genuinely block the first
production line, and they are not the five with the biggest boxes.



- [x] `architecture-assessment.md` complete — all 27 required sections (§70).
      **First complete draft, 2026-08-12. Not yet reviewed.**
- [x] Initial ADRs written, **none still `DRAFT`** — 2026-08-13. ADR-003 was the
      last one and is now `ACCEPTED WITH CONDITIONS`. Sixteen ADRs; two
      `DEFERRED` with reasons (ADR-004 rendering, ADR-012 clustering), two
      `REOPENED` by scope decisions (ADR-006 plugins, ADR-009 raster).
- [ ] Critical questions (§80, all 40) answered or explicitly deferred with cause
- [ ] Load-bearing assumptions validated — at minimum A-003, A-004, A-007
- [x] **Failure scenario pass complete** (§59) — adversarial review F9.
      [failure-scenarios.md](failure-scenarios.md), 2026-08-12. **Found ten gaps,
      three severe**: no runtime supervisor (N5), no cache size budget (N6), TLS
      absent from the architecture (N8). Two constraints written back into
      existing decisions, three additions, two decisions we had not realised we
      needed. Network partition between nodes is not walked, since clustering is
      deferred.
- [x] **2 AM scenarios written end to end** — adversarial review F5, met
      2026-08-13 in [ADR-017](adr/ADR-017-admin-api.md) §3. **Four rather than three**: stale tiles, a
      slow service, a failed registration, and an expired certificate, which
      ADR-014 added and which is the only one with a known date in advance.
      Written as the *design method* for the admin API rather than as a
      documentation exercise, and F5's test earned its keep — each scenario
      produced a requirement a CRUD-first surface would have missed. The largest:
      **worker introspection must expose allocation rate and GC pause**, because
      A-037 measured 80.9% GC pause at 18% CPU and a CPU graph alone shows an
      idle worker and explains nothing.
- [x] **Geometry and CRS reality pass** — fresh-challenger review G4.
      [geometry-crs-policy.md](geometry-crs-policy.md), 2026-08-12. Found that
      five separate problems are one problem: **lossy on read means not
      writable**, now enforced by the write path in ADR-008 §4.5a. Answered Q-56
      and gave Q-36 its first concrete requirement. Per-engine behaviour still
      needs verification.
- [~] **Air-gapped checklist — written 2026-08-13, not yet tested.** [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §7 makes delivery a single verifiable bundle and forbids runtime fetching: no pip (Q-76), no ACME, no PROJ grid downloads, no GDAL driver fetch, soft-fail revocation. **What remains is whatever the rendering engine needs**, since ADR-004 is deferred — fonts and glyph packs. The criterion says *and tested*, and §10 condition 3 is the test: install on a machine with no network route. Original: **Air-gapped checklist written and tested** — adversarial review F11.
      Q-15. Offline PROJ grids, GDAL driver data, fonts, MapLibre glyph packs and
      sprites, COG-capable clients, and any rasteriser's display requirements.
      Tested by attempting an install with no network.
- [ ] High-risk decisions have prototypes
- [ ] Performance-sensitive decisions have benchmarks — **tile path done three times**: [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) settles A-019, A-004, A-001, A-037 and A-021-on-PostGIS, and opened A-039. Remaining: connection budget, worker model, affinity routing, seeding, feature-query streaming, and a capacity number on hardware that is not capped and contended. **The same tile path on SQL Server and Oracle is deferred as [D-05](architecture-debt.md)** — accepted debt, not an oversight
- [ ] **Contradiction sweep — `REOPENED` 2026-08-13.** Round 1 found eleven contradictions and discharged blocker B1; independent review 3 found that **all eleven were forward-facing** (scope added without propagating) and **zero were backward-facing** (premises deleted without propagating), which is the larger and more mechanical class — A1, A2, A5, A6, A7, P11, P14. A sweep that discharges its blocker while walking past a self-contradicting ADR produces false assurance. Original entry: **Contradiction sweep run** (§63) — [contradiction-sweep-1.md](reviews/contradiction-sweep-1.md), 2026-08-13. **Run, not clean**: 11 findings, 3 severe, 8 dispositions applied and 3 open as Q-85 to Q-87. The criterion says *clean*, so it stays unticked until those three close. **The finding to carry forward is S3** — §82 stopped being applied under scope pressure, and the next sweep cannot catch that, because a missing justification looks exactly like one nobody wrote down
- [ ] Adversarial review complete, every material criticism resolved or recorded
- [ ] Fresh-challenger review complete (§67)
- [ ] Licensing implications understood
- [ ] No blocking open question remains
- [x] **Q-49 answered by the owner, 2026-08-13** — *"I will give this to the world."* **The requirement to test it with real GIS teams is dissolved rather than met**: it assumed a commercial-style justification a gift does not owe. The positioning was sharpened at the same time, from *better capabilities than GeoServer* (measurably false) to *the ArcGIS Server exit path* — [competitive-position.md](competitive-position.md) §6. What survives is a **prioritisation** risk, not an existential one: nobody outside the project has confirmed the exit path matters, so we may build the right thing in the wrong order. Original criterion: **Q-49 tested with real GIS teams.** [competitive-position.md](competitive-position.md)
      concludes that desk research cannot answer why this product should exist.
      Three conversations with organisations running ArcGIS Server would settle
      it, and every architectural decision after this one is cheaper to make with
      an answer than without.
