# Architecture Assumption Register

Every assumption an architectural decision rests on is recorded here (§11).

**Statuses:** `UNVALIDATED` · `VALIDATING` · `VALIDATED` · `CONTESTED` · `INVALIDATED` · `SUPERSEDED`

`CONTESTED` means evidence points both ways and the assumption is probably
stated at the wrong granularity. It is a signal to split the assumption, not to
pick a side.

**The rule that gives this register teeth:** invalidating an assumption triggers
review of every ADR listed in its *Depended on by* column. An assumption that no
ADR depends on is either mislabelled or unnecessary.

---

## Open assumptions

| ID | Assumption | Status | How it gets validated | Depended on by |
|---|---|---|---|---|
| A-001 | The tile path is CPU-bound enough that language performance materially affects capacity | **`VALIDATED` 2026-08-12** | Confirmed by [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md). With `ST_AsMVT` unavailable on two of three providers, in-process encoding is real CPU work: 94 ms for a dense tile of which only 23 ms is database time. The earlier note that it might be database-bound applied only to the PostGIS fast path | ADR-001, ADR-003 |
| A-002 | Single-binary distribution is genuinely valuable for air-gapped installs, not just aesthetically pleasing | `UNVALIDATED` | Ask the owner; check real air-gapped install constraints | ADR-001 |
| A-003 | Most published services are idle most of the time, making shared workers viable | **DOWNGRADED to informational 2026-08-15** — [ADR-029](adr/ADR-029-affinity-routing-is-not-the-default.md) removed affinity routing from the default, so this holds nothing up. It becomes load-bearing again the moment affinity is reconsidered. Previously: `UNVALIDATED` | Workload modelling; any real deployment telemetry we can obtain **The peer was checked 2026-08-15 and it is not a route.** The closest peer runs **stateless instances with state in Redis**, so the idle ratio cannot exhaust a context budget they do not have. **Re-verified 2026-08-16 against the implementation itself** rather than against a summary of it — Redis 7.4 in their default compose, described there as the durable control plane, and a `ConnectionStrings__Redis` the server requires ([reference-reading-log.md](research/reference-reading-log.md)). The original write-up this cited was deleted as unreliable; the claim survived because it was checkable. They did not answer this assumption; they removed it, by taking the dependency §82 rejected. **Our exposure on A-003 and A-014 is the price of not taking Redis** — which is a trade worth stating as one. | ADR-007, ADR-010 |
| A-004 | Hot-path geometry overhead is material enough to justify our own primitives | **`VALIDATED` 2026-08-12, decisively** | [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md). `NTS.Intersection` was **79% of the whole request**; our rectangle clipper took that stage from 438.6 ms to 7.0 ms, a 63x reduction. What we wrote is fast (transform 1.4 ms, encoder 3.8 ms); what we adopted was slow | ADR-003, tile pipeline |
| A-005 | Geometry running in the same runtime meaningfully reduces defect resolution time versus FFI | `UNVALIDATED` | Judgement plus prototype experience; record honestly in `experiments/lang-slice` | ADR-001, build-vs-adopt policy |
| A-006 | One internal geometry representation can serve both the feature path and the tile path without a second conversion | **`CONTESTED` 2026-08-13** — and by this register's own definition that means it is stated at the wrong granularity and should be split | Evidence points both ways. **Against:** the tile path measured ~139 bytes allocated per vertex read — three to four copies of every coordinate — and **both** primitives we chose to own won precisely by *leaving* the shared representation for flat arrays ([benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) findings 7 and 10). **For:** topology genuinely needs geometry objects, and GeometryServer is v1 core (Q-17a), so they cannot be abolished. Likely resolution is two representations with an explicit conversion where topology is required — which is **Q-66**, and [ADR-003](adr/ADR-003-geometry-engine.md) §6 deliberately does not decide it | ADR-003, Q-66 |
| A-007 | Crash containment is required in practice, not merely in principle — workers really do die (GDAL on malformed input, plugins, OOM) | `CONTESTED`, weakening | GeoServer runs every service in one JVM with no isolation and is widely deployed successfully — evidence *against*, for managed-code paths. ArcGIS and QGIS Server run large native stacks and isolate — evidence *for*, on native paths. **Weakened further 2026-08-12:** the vector-first decision removes GDAL raster decoding from the request path, which was the strongest concrete case for isolation. Remaining candidates are registration/overview generation (job-shaped anyway), geoprocessing, and plugins. Resolve per-path via failure scenario review (§59) and fault injection, not globally | ADR-007, ADR-009, ADR-006 |
| A-008 | Administrators will not correctly hand-tune per-service worker settings, so defaults must be good and adaptive | `VALIDATING` — supported by prior art | ArcGIS Server's documented guidance asks administrators to "pare down the number of running service instances to as many as are needed", a per-service manual task at a scale where it will not happen. See [research/arcgis-som-soc.md](research/arcgis-som-soc.md) §4 (P8). Still needs a real operator's view to move to `VALIDATED`. | ADR-007 |
| A-011 | A distinguished central manager process is a robustness and recovery liability, so placement and routing state must be recoverable without one | `VALIDATING` — supported by prior art | Esri removed the SOM/SOC split at 10.1 citing robustness, reduced failure and simpler provisioning and recovery. See [research/arcgis-som-soc.md](research/arcgis-som-soc.md) §3.1 | ADR-007, ADR-012 |
| A-012 | The sharing question is really about per-service state size, binding cost and neighbour tolerance — not about "shared versus dedicated" | `VALIDATING` — supported by prior art | ArcGIS shared instances are restricted to map and image services with limited capabilities, and exclude geoprocessing; `VERIFY` ~50 cached service contexts per instance. See [research/arcgis-som-soc.md](research/arcgis-som-soc.md) §3.4 | ADR-007, §20 worker classes |
| A-013 | Our Tier 2 dependencies (GDAL, GEOS, PROJ) are thread safe enough to permit a threaded worker model | `VALIDATED` (2026-08-12) | Confirmed against upstream documentation: GEOS reentrant C API with one context per thread; PROJ one `PJ_CONTEXT` per thread; GDAL re-entrant with one dataset instance per thread. None forces process-per-worker. Three derived constraints remain live — see [research/dependency-thread-safety.md](research/dependency-thread-safety.md) §6 | ADR-007, ADR-003, ADR-009 |
| A-014 | Routing requests to workers already warm for a service materially improves L1 hit rate without wrecking load balance | **DOWNGRADED to informational 2026-08-15** — [ADR-029](adr/ADR-029-affinity-routing-is-not-the-default.md) removed affinity routing from the default, so this holds nothing up. It becomes load-bearing again the moment affinity is reconsidered. Previously: `UNVALIDATED` | Prototype and benchmark under both uniform and skewed load. See [research/runtime-models-compared.md](research/runtime-models-compared.md) §3 | ADR-007, ADR-010 |
| A-015 | Per-service warm state is small — connections, schema, symbology, fonts, CRS — making bind/unbind cheap | `UNVALIDATED` | Measure. GeoServer's documented cache inventory supports it; if true it changes the shared-vs-dedicated calculation substantially | ADR-007, ADR-010 |
| A-018 | ~~A deliberately boring platform schema can be supported across SQLite and PostgreSQL at acceptable cost~~ | `SUPERSEDED` 2026-08-12 by **Q-70** — **dissolved rather than validated.** PostgreSQL is present in every deployment, so there is one platform-store dialect and no portability requirement to meet. The assumption went from four engines (original) to two (Q-51) to one (Q-70) in a single day, and at one engine it is not an assumption | Narrowed from four engines to two on 2026-08-12. Two dialects for non-spatial relational data is ordinary work rather than a structural risk. The four-way CI matrix and four job-claim implementations are gone | ADR-002, ADR-011 |
| A-019 | In-process MVT encoding can meet our latency targets | **`VALIDATED` 2026-08-12 — and demoted the same day: no longer load-bearing.** Q-67 removed its premise |  [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md): 94 ms against `ST_AsMVT`'s 62 ms on a dense z14 tile. The assumption existed to underwrite tiles from engines lacking `ST_AsMVT`; **Q-67 decided there are none**, so it is true but no longer carries anything. What it still informs is Q-68 — whether our own encoder is worth keeping for seeding | ADR-001, ADR-008, Q-68 |
| A-020 | Cache seeding absorbs the provider performance gap, so an expensive Oracle tile path is acceptable when paid once rather than per request | `UNVALIDATED` | Measure seed time for a realistic service set, and invalidation behaviour when data changes. See [research/hosted-datastore-and-tiles.md](research/hosted-datastore-and-tiles.md) §3 | ADR-010, ADR-008 |
| A-036 | RLS delegation and large streaming reads do not collide often — the layers wanting row-level security are not usually the layers wanting bulk export | `UNVALIDATED` | A delegated query runs in an explicit transaction holding `ACCESS SHARE` for the whole stream, which is exactly what ADR-007 §5b says must be short. Mitigations: statement timeouts, a lower feature cap on delegated layers, and delegation being per layer. If the same layers want both, this needs a better answer. See [security.md](security.md) §2.4 | ADR-007, ADR-008, security |
| A-035 | Data source count stays low relative to service count — many services share one registered database | `UNVALIDATED` — **unbounded** | [ADR-007](adr/ADR-007-service-runtime.md) §4.8's connection budget survives only because pools are per data source rather than per service. But nothing bounds source count, and a large enterprise plausibly registers a departmental database per team. Fifty sources at four workers and a pool of three is 600 connections — the same shape of number §4.8 was supposed to have eliminated. Auto-discovery as a first-class publishing mode makes high counts *more* likely. Raised by adversarial review F8 | ADR-007, Q-04 |
| A-032 | COG range-request traffic is view-proportional, so proxying it costs about what serving tiles costs | `UNVALIDATED` | [ADR-009](adr/ADR-009-raster-engine.md) §2.4 rests on it. A client fetches overview blocks for what is on screen, not the dataset — but this is a reasoned estimate, not a measurement. If imagery traffic dwarfs tile traffic the default flips to signed URLs | ADR-009 |
| A-033 | Target clients can render COG directly, so serving one format suffices | `UNVALIDATED` | True of modern web mapping stacks, false of simple WMTS consumers and older desktop tools — some of which are exactly what we are displacing. See [ADR-009](adr/ADR-009-raster-engine.md) §10 | ADR-009, compatibility layer |
| A-034 | Internal ports designed for our own use will be good enough to become a plugin contract later, without a redesign | `UNVALIDATED` | The risk is that internals grow assumptions no clean boundary can be drawn around, so the answer becomes "we cannot" rather than "we chose not to". Check at each phase gate | ADR-006 |
| A-030 | Lease expiry plus checkpoint verification prevents one job running twice across nodes | `UNVALIDATED` | A partitioned worker can keep working on a job whose lease was reclaimed elsewhere. Mitigated by verifying the lease at checkpoints. Test with induced partitions | ADR-011 |
| A-031 | Job rate stays low enough for a database-backed queue — jobs per minute, not messages per second | `UNVALIDATED` | If false, [ADR-011](adr/ADR-011-job-system.md) §3.1's rejection of an external broker reopens. Measure against a realistic estate doing registration, seeding and geoprocessing together | ADR-011 |
| A-028 | Administrators can and will declare layer volatility usefully, and keep it roughly accurate | `UNVALIDATED` | Unlike worker tuning, this is domain knowledge only the administrator has, so asking is reasonable. The risk is that it is set once at registration and never revisited while the data's real cadence changes | ADR-010 |
| A-029 | Tiles, not feature query responses, are where cache reuse actually is | `UNVALIDATED` | [ADR-010](adr/ADR-010-caching.md) §2 declines to cache feature responses thoroughly because their key space is unbounded. ArcGIS built a dedicated store for exactly that, so this is a real bet. Check against production traffic before treating it as settled | ADR-010 |
| A-026 | OGC API Features Parts 1+2+3 plus additive extensions can express what §28 requires, without bending the standard's meaning | `UNVALIDATED` — **and substantially more load-bearing after Q-78** | Map §28's requirements — statistics, aggregation, relationships, domains, subtypes, attachments, editor tracking — against the standard and count what must be an extension. A large extension set means the standard is a veneer and [ADR-005](adr/ADR-005-api-architecture.md) §10's dissent was right. **Q-78 raises the stakes considerably**: the feature engine now carries **six faces** — OGC API Features, WFS-T, ArcGIS FeatureServer, OData v4, gRPC and MCP — with different query languages, identity models, transaction semantics and error conventions. ADR-005's protocol-neutral interface was asserted, never proven, and an abstraction exercised by one implementation is not an abstraction. **This is the same question at three times the pressure**. **Scheduled 2026-08-19:** [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md) builds **face two** — WFS 2.0 read-only, with a filter language that is XML rather than SQL text, string feature identity where ArcGIS requires a unique integer, and an exception-report error convention. So this assumption stops being a thing to worry about and starts being a thing that gets tested, and the test has a shape: **it fails if a second filter front end cannot target one emitter** ([D-117](architecture-debt.md)), or if the adapter has to reach into the core to work | ADR-005, [protocol-surface.md](protocol-surface.md) §3 |
| A-027 | Optimistic concurrency can be made correct against writes we never see, by relying on database-maintained versioning rather than our own event record | `UNVALIDATED` — **load-bearing for editing** | Design the concurrency check against a database-maintained row version on each provider, then test it by writing around the API. Getting this wrong produces silent lost updates | ADR-005, ADR-008, §28 |
| A-025 | A small number of pinned service contexts suffices for real deployments, and administrators will not pin everything | `UNVALIDATED` | Observe pin counts in real use. The global pin budget in [ADR-007](adr/ADR-007-service-runtime.md) §4.12 is the backstop if this is wrong; needing the backstop routinely means the guidance failed | ADR-007 |
| A-024 | A small residual executor plus explicit refusal is acceptable to real users, given a capability report published in advance | `UNVALIDATED` | The load-bearing assumption behind [ADR-008](adr/ADR-008-query-engine.md) §2 and §4.3. Validate against real query patterns; the evidence that invalidates it is a list of refused queries users legitimately need | ADR-008, ADR-005, Q-19 |
| A-022 | ~~The datastore schema can be created on PostGIS, SQL Server Spatial and Oracle Spatial at acceptable cost~~ | `SUPERSEDED` 2026-08-12 | **Retired by Q-32.** The datastore is PostGIS only, shipped as a managed appliance. The three-engine requirement was an over-application of the no-mandatory-PostgreSQL decision: that objection was about the operational burden of running a database, and a managed appliance removes most of it. Removes three spatial DDL implementations | — |
| A-023 | A cheap schema fingerprint can be polled often enough to detect drift on registered sources without loading the source database | `UNVALIDATED` | Measure `information_schema` query cost against a large database with many registered layers (Q-43). If too expensive, fall back to checking on error plus TTL | ADR-007, ADR-010, [data-model.md](data-model.md) §3 |
| A-021 | Filter and predicates can be pushed down usefully on all three spatial dialects — **narrowed by Q-67: clip and simplify no longer need to be, since only PostGIS serves tiles. The feature path still needs filter pushdown on all three** | **`VALIDATED` on PostGIS 2026-08-12; `UNVALIDATED` on the other two, and now the project's most important unmeasured thing** | [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) run 3, findings 11 and 12. **The question was mis-stated as tuning.** Without pushdown a z16 tile reads 201,580 vertices to emit 2,080 — four administrative polygons overlapping every tile in the city are 90% of it. `ST_Simplify` + `ST_ClipByBox2D` in the database: 13x on latency and 15x on allocation at z16, parity with `ST_AsMVT` at z12. For SQL Server and Oracle the question is no longer *is in-process encoding fast enough* but *can clip be pushed down at all* — which sharpens [D-05](architecture-debt.md) | ADR-008, A-019, A-037, D-05 |
| A-017 | Data sources will frequently be foreign and possibly read-only, so the platform cannot rely on DDL rights in them | `VALIDATING` | Follows from the confirmed migration goal — an organisation displacing GeoServer has PostGIS administered by someone else. Confirm via Q-08. Load-bearing for [ADR-002](adr/ADR-002-primary-data-architecture.md) §4.1–4.2 | ADR-002, ADR-008, publishing (§38) |
| A-016 | GDAL-backed providers can be made optional, so a PostGIS-only deployment ships as one artefact | `VALIDATED` by design decision, 2026-08-12 — **and possibly understated, see A-038** | Adopted as a rule rather than tested as a hope. **Amended 2026-08-19: the boundary is the process, not the image.** It read *the serving container ships no GDAL; it lives only in the job worker image*, and [ADR-037](adr/ADR-037-job-workers-come-in-two-kinds.md) §5a spent that — GDAL is a package in this solution now, loaded by a child process. **The assumption itself holds and is what the row claims:** a PostGIS-only deployment still ships as one artefact, and the serving *process* still never loads GDAL. Any provider requiring GDAL is therefore an import source, not a serving provider, which is unchanged. ~~**A stricter form is available and is now recorded in Q-28**: keep GDAL out of the managed dependency graph altogether and reach it as command-line tools in the worker image, which a build-time test can enforce where an image boundary cannot~~ — **that stricter form is spent**, and what replaced it is `NativeDependencyTests`, which enforces one project per confined dependency and no path from the host to it. Narrower, still mechanical. [D-88](architecture-debt.md), ADR-009 §2.2, ([reference-reading-log.md](research/reference-reading-log.md)) | ADR-001 C7, ADR-009, deployment, Q-15 |
| A-037 | Allocation rate, not CPU, sets the tile-serving ceiling per worker | **`VALIDATED` 2026-08-12, decisively** — measured the day it was opened | [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) run 3. At concurrency 16 the process was suspended for GC **80.9% of wall-clock while using 2.92 of 16 cores** — 18% CPU utilisation. Sixteen times the concurrency bought 1.6x the throughput. Control: `/health` sustains 1,984 req/s at 0.0 MB/request, so it is not the framework. Pushdown (A-021) raises the ceiling 2.5x but does not remove it — 65.6% GC pause remains | ADR-007 §4.14, ADR-010 §6a, Q-66 |
| A-038 | GDAL is needed for the import formats we care about at all | **`INVALIDATED` 2026-08-16 — the premise it rested on is false.** It was `VALIDATED` 2026-08-13 by an inference in two steps: their import list includes File Geodatabase, *and* no managed .NET reader for that format exists, therefore GDAL is unavoidable. The second step is wrong. The peer implements File Geodatabase import in managed code — a `FileGdb` reader family under their import services, established from file names, contents deliberately not read — and their solution declares **no GDAL or OSGeo package reference at all**; GDAL appears only as command-line tools inside a separate worker image. So a peer serves the format this assumption called the blocker, without the dependency this assumption called unavoidable. **§11 therefore puts ADR-009, A-016, Q-15 and Q-52 under review**, and the replacement question is a cost question rather than a possibility one — [Q-108](open-questions.md). Note what does *not* change: nothing here says writing such a reader is cheap, only that it is possible, and the format's public reverse-engineering (GDAL's own `OpenFileGDB` driver is open source, with published format notes) is what makes it possible without touching anybody's source. Original reasoning retained below, because it was sound in shape and wrong in a premise, and that distinction is the useful record. ~~Opened because the peer's manifest showed managed .NET readers and no GDAL binding. **Settled by their published import list**: it includes **File Geodatabase (`.gdb.zip`)**, for which no managed .NET reader exists — the practical routes are GDAL's `OpenFileGDB` driver or Esri's SDK. For the ArcGIS exit path specifically, `.gdb` import is close to mandatory, since it is the format an Esri estate's data actually arrives in. **Managed readers cover most formats; GDAL covers the one that matters most to our chosen positioning.** A-016's placement of GDAL in the job-worker image stands, and the dependency is not avoidable~~ | ADR-009, A-016, Q-15, Q-52, [Q-108](open-questions.md), [reference-reading-log.md](research/reference-reading-log.md) |
| A-039 | ~~Every provider can clip server-side cheaply enough to avoid shipping whole geometries~~ | `SUPERSEDED` 2026-08-12, hours after it was opened — **by Q-67, not by evidence** | Follows from [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) finding 11: a tile's cost floor is set by the largest geometry overlapping it, not by its content, and that is engine-independent — any client selecting whole geometries by bounding box pays it. PostGIS answers with `ST_ClipByBox2D`; `.STIntersection()` and `SDO_GEOM.SDO_INTERSECTION` were the untested candidates on the other two. **Q-67 removed the requirement rather than answering it** — only PostGIS serves tiles, so only `ST_ClipByBox2D` matters. The underlying finding does not go away: the giant-geometry floor still applies to **bbox feature queries on every engine**, where it is semantically correct (the user asked for features intersecting the box, and Turkiye does intersect it) and governed by [geometry-crs-policy.md](geometry-crs-policy.md) §6 rather than by clipping | superseded — see Q-67 |
| A-040 | All three engines can stream BLOBs out of a query without materialising them in our process | `UNVALIDATED` — **new 2026-08-13, load-bearing for attachments** | PostgreSQL `bytea`, SQL Server `varbinary(max)` and Oracle `BLOB` all document streaming access, with different APIs and different behaviour under TOAST, row-overflow and out-of-line storage. If any engine cannot stream, attachments on that engine materialise arbitrarily large user-chosen payloads and reproduce A-037's GC ceiling on demand. Validate with a large-attachment round trip watching allocation | [ADR-013](adr/ADR-013-feature-service-data-model.md) §4a, ADR-008 |
| A-041 | A bounded separate connection pool is sufficient protection against slow-client exhaustion during attachment streaming | `UNVALIDATED` — **new 2026-08-13** | Database-stored attachments hold a pooled connection for as long as the client takes to read. A client reading one byte per second holds it indefinitely; enough of them exhaust the pool and the whole layer stops serving. Pool isolation contains the blast radius to attachments. If it proves insufficient, buffer-and-release above a size threshold is the fallback — **which reintroduces the memory problem streaming exists to avoid, and is the trigger to reopen the storage decision** | [ADR-013](adr/ADR-013-feature-service-data-model.md) §4b, §9, ADR-007 §4.8 |
| A-042 | Caps on vertex count, batch size and wall clock are sufficient to make a public general-overlay endpoint safe | **`INVALIDATED` 2026-08-14** by [benchmarks/geometry-overlay](../benchmarks/geometry-overlay/RESULTS.md). A 6,408-vertex adversarial comb pair cost **153 seconds and 16.7 GB** where a real 72,919-vertex national outline cost **312 ms and 17 MB** — eleven times smaller, 490× the time. Vertex count does not discriminate between the safe case and the attack, so any cap generous enough to serve real data admits the attack. Candidate segment pairs predict an order of magnitude better and still under-predict the adversarial case by 14×. **No input-derived cap is sufficient**; bounding the work needs a bound on execution, which OverlayNG offers no cooperative way to do. Triggers review of everything resting on it — see [Q-97](open-questions.md). | GeometryServer is v1 core (owner: *crucial*) and publishes `intersect`, `difference`, `union` and `cut` — general polygon overlay on caller-supplied geometry. Run 1 measured `NTS.Intersection` at 438.6 ms, 79% of a request, and the fix there was to **stop calling it**; that escape does not exist when overlay is the product. A caller may post a 72,919-vertex national outline, which is real data in our own test set. Validate by measuring overlay cost against vertex count to find where the cap must sit — **and by checking whether a cap low enough to be safe is still high enough to be useful** | ADR-005 §3.3b, ADR-007 §4.9, A-037 **Superseded in effect 2026-08-15, not reinstated.** The mechanism A-042 named — caps on vertex count, batch size and wall clock — is still insufficient and this row stays INVALIDATED. What changed is that overlay ships anyway, bounded by a killable process rather than by a number ([ADR-022](adr/ADR-022-geometry-server.md) §9, Q-97). The distinction matters: the assumption was not rescued, it was routed around. |
| A-043 | Six geometry engines can be made to agree closely enough that the same query returns the same answer on any provider | **`PARTLY VALIDATED` 2026-08-19 — for the two engines v1 has, and the measurement is narrow on purpose.** Previously `UNVALIDATED` — *new 2026-08-13, and it may be false* | Q-80 and Q-81 take the count to six: PostGIS's GEOS, DuckDB's GEOS, MySQL 8's Boost.Geometry, MariaDB's, SQL Server's, Oracle's SDO_GEOM, plus NetTopologySuite in-process. They differ on validity, precision, `touches`, and empty-geometry results. **Measured where it can be: [experiments/geometry-oracle](../experiments/geometry-oracle/RESULTS.md), 448 comparisons.** v1 ships two of the six — PostGIS's GEOS for every spatial filter on a query, and NetTopologySuite in the overlay worker for `GeometryServer/relation` — and six named predicates plus eight DE-9IM patterns are answerable by both. **They agree on every case expressible through the product, at two orders of coordinate magnitude**, including the four edges this row names: an invalid bowtie (neither throws, both answer identically, and both say a self-intersecting polygon is *not* within itself), 1e-9 precision in both directions with no snapping, `touches` on shared edges, vertices and collinear lines, and a polygon in a hole. The one divergence is empty geometry, where PostGIS answers and our surface refuses — **and the product refuses it at the door in a shared reader, so no path reaches one engine and not the other**. 420 of those comparisons are now a test, falsified by making `Touch` mean `Crosses`. **What this does not say:** anything about DuckDB, MySQL, MariaDB, SQL Server or Oracle, which are four of the six and are deferred with the decisions that would bring them in — so *six engines* remains unvalidated as written. And `st_dwithin` has one engine, so it has no oracle at all. **If false for a later provider, and it probably will be at the edges, the honest response is not to hide it but to publish a per-provider conformance position** — the capability report cannot help, because every engine claims `intersects`. Validate with `experiments/geometry-oracle`: one corpus of adversarial geometries, all six engines, diff the answers | ADR-003, ADR-008, Q-20, Q-80, Q-81 |
| A-044 | Certificate rotation can take effect on the next handshake without recycling the listener or the worker | `UNVALIDATED` — **new 2026-08-13, load-bearing** | [ADR-014](adr/ADR-014-tls-and-certificates.md) §2b. If false, rotating a certificate means restarting a worker, which evicts every warm service context and triggers the cold-start storm ADR-007 §4.4's affinity routing exists to prevent — on a schedule, for a reason unrelated to any service. Validate by rotating under load and watching warm contexts survive | ADR-014, ADR-007 §4.4, §4.16 |
| A-045 | Soft-fail revocation checking is an acceptable posture for the air-gapped profile | `UNVALIDATED` — new 2026-08-13 | OCSP and CRL endpoints are unreachable offline, so hard-fail makes an air-gapped install validate nothing successfully. Soft-fail is the only workable default and is **a real weakening of the guarantee, not a technicality**. Validation here is a documentation and disclosure question rather than a test: does an operator who needs hard-fail have a supported way to get it | ADR-014 §7, Q-15 |
| A-046 | An opaque-token session lookup per request is affordable at the concurrency ADR-007 targets | `UNVALIDATED` — **new 2026-08-13** | [ADR-015](adr/ADR-015-authentication.md) §3's central bet. One indexed primary-key read against a database we already hold a pooled connection to, cacheable in-process with a short TTL. If false, the fallback is a longer TTL, which trades revocation latency for throughput — **and that trade must become a stated number rather than a default**. Measure at the concurrency A-037 was measured at | ADR-015, ADR-007 |
| A-047 | Every provider supporting RLS delegation can accept a principal name we generate, through administrator-controlled mapping | `UNVALIDATED` — **new 2026-08-13, and it spans five engines** | [ADR-015](adr/ADR-015-authentication.md) §1a. Authorization delegates to row-level security with transaction-scoped `SET LOCAL ROLE` (D-01), so an authenticated principal must map deterministically to a **database role name**. PostgreSQL roles, SQL Server users and Oracle proxy authentication differ in naming rules, length limits and case behaviour; MySQL and MariaDB add two more. A mapping that works on one may not on another | ADR-015, [security.md](security.md) §2, D-01 |
| A-048 | Expand-and-contract is achievable for every migration we will need, including index and constraint changes on large hosted tables | `UNVALIDATED` — **new 2026-08-13** | [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §5. The discipline is standard, but adding `NOT NULL` or a unique index to a large table can lock — and ADR-007 §5b already says **we must not block a DBA's DDL**. That interaction is unexamined, and it is the case where our own migration discipline collides with our own operational promise | ADR-016, ADR-007 §5b |
| A-049 | A curated Python wheel set can cover realistic geoprocessing without pip at runtime | `UNVALIDATED` — **new 2026-08-13** | [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §7 decided nothing is fetched at runtime, which answers Q-76 in favour of the offline promise. If users routinely need packages we did not anticipate, either the set grows unmanageably or the promise breaks. The escape hatch — a customer-built worker image — shifts the maintenance burden rather than removing it | ADR-016, Q-17b, Q-15 |
| A-050 | Per-worker allocation rate and GC pause can be sampled continuously, cheaply enough to be always-on metrics | `UNVALIDATED` — **new 2026-08-13** | [ADR-017](adr/ADR-017-admin-api.md) §3.2 depends on it, and A-037 makes it the pair of numbers that explain this runtime's actual failure mode — 80.9% GC pause at 18% CPU means a CPU graph shows an idle worker and explains nothing. The benchmark harness read them per request via `GC.GetTotalAllocatedBytes` and collection counts without difficulty, so the mechanism exists; whether it is affordable as an always-on metric rather than a benchmark instrument is untested | ADR-017, ADR-007 §4.17, §46 |
| A-051 | A supervisor-served admin surface can authenticate without the platform store without becoming a bypass | `UNVALIDATED` — **new 2026-08-13** | [ADR-017](adr/ADR-017-admin-api.md) §6's break-glass path, needed because Q-69 put the platform store inside the mandatory datastore, so a datastore outage blinds the admin API exactly when it is needed. **This is the classic shape of an authentication bypass**, bounded by a condition — valid only while the store is unreachable — that somebody must implement correctly. Test that it cannot be used while the store is reachable | ADR-017, ADR-015, [runtime-supervisor.md](runtime-supervisor.md) |
| A-010 | The 100–1,000 service target will not shift upward by an order of magnitude after launch | `UNVALIDATED` | Owner confirmation; revisit at each phase gate | ADR-007, ADR-012 |

**Priority.** A-013 is resolved — a threaded worker model is available. A-003 is the
load-bearing assumption under the shared-worker model. **A-037 is
the first assumption this project found by measuring rather than by reasoning**
— it was not on anyone's list until a tile turned out to allocate 204 MB — and
it was validated the same day it was opened. **A-039 came the same way**, from
finding that a sparse tile reads 97 times more geometry than it emits. A-007 is now `CONTESTED`
and needs splitting into a managed-code path and a native-code path rather than
being answered once.

## Failure impact — the load-bearing five

Added after adversarial review F4. The register's "depended on by" column is a
link, not an impact analysis, and one assumption failing can partially reverse a
flagship decision without that being written anywhere.

| ID | If it is false | What actually changes |
|---|---|---|
| **A-015** warm state is small | Context bind/unbind is expensive | ADR-007 §4.3 lazy binding becomes a cold-start latency problem; §4.4 eviction becomes costly so the budget shrinks; §4.12 pinning becomes the norm rather than the exception — **which recreates per-service resource allocation, the thing §3 said killed ArcSOC.** ADR-010's L1 model changes shape. This single assumption partially reverses ADR-007. |
| **A-019** in-process MVT meets latency targets — *validated on PostGIS 2026-08-12; the two engines this row is about are unmeasured and, per D-05, will stay that way for now* | Tiles from SQL Server and Oracle are too slow | The multi-database promise is hollow for tiles. ADR-008 §4.8's tile path needs a different answer, ADR-010's seeding becomes mandatory rather than an optimisation, and ADR-001's weighting shifts again. |
| **A-014** affinity routing works and is stable | Blind routing | ADR-007 §4.4 is deleted; we become GeoServer with extra steps. Acceptable, but L1 hit rates and therefore ADR-010's value both fall, and the pinning budget becomes the only affinity mechanism. |
| **A-024** refusal plus a capability report is acceptable | Users reject it | ADR-008 §2's central choice fails. Combined with F3's amendment the fallback exists, but the native API would have to adopt best-effort too, which removes the principle entirely. |
| **A-027** concurrency correct against unseen writes | Silent lost updates | The worst defect class an editing API can have. Editing cannot ship. Q-41's companion schema stops being optional. |

## Validated

| ID | Assumption | Validated | Evidence |
|---|---|---|---|
| A-019 | In-process MVT encoding meets latency targets — **on PostGIS only, see D-05** | 2026-08-12 | [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md). **94 ms against 62 ms** for `ST_AsMVT` at a dense z14 tile with 4,863 features — 1.5x, not an order of magnitude. The multi-database promise is not hollow. **Caveat:** at z12 it is 1,471 ms against 428 ms, and simplify is now 55% of that. Low zoom needs more work. |
| A-004 | Hot-path geometry overhead justifies our own primitives | 2026-08-12 | Same run, and it was not close. `NTS.Intersection` was **79% of the entire request**. Our rectangle clipper took that stage from **438.6 ms to 7.0 ms**. Our transform costs 1.4 ms and our MVT encoder 3.8 ms — **what we wrote is fast, what we adopted was slow.** |
| A-001 | The tile path is CPU-bound enough that language performance materially affects capacity | 2026-08-12 | Same run. The doubt recorded against this assumption came from `ST_AsMVT` pushdown, which exists only on PostGIS. On the path that has to work everywhere, 94 ms of a dense tile is ours and 23 ms is the database. It is CPU-bound in our process. This does **not** retroactively validate the language choice — ADR-001 §6 still stands: no language comparison was run. |
| A-013 | GDAL, GEOS and PROJ permit a threaded worker model | 2026-08-12 | [research/dependency-thread-safety.md](research/dependency-thread-safety.md). Deliberately left in the open table too: the headline is validated, but three derived constraints are still live design work — per-thread context lifecycle, GDAL dataset thread-affinity, and two contention points (PROJ grid cache mutex, GDAL block cache under concurrent writes). |

## Invalidated / superseded

| ID | Assumption | Status | What happened |
|---|---|---|---|
| A-009 | PostgreSQL/PostGIS is an acceptable hard dependency for the baseline deployment | **`REINSTATED` and accepted by owner decision, 2026-08-12 (Q-70)** — having been `INVALIDATED` earlier the same day. Both movements are kept on the record | The owner decided PostgreSQL is not mandatory: Oracle Spatial and SQL Server Spatial are first-class. The reasoning is that ArcGIS Server deployments run heavily on those engines, and requiring PostgreSQL purely for our metadata puts a barrier in front of the migration target. **Reopened 2026-08-12 by Q-69.** The owner made the datastore mandatory. The datastore is PostGIS-only (Q-32). Those two facts together make PostgreSQL a hard dependency of every deployment, which is exactly what this assumption says and exactly what invalidating it denied. **The register is doing its job: this is recorded as a live contradiction, not resolved by whichever decision was made last.** **Q-70: the owner accepts it.** The datastore is mandatory (Q-69) and PostGIS-only (Q-32), so PostgreSQL is a hard dependency of every deployment. The reason originally given for invalidating this assumption — that requiring PostgreSQL puts a barrier in front of ArcGIS-on-Oracle migration targets — was correct and is outweighed rather than refuted: the datastore ships as a managed appliance, so the ask is *run our container*. **An organisation with a no-PostgreSQL policy is now a segment we cannot serve, and that is accepted.** Previously superseded by **A-018**, which Q-70 in turn retires. Triggered a reopening of [ADR-002](adr/ADR-002-primary-data-architecture.md) — which its own §9.2 condition had anticipated, and the timing held: no metadata SQL had been written. **The register worked as designed, on its first real test.** |
| A-057 | `ST_AsMVT` output is acceptable to the ArcGIS and MapLibre clients we care about | **`PARTLY VALIDATED` 2026-08-14.** The *geometry* is verified: [tools/render-tile.py](../tools/render-tile.py) decodes a real tile with an independent reader and draws it beside the same extent from PostGIS, and they are the same picture (z16/38030/24562, 1,258 features). **What is still unvalidated is the half the assumption actually names** — no ArcGIS or MapLibre client has consumed one of these tiles. A correct tile and a tile a given client accepts are different claims, and only the first has evidence |
| A-058 | The datastore has spare capacity for tile encoding in a 100–1,000 service deployment | `UNVALIDATED` — [ADR-021](adr/ADR-021-tile-encoding.md) condition 1, and it is the strongest surviving argument against that decision. `ST_AsMVT` puts every byte of tile cost inside PostgreSQL; the benchmark machine's PostgreSQL had headroom, and [ADR-019](adr/ADR-019-portal-server-split.md) makes the datastore mandatory and shared by every service |
| A-059 | A vertex cap bounds the work for single-pass geometry operations | `UNVALIDATED` by measurement — [ADR-022](adr/ADR-022-geometry-server.md) §3. It is a claim about algorithmic shape rather than behaviour, and the shape is visible in the code, but A-042 was also an argument about shape and measurement overturned it |
| A-060 | Using the datastore's PROJ rather than an in-process library costs nothing that matters | `PARTLY VALIDATED` 2026-08-14 — [ADR-022](adr/ADR-022-geometry-server.md) §4. Correctness is trivially established (the answer *is* PostGIS's). The round-trip cost under concurrency is unmeasured, and so is the consequence that `project` cannot answer during a datastore outage while `areasAndLengths` can |
| A-061 | An administrator's first act against an unfamiliar server is to open its URL in a browser | `UNVALIDATED` — [ADR-023](adr/ADR-023-rest-services-directory.md) §7. Asserted from the project owner's own request (*"when I open rest/services as a url, I want to see all services in a list"*) and from ArcGIS having designed for it, which is two data points and one of them is a competitor's judgement. **The validation route was Q-49's *test with real GIS teams*, dissolved when Phase 0 ended** — so this joins A-003 and four others in having no route at all, and that is the honest status rather than a to-do. What partly stands in for it: the directory found a shipped authentication defect within an hour of existing, which evidences that *browsing* is useful without evidencing that administrators do it first |
| A-064 | A security report will arrive at some point after publication, and the cost of having nowhere to send it exceeds the cost of the policy | `UNVALIDATED` | **No route, and not because one was missed** — it is a bet on an event, taken because the downside is asymmetric. The only evidence it can ever get is the first report | [ADR-025](adr/ADR-025-governance-and-maintenance.md) |
| A-065 | Requiring a DCO rather than a CLA does not deter contribution, and requiring a CLA would | `UNVALIDATED` | Observe contributions after publication. Widely believed and consistent with the Linux kernel, Git and Docker; this project has no evidence of its own, having had no contributors | [ADR-025](adr/ADR-025-governance-and-maintenance.md) |
| A-066 | A stale `Public` grant discloses nothing that was not already disclosed | **`VALIDATED` 2026-08-15** by reasoning rather than measurement, and it is close to a tautology: `Public` means anonymous may read it, and a stale window cannot make that more true | — | [ADR-026](adr/ADR-026-serving-through-a-platform-store-outage.md) |
| A-067 | Platform-store failures that leave the data sources up are common enough to be worth serving through | `UNVALIDATED` | Count them in a real deployment. Reasoned from failure shapes — failover, restart, pool exhaustion, a separate instance, a registered source in the customer's own database — and **measured to be worth nothing in the baseline single-instance shape**, where the data goes with the store | [ADR-026](adr/ADR-026-serving-through-a-platform-store-outage.md) |
| A-068 | Fifteen minutes is longer than a transient outage and shorter than one nobody is handling | `UNVALIDATED` | Observe operator response in a real deployment. Configurable (`Graticula:CatalogFallbackMinutes`) because the right value is a property of the deployment, and zero disables degraded serving entirely | [ADR-026](adr/ADR-026-serving-through-a-platform-store-outage.md) |
| A-069 | One Latin/Greek/Cyrillic typeface serves the deployments this product is for | `UNVALIDATED`, and **known to be false for a CJK deployment** | Ask a deployment that labels in a script DejaVu does not cover. Taken because the audience is a Turkish-speaking organisation leaving ArcGIS Enterprise (ADR-018 §1), and stated as a limit rather than discovered as a bug | [ADR-027](adr/ADR-027-glyphs-and-sprites.md) |
| A-070 | Substituting an unavailable font beats refusing the request | `UNVALIDATED` by use. Reasoned from what a client does with a 404 — drops every label and reports a fetch failure, which reads as a broken server | Observe whether anybody is surprised by a substituted typeface | [ADR-027](adr/ADR-027-glyphs-and-sprites.md) |
| A-071 | One style per service is enough to be useful, even though it is not enough to be finished | `UNVALIDATED` | Wait for the first request for a dark theme, which is the expected counterexample rather than a hypothetical one | [ADR-028](adr/ADR-028-style-documents.md) |
| A-072 | Refusing a style at write time costs an author less than a blank map costs them at read time | `UNVALIDATED` by use, and it is the premise of the whole validator | Observe whether authors work around the checks | [ADR-028](adr/ADR-028-style-documents.md) |
| A-073 | Warm-state fragmentation across workers costs less than the affinity control system would cost in instability | `UNVALIDATED` | `benchmarks/worker-model`, which does not exist. **This is the mirror image of A-014** — the same missing measurement read the other way — and it is recorded so the reversal carries its own assumption rather than appearing to be free | [ADR-029](adr/ADR-029-affinity-routing-is-not-the-default.md) |
| A-074 | A QGIS or GDAL client asking a WFS feature type for its default output format gets GML 3.2 — so a WFS offering only GeoJSON would not have connected, and the GML writer is load-bearing rather than ceremonial | **`VALIDATED` 2026-08-19, by measurement rather than by argument.** GDAL 3.11.3 was pointed at `/wfs` through its own WFS driver and sent **394 `GetFeature` requests without an `outputFormat` on a single one of them** — read out of the server's own request log, not out of GDAL's documentation. It took the default, which is GML 3.2, so [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md) §4 Alternative B was rejected on a true premise and the GML writer is the half that makes the surface reachable. **A second thing fell out of the same log and was not being assumed at all:** GDAL spells its parameters `SERVICE`/`VERSION`/`REQUEST`/`TYPENAMES` in upper case, so the case-insensitive binding was load-bearing on the first real client rather than a courtesy. **QGIS is still unmeasured** — it is not installed on the development machine — so this is validated for one of the two clients it names | Point a real QGIS and a real `ogr2ogr` at `/wfs` and record what `outputFormat` each actually requests. Do it **before** the GML writer is finished rather than after: if they negotiate GeoJSON on their own, [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md) §4 Alternative B was rejected on a false premise and half the work is unnecessary. A day of testing, not an argument | [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md) |
| A-075 | ArcGIS Pro's portal connection works against a portal API this project implements, rather than requiring something only Esri's own portal returns | **`VALIDATED` 2026-08-20.** Pro connected, browsed and added a layer. Nothing Esri-only was required; what was required was ten endpoints and one classification token | Measured, not argued — seven distinct failures each read out of the server's own request log, because Pro says *unable to connect* for all of them | [ADR-040](adr/ADR-040-the-portal-surface-is-how-arcgis-pro-connects.md). **Recorded here 2026-08-20, three days late** — it was written into ADR-040 §7 when the decision was taken and never reached this register, which is the drift CLAUDE.md §2 puts assumptions in one place to prevent |
| A-076 | A CPU rasteriser meets the latency a WMS client expects at typical image sizes on this runtime, without the allocation profile ADR-007 §4.14's worker sizing has no term for | **`VALIDATED` 2026-08-20, by measurement.** [benchmarks/map-rendering](../benchmarks/map-rendering/RESULTS.md): GC pause **0.1%–2.3%** of wall clock at 1024² and 4096², at one and eight concurrent requests. ADR-004's objection was 80.9% | Measured against the running server with its own runtime counters, not modelled | **The benchmark also inverted the premise.** Drawing `tr_ilce` as a 1024² map costs 426 ms and 4.7 MB; serving the *same features* as FeatureServer JSON costs 808 ms and 11.8 MB. The map is cheaper than the face this server already had, because rendering never serialises a coordinate to text — so the cost ADR-004 feared was already being paid, by a different surface, every day |
| A-077 | ADR-033's canonical symbology document carries enough to **draw** a map a cartographer would accept, rather than enough to **describe** one | `UNVALIDATED` | [ADR-041](adr/ADR-041-the-map-renderer.md) condition 3 — a drawn map compared by eye against the vector-tile face of the same layer, and the comparison recorded | It was designed to be derived into two protocol documents and never to be executed, so its fidelity has been asserted since 2026-08-17 and tested by nothing. A gap here is a defect in [ADR-033](adr/ADR-033-symbology.md)'s derivation rather than in the renderer, and reopens that decision rather than this one |

---

## Recording rules

1. An assumption enters this register the moment an ADR relies on it — not later.
2. Every row names a concrete validation method. "We will see" is not a method.
3. Status changes are made here first, then propagated to the dependent ADRs.
4. Do not delete invalidated assumptions. Move them down and record what
   replaced them; the history is how we avoid rediscovering the same mistake.
