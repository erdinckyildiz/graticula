# Open Questions Register

Do not hide uncertainty (§61). A question belongs here from the moment it is
noticed, not once it becomes convenient to answer.

**Ownership:** questions marked **[OWNER]** are product or business decisions and
are the only ones escalated to the project owner (§84). Everything else the
council investigates, prototypes, benchmarks and decides on its own.

---

## Carried from Phase 0 — these were blocking, and two of five are now closed

*Renamed 2026-08-14. The heading said **Blocking Phase 0** for a day after Phase
0 ended, which made five questions look retired by a date rather than by an
answer.*

*Renamed again 2026-08-19, twice in one day, because it had become false in the
other direction: it said none were answered. **Q-04 is answered** — measured rather
than argued, and moved into **Answered** below with its numbers — and **Q-23 is
dissolved**, which is a different thing and is marked as one: nothing in this process
holds a PROJ transformation object, so the question has no subject in v1, and nobody
established whether a `PJ` is thread-affine. It is kept in this table, struck
through, because a dissolved question comes back if the decision that dissolved it
reverses. **Three remain open** — Q-01, Q-18 and Q-20 — and the sentence that
follows is about those three. Per [CLAUDE.md](../CLAUDE.md) §1, a carried item
does not become finished by being carried, and Phase 1 does not end with any of
them still open.*

*Found by [tools/status-page.py](../tools/status-page.py), which reads this
file's headings rather than trusting them — the first thing the generated page
reported was five questions blocking a phase that had ended.*

| # | Question | Owner | Resolves in |
|---|---|---|---|
| Q-01 | Which language, on measured evidence? | Council | ADR-001 |
| Q-18 | What genuinely justifies a server over static cloud-native publishing **and over a stateless thin server plus a capable database**? Sharpened by [research/postgis-thin-servers.md](research/postgis-thin-servers.md): pg_tileserv deletes publishing, authorization, geoprocessing and MVT encoding with one constraint. Each subsystem we keep must be individually defensible. | Council | [product-context.md](product-context.md), assessment §8-§10 |
| Q-20 | **How many distinct geometry engines end up evaluating our predicates, and how do we prevent behavioural divergence?** — **escalated 2026-08-13, and the register called this one in advance.** It was written as *PostGIS's, ours, possibly DuckDB's* before DuckDB was in scope. Q-80 and Q-81 put it in scope and add two more. The count is now **six**: PostGIS's GEOS, DuckDB's GEOS, MySQL 8's **Boost.Geometry**, MariaDB's, SQL Server's own implementation, Oracle's SDO_GEOM, plus NetTopologySuite's JTS port in our own process. **These disagree at the edges** — on validity, on precision, on what touches, on empty-geometry results — and the same query can return different answers on different providers. **This is a correctness problem, not a performance one, and ADR-008 §2's never-degrade-silently does not cover it**: refusing an unsupported operation is honest, quietly returning a different answer is not. Needs `experiments/geometry-oracle` and a documented conformance position per provider. **MEASURED 2026-08-19, and the count for v1 is two rather than six — [experiments/geometry-oracle/RESULTS.md](../experiments/geometry-oracle/RESULTS.md).** Four of the six belong to providers v1 does not include, so they defer with the decisions that would bring them in. What is left is sharper than a smaller version of the same question, because **both remaining engines are reachable today**: GEOS inside PostGIS evaluates every spatial filter on a query (`PostGisFeatureSource.Predicate`), and JTS — NetTopologySuite, in the overlay worker — evaluates `GeometryServer/relation` (`Satisfies`). **Six predicates are answerable by both**, and until this run nothing had compared them beyond `intersects` on a corpus of real polygons. **Result: 392 of 448 comparisons agree, and the 56 that do not are one case.** Extended the same day, twice, because the first run left the two hardest comparisons out: **DE-9IM** — which both engines answer, through `relationParam` on one side and `st_relate` on the other, and which the first version wrongly recorded as the query path's alone — and **coordinate magnitude**, since everything was written within 0–25 while web-Mercator metres run to 2×10⁷. Eight patterns (256 comparisons, 224 agree) and every case again at an offset of 2×10⁶ (224, 196 agree); every disagreement in every slice is still the empty case, so filtering those out gives **392 of 392**. The nanometre cases keep their distinction at 2×10⁶ rather than collapsing, which is what makes that slice mean anything. Fifteen hand-written cases around the four edges this question names — validity, precision, what touches, empty geometries — because a corpus contains almost no bowties, no pairs a nanometre apart and no polygon inside another's hole. Both engines agree on **every case expressible through the product**, including the ones chosen to be hardest: the **invalid bowtie throws on neither side and both answer identically** (`overlaps` true, and against itself `within` is false — a valid geometry is within itself and an invalid one is not, and both say so); **precision is exact in both directions at 1e-9**, neither snaps; a polygon in a hole is `disjoint` and not `within` on both; a nanometre-tall sliver sharing a base is `within` on both. **The one divergence is a refusal rather than a wrong answer** — an empty polygon, which PostGIS answers and our surface refuses with *'rings' must be a non-empty array* — **and it is not reachable through the product**: `ArcGisGeometryReader` is shared, so a query filter of `{"rings":[]}` is refused by the same sentence, measured. So there is no path where an empty geometry reaches one engine and not the other. **The fifteen cases are now a test** — `WorkerAgainstPostgisTests.Both_engines_agree_on_the_case_engines_disagree_about`, 15 cases × 14 comparisons × 2 magnitudes = 420 in six seconds, falsified by making `Touch` mean `Crosses` in the worker, which failed three cases — so an NTS or GEOS upgrade that moves an answer fails the build, which is the only way this agreement can now be lost. **What stays open, and it is why this is not closed:** magnitudes beyond 2×10⁶ — the far edge of web Mercator is ten times further, where the step between doubles is larger than the nanometre cases, so a different set would be needed rather than the same one moved; `st_dwithin`, which has one engine and therefore no oracle, and is the one predicate where a projected unit and a degree could be confused; mixed-dimension pairs, which are not covered at all; and the four deferred engines, about which this says nothing. The original count is right for the product those decisions would create and wrong for the product v1 is. | Council | ADR-003, ADR-008, Q-80, Q-81, `experiments/geometry-oracle` |
| Q-23 | ~~Can a PROJ `PJ` transformation object be used from more than one thread, or is it thread-affine?~~ **DISSOLVED for v1, 2026-08-19: nothing in this process holds one.** Decides whether prepared transformations are shared or duplicated per thread — tile and render hot path, and it changes what L1 costs. **`PostGisProjector` is the only `IProjector` in the solution, and every transformation in the product is `ST_Transform` inside PostgreSQL** — the GeometryServer's `project`, a query's `outSR`, and the tile path's bounds (`PostGisTileSource`), all in SQL. There is no PROJ binding anywhere in the solution: `proj_create` and `libproj` appear nowhere, and the one component that carries PROJ at all — GDAL, in the import reader — resolves EPSG codes and is explicitly forbidden to reproject (ADR-038 §5: *the header says which system they are in and the importer decides what to store*). **So each transformation has PostgreSQL's own per-connection PROJ context and the question has no subject.** `PostGisProjector`'s own class note recorded this on the day it was written; what was missing was the register saying so, which is the staleness this repository keeps finding in itself. **Exercised as well as argued, 2026-08-19:** 960 concurrent projections over 24 threads — six points including the poles and the antimeridian, 4326 to 3857 — produced **one distinct answer**, identical to the serial one digit for digit. Exact equality rather than a tolerance, because a thread-affine transformation misused across threads does not throw, it returns slightly wrong numbers. **What would bring it back, precisely:** a decision to project in process. That is ADR-021's territory — it measured the tile path and the datastore won there too — so Q-23 returns if and only if ADR-021 or ADR-010 is reopened to put a projection library in this process, and then it returns with its original teeth. Recorded as dissolved rather than answered: **nobody measured whether a `PJ` is thread-affine.** The question was made irrelevant by an architectural decision, which is a different thing from being resolved, and if the decision reverses the answer is still unknown. | Council | ADR-010, ADR-021, `PostGisProjector`, [research/dependency-thread-safety.md](research/dependency-thread-safety.md) §4 |
| Q-24 | ~~GDAL 3.10 for read-only raster thread safety?~~ **Closed.** [ADR-009](adr/ADR-009-raster-engine.md) puts GDAL on job workers at registration time only, so raster thread safety is no longer a request-path concern at all. File-based *vector* providers still hit GDAL per request and RFC 101 does not help them, which is now tracked under ADR-008 rather than here. | — | closed 2026-08-12 |
| Q-24-old | ~~Do we require GDAL 3.10 or later for read-only raster thread safety?~~ **Largely moot after the vector-first decision** — GDAL leaves the request path for imagery. Retained only because file-based *vector* providers still hit GDAL per request, and RFC 101 is raster-only, so it does not help them. | Council | ADR-009, deployment |

## Open, not blocking

| # | Question | Owner | Notes |
|---|---|---|---|
| Q-101 | **Is the capability report a generated, versioned vocabulary, or free-form per-provider prose?** [ADR-008](adr/ADR-008-query-engine.md) §2 made a per-layer capability report the mechanism for *never degrade silently*, and ADR-008 already records the complaint that the report **"is doing far more work than it was designed for"**. A closed vocabulary bounds it: a named, versioned key set, so that what a provider may claim about itself is diffable rather than describable. **The lesson is one this project has already learned once** — [tools/conditions.py](../tools/conditions.py) exists because a number nobody can regenerate is a number nobody maintains, and a capability set nobody can regenerate is the same shape. Two sub-questions, and the second is the one with teeth: whether we adopt the vocabulary shape at all, and whether it is **generated from the code that implements the keys** or hand-maintained beside it — because a hand-maintained vocabulary drifts from the provider it describes and then lies in exactly the situation it exists to prevent. Observed in Honua as a published artefact; the mechanism, not the keys | Council | ADR-008, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-102 | **Is there a deployment-level capability manifest, distinct from the per-layer capability report?** Ours is per layer. The question a client asks *first* is about the server — which protocols are compiled in, which formats it will emit, which operations exist at all — and we have nowhere to answer it. [ADR-023](adr/ADR-023-rest-services-directory.md)'s services directory answers *what is published*, which is a different question at a different level, and conflating the two leaves a client probing endpoints to discover the shape of the server. Distinct from Q-101: that one is about the vocabulary, this one about whether there is a second level to speak it at | Council | ADR-005, ADR-017, ADR-023, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-103 | **Is bounded database admission fixed-cap by default, with adaptive admission an explicitly chosen profile rather than the default?** [benchmarks/feature-query/RESULTS.md](../benchmarks/feature-query/RESULTS.md) §3b measured the curve this question is about: 942 req/s at concurrency 8, then **falling throughput with linearly growing latency** — the signature of a queue, at 3.6 of 16 cores. That is the shape bounded admission exists to prevent. **The honest limit is in the same section:** §3b.3 could not locate the queue, because the do-nothing control saturates in the same place, so it is not yet established that the knob belongs on *database* concurrency at all. What transfers from Honua is the **shape of the default** — a predictable fixed cap, with adaptive behaviour opt-in because what a cap does is inspectable and what an adaptive controller does under load is not. Their published sizing figures are a hypothesis for our benchmark, not evidence for us (§3). Overlaps Q-04 and ADR-007 condition 3, and should be answered by the same run | Council | ADR-007 §4.8, Q-04, `benchmarks/connection-budget/`, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-104 | **Is the catalogue exportable as a manifest that can be diffed, reviewed and applied?** The connection/layer/service graph is configuration, and at our own scale target of 100–1,000 services (CLAUDE.md §7) nobody reviews it by clicking through it. **Recorded as a question rather than a recommendation, deliberately:** §82 asks what concrete problem a technology solves, and *GitOps* is exactly the kind of word that arrives without an answer to that. The candidate problems are real ones — reviewing a change before it reaches production, reproducing a deployment, and knowing what changed after an incident — but each has a cheaper answer available (an audit log, a backup, a diff of the platform store), and the question is whether any of them is cheaper *in practice*. Observed in Honua; not adopted | Council | ADR-017, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-105 | **Is "authorization composition only ever restricts" stated once as an invariant, or re-derived per surface?** Honua composes a service policy with a layer policy under **deny-wins** — a permissive service never overrides a restrictive layer. **The rule does not transfer**: [ADR-018](adr/ADR-018-authorization-and-roles.md) computes read from owner and sharing scope and has no service-level policy to compose with. The invariant behind it does — *no composition step may widen what a narrower element allowed* — and this project has already met one instance of its violation without naming the class: ADR-018 §3b-i records a service with no layer behind it escaping governance entirely, and the fix was to give it the same three scopes rather than to state the rule that would have caught it. The question is whether the invariant becomes a stated property with a test, or stays an outcome that each new surface has to arrive at independently | Council | ADR-018 §3b-i, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-114 | **Should the generated default consider what a layer *is*, not only its geometry?** Opened 2026-08-17 with [ADR-033](adr/ADR-033-symbology.md) §5b. A deterministic colour per layer fixes *everything is the same blue*, and it still draws a road network and a parcel fabric the same way — both are areas or lines to the server, and the difference is what the data means. Candidates, none chosen: the layer's name, which is a guess dressed as inference; the field names, which are a better guess; the geometry's own statistics, since a dense network of short lines is not a river; or nothing at all, on the grounds that a server that guesses meaning will guess wrong visibly. **The cost of getting it wrong is low** — a colour somebody overrides — which is exactly why it should not be answered by whoever is in a hurry. | Council | [ADR-033](adr/ADR-033-symbology.md) |
| Q-115 | **Should the in-process geometry operations have a time bound, and what would enforce it?** Opened 2026-08-17 from the owner's question about the reference's pooling page: ArcGIS's *maximum time a client can use a service* governs every request, and ours governs overlay only. `project`, `areasAndLengths`, `lengths`, `labelPoints`, `convexHull`, `densify` and `generalize` are bounded by the 500,000-vertex cap and by nothing in seconds. [ADR-022](adr/ADR-022-geometry-server.md) §3's argument for the cap is sound — each is one pass over the coordinates, so input size bounds work exactly — which is why this has not bitten. **What makes it a question rather than a task:** nothing in `GeometryOperations` takes a `CancellationToken`, so a timeout added today would abandon the response while the CPU kept burning, and a timeout that stops nothing is worse than none because it looks like protection. Three routes, none chosen: thread cancellation through those algorithms, which is a change to every one of them for a bound that may never fire; move them behind the worker process, which already has a deadline and a heap ceiling but costs a process hop on operations currently measured in single milliseconds; or state that the cap is the bound for this half and stop implying otherwise. **The third is nearly free and may be right** — but it is a claim about a whole class of operations, so it should not be made by whoever noticed. | Council | [ADR-022](adr/ADR-022-geometry-server.md) |
| Q-116 | ~~**Can a member be removed, and what happens to what they published?**~~ **ANSWERED by the owner 2026-08-18 — [ADR-015](adr/ADR-015-authentication.md) §6c.** Opened 2026-08-18 by the console suite needing a reader without `admin:manageServer` and having nowhere to put one. `/admin/members` creates, lists, sets a role, sets a password, disables and enables; there is no delete, and the omission looks deliberate rather than forgotten — `service.owner` names a member, so a removed member is a service with an owner who is not there, and every sharing decision recorded *by* them becomes unattributed. **Disable already covers the case that actually arises:** somebody leaves, their account stops working, their content keeps serving and keeps its author. So this is not a gap by default. **What makes it a question is a smaller need than deletion:** a test, or a script, that creates a throwaway account and wants to not leave it behind. Three routes, none chosen — a real delete that refuses while the member owns anything, which is the ArcGIS shape and the most conservative; a delete that reassigns ownership to whoever runs it, which is convenient and quietly rewrites history; or nothing, and the suite keeps synthesising the reader it needs, which is what it does today and what leaves [ADR-034](adr/ADR-034-server-and-studio.md) condition 1 one sentence short of dischargeable. | Council | **ANSWERED, and none of the three routes this row listed was the answer.** The owner's shape is a *preflight and a choice*: a member who owns nothing goes outright; a member who owns something is refused unless the request said what to do with it, and the refusal carries the counts. `transfer` moves ownership and nothing stops serving; `delete` takes the owned things with them. **Neither of this row's two candidate designs survives** — a delete that refuses while the member owns anything (the row called it the most conservative) makes the operator dismantle by hand, and a delete that silently reassigns to whoever ran it rewrites history, which the row already objected to. Asking is the third thing. **Groups are named in the decision and not in the schema**, on the owner's *"şu anda grubumuz yok ama olacak"*, so the disposition is written around *owned things* rather than around services — which makes groups an addition when [ADR-018](adr/ADR-018-authorization-and-roles.md)'s deferred scope arrives, not a redesign. **Recorded in:** [ADR-018](adr/ADR-018-authorization-and-roles.md), [ADR-034](adr/ADR-034-server-and-studio.md) condition 1, [D-59](architecture-debt.md) |
| Q-121 | **ANSWERED 2026-08-21 by owner decision: yes, registered in place and never copied** — [ADR-043](adr/ADR-043-imageserver-and-the-raster-face.md) §3.3. Registration reads the header and stores a reference; the file stays where it is. **The bytes still travel through this server**, because ADR-009 §2.4 already settled that a client fetching a COG straight from object storage cannot be authorised per layer, and per-layer authorisation is the requirement this product exists to serve. So a range-request proxy is what *in place* costs. **Original follows.** Can a COG be registered in place — served from where it already lives, never copied?** Opened 2026-08-18 from the owner's question *"raster olsa gdal zorunlu mu?"*, which turned out to have a better answer than the register held. [ADR-009](adr/ADR-009-raster-engine.md) §2.1 decided *serve COG, convert everything else at registration*, and §2.2 keeps GDAL out of the serving artefact — so **serving a COG needs no GDAL at all**, and a deployment whose imagery is already COG never loads it. That much is now written into §2.1, because the three bullets implied it and a quick read of *"GDAL reads it once"* leaves the opposite impression. **What is genuinely open is the storage half.** §2.1 says conversion needs somewhere to write, and that a read-only registered source holding a non-COG can be catalogued but not served — it is silent on a read-only source holding a **COG**, which is the case where nothing needs writing. Three shapes: **copy it into the datastore anyway** (uniform, and pointless for a file that is already in the served format — it doubles the storage of an imagery estate for nothing); **catalogue it and serve range reads through us** (one contract, our authorization, our cache, and we become the read path for somebody else's bytes — the per-layer authorization concern §2.1's own paragraph at line 143 already raises); or **catalogue it and hand the client the object-storage URL** (cheapest, and it takes the sharing model out of the loop entirely, which for a private layer is not a trade-off but a hole). **Recommendation: the second**, because it is the only one of the three that keeps the sharing decision ours, and ADR-018's whole model is that no route to content bypasses one. **Derived from the reference's public description** — ADR-030 condition 1 — which lists cloud-optimized GeoTIFFs *"registered in place from S3/Azure"* as a capability and *"No GDAL required on the server"* as a property; COG's own behaviour is cited from [its specification](https://cogeo.org/) rather than from them, per condition 3. **Not urgent:** v1-scope §3b cut rendering and raster, so this arrives when ADR-009 re-closes. Recorded now because the *answer to the GDAL question* depends on it, and that question is live. | Council, and the object-storage position **[OWNER]** | [ADR-009](adr/ADR-009-raster-engine.md) §2.1, §2.2, [ADR-037](adr/ADR-037-job-workers-come-in-two-kinds.md), [A-016](architecture-assumptions.md), Q-28, [v1-scope](v1-scope.md) §3b, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-120 | ~~**What language is a job worker written in, and is one kind enough?**~~ **ANSWERED 2026-08-18 by [ADR-037](adr/ADR-037-job-workers-come-in-two-kinds.md), hours after it was opened, and the recommendation below was taken as written: two kinds, chosen by what bounds the work rather than by what it is about.** A **.NET** worker for work our own runtime must bound and kill — `DOTNET_GCHeapHardLimit` is a ceiling the runtime enforces, and ADR-022 §9 exists because [A-042](architecture-assumptions.md) was invalidated by a 6,408-vertex input costing 153 seconds and 16.7 GB and killing the host's Docker daemon. A **Python** worker for work whose value is in somebody else's ecosystem: foreign formats now, foreign tools later. **Two is a ceiling, not a floor** — a third kind needs its own ADR. The boundary is a **GeoParquet file and a process**, which executes [Q-74](#)'s choice rather than making a new one, and is a stronger seam than an interface because no library type can cross it in either direction. **Amended 2026-08-19 by [ADR-038](adr/ADR-038-how-a-geodatabase-becomes-a-service.md): for the import path the file half is gone and the process half stands.** GeoParquet was Q-74's choice *because Python's stack reads it natively*, and §5a reversed the worker to .NET the day this was written — so both ends are .NET and the reason no longer reaches this boundary. Features cross as newline-delimited JSON on the pipe the two processes already have — each geometry as base64 WKB, which the same day's first real archive forced: GeoJSON was the first choice and RFC 7946 pins its coordinates to WGS 84, so every feature of an EPSG:2952 layer was refused by a guard that was right (ADR-038 §4B). It needs no Parquet reader in the serving process and streams where `ImportedDataset` does not. Q-74's own boundary, for the Python SDK, is untouched and its reason is intact. **The dissent is recorded in ADR-037 §11 and it is the author's own, against the option chosen:** a second language runtime in every deployment is the kind of cost §82 exists to refuse, and *File Geodatabase import* is a smaller problem than that is a cost. The reply — that the runtime is bought for the geoprocessing surface Q-17b already settled, and import is the first job that pays for a process arriving anyway — does not fully dissolve it, so ADR-037's revisit triggers include *geoprocessing is cut for good*. Original: Opened 2026-08-18, when the owner said the File Geodatabase reader need not be .NET — *"ileride koyacağımız geoprocessing araçları pythonda yazılacağı için, python kütüphanesi kullanılabilirdi"* — and that observation turned an absence into a question. **Nothing decides this today.** `Graticula.Overlay.Worker` is .NET and is a separate process for a reason [ADR-022](adr/ADR-022-geometry-server.md) §9 measured rather than argued: an overlay that cannot be bounded from inside must be killable from outside. Neither [ADR-011](adr/ADR-011-job-system.md) nor [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) says what language a *job* worker is written in — ADR-016 goes as far as a curated Python wheel set for user tools without saying whose process runs them. **Two things now want the same process:** File Geodatabase import, where `pyogrio` reads a `.gdb` in one line against a multi-month binary-format project in .NET ([file-geodatabase-readers.md](research/file-geodatabase-readers.md) §8), and the geoprocessing runtime, which Q-17b already settled as Python. **And half the boundary is already chosen:** [Q-74](#) picked *materialise to a file the tool reads* and concluded the format wants to be Arrow or GeoParquet because Python's stack reads them without a shim — `pyogrio` is Arrow-native, so the same boundary serves both directions. **Recommendation: two worker kinds, by decision rather than by residue.** A .NET worker for what our own runtime must bound and kill, and a Python worker for what reads foreign formats and runs foreign tools; they are different jobs with different reasons, and pretending one process should do both is how a worker acquires a dependency graph nobody chose. **What must not be confused with this:** v1-scope §3c cut the Python runtime and Q-75/Q-76 gate the Python SDK, but **both are about *user-supplied* code** — arbitrary execution and user dependency installation. Our own import script against our own pinned wheels is a packaging cost, not a security surface, so an import worker does not pull Q-75 forward. **Cost stated:** a second runtime to patch and a wheel set arriving earlier than ADR-016 planned. | Council, and the deployment shape **[OWNER]** | [ADR-011](adr/ADR-011-job-system.md), [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §7, [ADR-022](adr/ADR-022-geometry-server.md) §9, Q-17b, Q-74, Q-75, Q-76, [Q-108](#), [A-049](architecture-assumptions.md) |
| Q-119 | **Where does an anonymous caller discover a public group, and until there is such a place, do `public` and `organization` differ?** Opened 2026-08-18, hours after Q-118 was answered, by checking whether the setting Q-118 produced actually does anything. **It did not, and that was a defect rather than a gap:** `visibility` was stored, reported by two endpoints and read by no `where` clause, so a group set to *everybody, including anonymous callers* was discoverable by exactly the people who could already see it — while the console said otherwise and the endpoint's own note said *"it can now be found by anybody"*. **That is [D-67](architecture-debt.md) precisely, and it shipped in the same change that refuses `join_policy = 'request'` on the ground that a policy stored and unenforced is D-67 over again.** One of the two had to move. **Enforced the same hour**, because enforcing was cheaper than refusing: one disjunct in `ListAsync`, a caller who arrives that way comes back `Outside`, and the describe endpoint withholds the member and item lists on that standing rather than filtering them — ADR-036 §4g's *"being able to see that a group exists is not being able to read what is in it"* kept where it can be checked. Measured: an outsider does not list a `members` group and gets 404; lists an `organization` or `public` one and gets 200 with `inside: false` and both lists withheld; a member gets `inside: true` and both lists. **What stays open is the difference between the two open values.** `/admin/groups` refuses an anonymous caller outright — *"Groups are for members. Sign in."* — so `public` and `organization` behave identically today, and the console's *everybody, including anonymous callers* is still a claim the server does not keep. Three ways to close it: a public discovery surface outside `/admin` that lists public groups to anybody, which is a new endpoint and a new anonymous read path; letting the anonymous caller through to this listing filtered to `public` only, which is smaller and puts an unauthenticated reader on an admin route; or **refusing `public` on write until one of the two exists**, which is what `request` gets and would make the console's three options two. **Recommendation: the third for now**, because it is the consistent one — the argument that refused `request` applies unchanged, and the current state is the one this project has twice recorded as the worst available: a setting reported and half-enforced. The first is the right answer eventually and is a decision about anonymous surfaces rather than about groups. | Council, and the anonymous surface **[OWNER]** | [ADR-036](adr/ADR-036-groups.md) §4g and §4h, [D-67](architecture-debt.md), [D-79](architecture-debt.md), Q-118 |
| Q-118 | ~~**Does a group have a visibility of its own, and who may see its membership and contribute to it?**~~ **ANSWERED by the owner 2026-08-18, hours after it was opened, by sending the reference's Settings tab — [ADR-036](adr/ADR-036-groups.md) §4g.** All three axes exist and are editable, and there is a fourth this row had not recorded: **how people join** — by invitation, by request, or by adding themselves. *By request* implies a queue of pending requests, so the value is stored and refused on write until the queue exists, because a policy the server accepts and does not honour is D-67 over again. Every default is what this server already did, so nothing changes behaviour: an operator gains the ability to say otherwise. **The members-list axis dissolved rather than being decided** — the reference has no such setting; its Members tab is visible to members and its *Who can view this group?* covers the discovery half, which is what that half of the question was really asking. Original: Opened 2026-08-18 from the two Portal screenshots the owner sent with *"looks good"*, and from three settings in them that [ADR-036](adr/ADR-036-groups.md) deliberately did not copy. Theirs carries **Viewable by** on the group itself — *Group members*, *Organization*, *Everyone (public)* — which answers *who may discover that this group exists*, a different question from *who may read its items*; a **Members list** setting, visible to all members or to the owner only; and **Contributors**, all members or the owner only, which is who may *add* items and is distinct from §4b's `item_update`, which governs editing what is already shared. **Ours answers all three by construction rather than by decision:** a group is discoverable only by the people in it, members always see each other, and only owners and managers contribute. **That is the closed end of each axis except the second**, where ours is the open one and nobody chose it — which is the half of this question that is a defect rather than a gap. **What makes it a question:** each is a fifth, sixth and seventh thing to be visible on an object whose whole purpose is visibility, and ADR-036 §3 spent a section arguing that an authorization axis must not arrive quietly. The group's own visibility is also load-bearing for their two other tabs — *Featured groups* and *My organization's groups* are both discovery over groups you are not in, and neither means anything without it. **Recommendation: the members-list default first**, because it is the one where ours is more open than theirs and was not chosen; then the group's visibility, because two screens depend on it; contributors last, since owners-and-managers is defensible and matches who may share. | Council | [ADR-036](adr/ADR-036-groups.md) §4e, [ADR-018](adr/ADR-018-authorization-and-roles.md) §3b |
| Q-117 | **What does CI actually prove about a repository somebody else clones?** Opened 2026-08-18 by [D-62](architecture-debt.md), where two files of production source had never been committed — `.gitignore`'s `secrets/` matched a source directory — and nothing detected it. Every local check passed because the files were on disk; CI would have failed, and did not run, because no push had happened since. **The narrow fix is done** (the pattern is anchored). **The question is what class of thing this belongs to:** the build page currently asserts that nine suites pass, and says nothing about whether what was tested is what a stranger would receive. Three candidate answers, none chosen — a job that clones into an empty directory and builds, which is nearly free and catches exactly this; a check that every file the solution compiles is tracked, which is narrower and catches it earlier; or nothing, on the argument that a push-triggered build would have caught it eventually and the anchor already fixed the cause. **The third is defensible and is why this is a question:** the counter-argument is that *eventually* did not happen for six days, and the recovery depended on a build output sitting outside the repository by luck. **The second is done, 2026-08-18, and the first is measured once rather than automated.** `tools/registers-check.py` now asks git which of the repository's own source files an ignore rule hides — every `.cs`, `.csproj`, `.js`, `.css` and `.html` under `src/` and `tests/`, in one `git check-ignore -z --stdin` call — and fails if any of them would not reach a clone. Verified by putting the unanchored `secrets/` rule back: it named both files and the reason. **And a fresh clone was built:** `git clone` into an empty directory, `dotnet build Graticula.sln -c Release`, succeeded — so D-62's repair is confirmed by measurement rather than by reading. **What stays open is the first option**, a CI job that does that clone-and-build on every push, and it is worth less now than it was this morning: the narrow check catches the cause at the moment somebody writes the rule, which is earlier than a push. It is also worth noting that with [D-63](architecture-debt.md) open, a new CI job would prove nothing at all. | Council | **OPEN, and narrower.** The recurrence is prevented and the clone is proven; what remains is whether CI should assert it, which waits on CI running at all. **Recorded in:** [D-62](architecture-debt.md), [D-63](architecture-debt.md), [ADR-025](adr/ADR-025-governance-and-maintenance.md) |
| Q-112 | ~~**Do we add groups, and if so with ArcGIS's update semantics?**~~ **ANSWERED 2026-08-18 by [ADR-036](adr/ADR-036-groups.md), and built the same day.** All three of this row's open decisions were taken: the group scope exists and confers **reading only** in v1; the update capability is a property of the **group** rather than of each share, because *"the planning team can edit these"* is a sentence and the per-share alternative is not; and it is **fixed at creation**, because widening it would make every item already shared with the group editable by every member, retroactively. The second axis ADR-035 condition 4 demanded — a manager who is not the owner — is in the authorization model from the first line rather than retrofitted. Measured: a member of the group reads the service (200), a member outside it does not (404), anonymous does not (404), and the member cannot edit it (403). Opened 2026-08-17 by the owner, who wants a private layer to be shareable with a group and readable by its members. [ADR-018](adr/ADR-018-authorization-and-roles.md) §3b deferred groups deliberately — *"adding them here would be adopting a subsystem to complete a table"* — and left the scope column a string so `group` is a value rather than a migration. **The owner's understanding of ArcGIS is confirmed, and two constraints they did not name are the interesting part.** Esri's documentation: an item stays *"semiprivate"* when shared only with a group you belong to, so group members read it while it is otherwise private; editing requires a group **created** with the item update capability, offered as *only their own items* or *all items shared with the group*; **that capability cannot be changed on an existing group** — *"you cannot change the item update capability setting. You can only specify the update option on new groups"* — and it requires invite-or-approve membership; and even inside such a group, **update is not ownership**: only the owner or an administrator may delete, share, move, change owner, publish, or overwrite a hosted layer's data. **The immutability reads as a limitation and is defensible as a safety property:** flipping a group from view to update-all would silently make every already-shared item editable by every member, which is a widening nobody asked for at the moment they asked for something else. Three decisions, and they are separable: whether group scope exists at all; whether update capability is a property of the *group* rather than of each share; and whether it is immutable. The middle one is the one that carries ArcGIS's whole design — a per-share grant would be a different product. **Ordering answered 2026-08-18: privileges first.** The owner asked — *"aslında gruplardan önce yetki mekanizmasını mı tanımlamak mantıklı acaba"* — and [ADR-035](adr/ADR-035-role-privileges-are-editable.md) §4d says yes for three reasons, the first of which is decisive: every group operation is a privilege and there is nowhere to put one, so building groups first means hard-coding who may create a group and then rewriting it. **What must be decided with the privilege work rather than after it** is whether a group's *manager* — a member who is not its owner but holds its operations inside it — is part of the authorization model from the start; a second axis retrofitted into a check written for one axis is the change most likely to go wrong quietly. That is ADR-035 condition 4. **Two things added 2026-08-18, when the owner asked what notes exist.** First, the surface is already settled and was settled twice: the owner's *"studio tarafında bir de gruplar olacak. portal tarafının grupları gibi"*, and [ADR-034](adr/ADR-034-server-and-studio.md) §6 already records *"Q-112 (groups), when answered, lands in Studio."* So *where* is not part of this question. Second, **the one piece of preparation this project believed it had does not exist.** [ADR-018](adr/ADR-018-authorization-and-roles.md) §3b said the sharing column takes a string so that `group` arrives *"as a value rather than a migration"*, and two code comments repeated it. Measured against the live schema: `layer`, `service` and `system_service` each carry `check (sharing in ('private','organization','public'))`. Widening a check is expand-only and cheap, so this changes the estimate rather than the answer — but it was the sentence anybody planning this would have started from, and all three places are corrected. **What is genuinely unwritten is the creation flow:** there is no `group` table, no `content:createGroup` privilege, no membership model and no screen. [ADR-015](adr/ADR-015-authentication.md) §6c has already made room for the consequence — a member's removal is written around *owned things* rather than services, so a group becomes a third owned thing rather than a redesign — which is the only part of groups that exists today | Council | [ADR-018](adr/ADR-018-authorization-and-roles.md) §3b and §3b-ii, Q-59 |
| Q-113 | **Answered and built 2026-08-17: yes, in bytes, reported as `exceededTransferLimit`.** Original: **Does a request get a cost ceiling, separately from a statement?** Opened from the peer's per-service settings screen, which bounds six kinds of request cost where we had one. **Bytes, because bytes are what the ceiling is for** — a row is not a unit of cost, and one polygon can outweigh ten thousand points. Measured before deciding: one request for 5,000 districts with geometry and every field returns **4.2 MB**, and `FeatureQuery.MaximumLimit` permits 50,000 rows, so a single request inside every limit this server had could return around forty megabytes. **The ceiling is expressed in the protocol's own vocabulary rather than a new one:** an ArcGIS client already pages on `exceededTransferLimit`, so a body truncated by size sets the same flag a body truncated by row count sets and no client changes. An error would have been a new contract; a silent truncation would be worse than either. Enforced in `FeatureServerQueryWriter`, which is the only place that knows how many bytes it has committed — anywhere else would have to buffer the response to measure it, which is the allocation A-037 named as the binding constraint. Default 64 MiB, `Graticula:MaximumResponseBytes`, and **zero disables it**, which is every earlier build's behaviour. Two invariants hold by construction and by test: the body is always valid JSON with its header intact, and **at least one feature is always returned**, because an empty page carrying `exceededTransferLimit` is a paging loop that never advances. **All of it built the same day, after the owner pointed out that the rest matters too — and they were right, "later" was the polite form of leaving it half-done.** Migration 17 gives a service five more columns and each is enforced where it can be: **max record count** and **default record count** in the query parser, replacing a literal `1000` and a compile-time `MaximumLimit` that no operator could reach; **max response bytes** as a per-service narrowing of the server figure; **max request bytes** checked from `Content-Length` *before the body is read*, since a ceiling applied after parsing has already paid the cost it exists to avoid; and **max edits per transaction** after the batch parses and before it is written. **Every one narrows and none widens** — `RecordCount` takes the smaller of the service's and the server's, which is ADR-031's rule on a second axis. **The edit ceiling refuses rather than truncates, and it is the only one that must:** shortening a response is a smaller answer to the same question, while shortening a transaction is a *different edit* applied silently, and `rollbackOnFailure` exists so a caller can require all or nothing. *Max features per layer* is deliberately **not** a sixth column: on a service queried one layer at a time it is the same ceiling stated twice, and two knobs over one fact is what ADR-031 §2b refused for sharing. Measured against the live catalogue: no parameter gives the service default of 10, a request for 5,000 is clamped to the service maximum of 50, 20 stays 20, four edits against a ceiling of three is refused with the reason, three is accepted, and a 2,087-byte body against a 700-byte ceiling answers 413 | Council | [ADR-007](adr/ADR-007-service-runtime.md) §4.8, [ADR-031](adr/ADR-031-service-capability-configuration.md), O5 |
| Q-111 | **Can a local administrator still sign in when the configured external identity source is unreachable or misconfigured — and should changing that source require a local credential?** Opened 2026-08-16 by [ADR-015](adr/ADR-015-authentication.md) §5a, which decided one identity store for the fused deployable and, in writing down why ArcGIS has two, found the failure the second one guards. Esri's guidance for disabling the primary site administrator warns that if the identity store becomes corrupted or unavailable you will not be able to sign in to the site **at all**, and refuses to let the identity store be changed while that account is disabled — so their break-glass credential is the local one that bypasses the store, and the operation it protects is the one that can lock everybody out. **Nothing is broken here today**, and that is the point of recording it early: §9a and D-10 say no external source is built, so every account is local and there is no lockout to have. It arrives with the first IdP, by the route that looks like tidying up — treating the IdP as *the* identity store and local accounts as legacy. Two halves: whether a local administrator is guaranteed a path in, and whether mutating the identity configuration is itself gated on a local credential, which is the stronger and less obvious half | Council | [ADR-015](adr/ADR-015-authentication.md) §5a and condition 5, [D-10](architecture-debt.md), Q-15 |
| Q-99 | Do ArcGIS's geometry *editing* operations — `autoComplete`, `reshape`, `trimExtend` — belong on GeometryServer or on FeatureServer? All three take a geometry and change an existing feature's shape against it, which is a write. GeometryServer as built is a pure calculator: it holds no state, needs no layer and asks for no privilege. Putting an editing operation on it means either a second authorization surface on a service that currently has none, or an operation that returns a shape and leaves the caller to write it — in which case the name is misleading. These are the last three refusals on the service, and after 2026-08-15 they are the only ones **not** refused on cost. | Council | [ADR-022](adr/ADR-022-geometry-server.md) §2b, [ADR-005](adr/ADR-005-api-architecture.md) §3.8 |
| Q-100 | How does `findTransformations` reach PROJ's operation database? It lists the datum transformation paths between two references, ranked by accuracy — and the paths live in `proj.db`, which this server does not have. [ADR-022](adr/ADR-022-geometry-server.md) §4 sends projection to the datastore precisely to avoid shipping PROJ, but that decision was about the **datum grids**, which are hundreds of megabytes and a distribution commitment. `proj.db` is about 9 MB of metadata and no grids at all, so §4's reasoning does not obviously carry. Three routes: ship `proj.db` and a reader in process; find a way to read the datastore's copy, which PostGIS exposes no SQL function for; or leave the operation refused. **The operation is currently refused with this reason rather than answered with the one path PROJ happened to choose**, which would be a ranked list of one and not an answer to the question asked. | Council | [ADR-022](adr/ADR-022-geometry-server.md) §4, [geometry-crs-policy.md](geometry-crs-policy.md) §3 |
| Q-09 | Should the platform be polyglot (§80.2)? | Council | Distinct from ADR-001. Most likely relevant to geoprocessing extensions. Must not be used to dodge the core language decision. |
| Q-11 | Where does the service catalog's identity model come from — who mints stable IDs, and how do renames work (§37)? | Council | Cheap to get right now, expensive later. |
| Q-12 | How is monitoring cardinality bounded at 1,000 services? | Council | **Elevated by [ADR-007](adr/ADR-007-service-runtime.md) §5.** With worker, process and connection counts flat in service count, cardinality and catalog scale become the *only* things that grow. At 10,000 services this is the binding constraint, not the runtime. |
| Q-14 | Which CRS set must be supported at launch, and is on-the-fly reprojection a hot path or a convenience? | Council | Affects caching keys and tile pipeline design. |
| Q-48 | How are the platform store and the datastore backed up and restored **consistently**? They are two databases, possibly two engines, and restoring a Tuesday platform store against a Wednesday datastore produces services pointing at tables that do not match their definitions. Needs either a documented ordering with a quiescing step, or a version stamp shared across both so a mismatched restore is detected rather than silently wrong. Raised by F10. | Council | deployment, ADR-002, data-model |
| Q-59 | **Re-answered 2026-08-14 by owner direction: adopt the role / user-type capability matrix of ArcGIS Portal.** That replaced, rather than confirmed, the four invented roles of the day before — which is exactly what ADR-018 condition 1 existed to allow. **The structural consequence is the interesting part: Portal has no read privilege.** Roles grant *doing*; *seeing* comes from each item's sharing scope. So the ownership-and-sharing axis ([security.md](security.md) §2.0), deferred one day earlier as not due until the publish endpoint shipped, became due immediately — it is not an enhancement to reading, it **is** reading. Five Portal roles (`viewer`, `data_editor`, `user`, `publisher`, `administrator`) over fourteen privileges, a user-type ceiling enforced so that Q-16 migration cannot silently widen an imported deployment, and three sharing scopes with groups deferred. **Superseded first answer, 2026-08-13, recorded because the reasoning was sound and was replaced rather than refuted:** [CLAUDE.md](../CLAUDE.md) §2 requires that distinction, so ADR-018 condition 1 is owner confirmation and the ADR is a proposal that happens to be running. **Four roles, taken from this question's own wording rather than invented**: `viewer`, `publisher`, `gis-administrator`, `platform-administrator`, nesting, over nine permissions. ~~**Fixed, not custom**~~ — **overruled 2026-08-18 by [ADR-035](adr/ADR-035-role-privileges-are-editable.md): role privileges are editable, the administrator's excepted.** The argument below is left standing rather than deleted, because it was sound and was overruled rather than refuted, and because the cost it predicted now arrives and somebody will need to know it was foreseen: a custom role is an expression over a permission catalogue, and the moment customers write roles against it the catalogue becomes a public contract that cannot be renamed or split without breaking their grants; nine permissions derived from three endpoints is not a vocabulary worth freezing. The `role` table already carries arbitrary names, so custom roles are a feature rather than a migration. **The one non-obvious split**: `datasource.register` is separate from `layer.publish.hosted`, because publishing puts a file in our datastore while registering hands the server a credential to somebody else's database. **What it does not answer**: the ownership and sharing axis ([security.md](security.md) §2.0), which is not due until ADR-017's publish endpoint ships. Original question: What is the role set, and what capability does each carry? At minimum: viewer, publisher (may create hosted content), GIS administrator (may register sources and publish from them), platform administrator. Are roles fixed or custom? ArcGIS separates licensed *user types* from *roles*; we have no licensing tiers, so we probably need only roles - but that should be decided rather than assumed. | Council | §42, security |
| Q-60 | Are **groups** needed in v1? Sharing scopes are private, group, organisation, public. Groups are the expensive one - membership, nesting, who may create them - and without them sharing collapses to private, organisation or public, which may be enough to start. | Council | §42, security |
| Q-61 | How are **per-user datastore quotas** enforced, and what happens at the limit? Without quotas one publisher fills the store. Interacts with the L3 cache budget (ADR-010) - they compete for the same disk - and with the failure mode when a publish is refused mid-upload. | Council | ADR-010, data-model, §49 |
| Q-62 | How do an owner-set sharing scope and an admin-set role compose when they disagree? The safe default is that the more restrictive wins, but safe and expected are not the same, and an owner who shares publicly and finds it invisible will report a bug. | Council | security §2.0 |
| Q-63 | Where does the routing decision live, given the supervisor is deliberately not on the data path? Front router process, `SO_REUSEPORT` with a shared warm map, or accept-and-bind-locally which abandons affinity. Constrained by the supervisor design: whatever routes must observe supervisor state, so a dead worker is detected before routing rather than after. Should be settled by `experiments/affinity-routing`. | Council | [runtime-supervisor.md](runtime-supervisor.md) §9, ADR-007 §4.4 |
| Q-64 | How is a memory leak distinguished from a legitimately growing cache, with no baseline? A high level is not a fault - a worker with many warm contexts is doing its job, and recycling it destroys the value it accumulated. Growth without corresponding load is the signal, and that needs history a fresh deployment lacks. Starting crude (hard ceiling plus rate check) and saying so. | Council | [runtime-supervisor.md](runtime-supervisor.md) §6, ADR-007 §4.7 |
| Q-65 | What is the worker adoption protocol after a supervisor restart, and how is it made idempotent? Workers must be discoverable, re-attachable without spawning duplicates, and any mid-flight drain or quiesce must have recorded state so the new supervisor can finish or abandon it deliberately. Most likely part to be got subtly wrong. | Council | [runtime-supervisor.md](runtime-supervisor.md) §8 |
| Q-52 | Which file formats are **serving providers** and which are **import sources**? Proposed line: serving formats designed for range-request access (COG, and later GeoParquet and FlatGeobuf) are providers; interchange formats (Shapefile, KML, GPX, File Geodatabase, GeoJSON files) are imported into the datastore as a job. §27 currently lists interchange formats as providers, which is wrong. The GDAL rule in Q-28 gives a clean test: anything needing GDAL cannot be a serving provider. | Council | §27, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-53 | Do we ever support data warehouses (Snowflake, Databricks, Redshift)? **Recommend no**, because every query costs money and the architecture has no cost dimension — caching, seeding and job retry all assume compute is roughly free, and §49 counts features, bytes and time but not currency. The prerequisite is a cost model, not a connector. | Council | §49, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-30 | How is cross-node change notification done without `LISTEN`/`NOTIFY`? Polling a change-sequence column is the portable default. [ADR-010](adr/ADR-010-caching.md) §7 makes this concrete and uncomfortable: the poll interval bounds the invalidation delay, and for the "wrong" class of invalidation that window is a period during which incorrect data is served. **The number must be documented, not described as "eventual".** | Council | ADR-002 §4a.4, ADR-010 §7, §45 |
| Q-46 | Can a fast job return its result directly instead of a job identifier and a polling URL? Registration is a job for good reasons, but returning a polling URL for a two-second operation makes a worse first five minutes for every new user. [ADR-011](adr/ADR-011-job-system.md) §10 flags this as worth designing rather than accepting. | Council | ADR-011, admin API (§39) |
| Q-45 | What is the plan-identity stability contract? Cache keys derive from compiled plan identity, so a planner change silently invalidates every cached tile in a deployment. That has to be a deliberate, visible, released event rather than a surprise on upgrade. | Council | ADR-010 §8, ADR-008 |
| Q-41 | Do we offer an optional companion schema in a registered database where granted rights, to hold versioning and editor-tracking bookkeeping? **Important again now that editing is in scope.** It is how ArcGIS gives referenced data advanced capability, and it is the difference between concurrency that survives direct database writes and concurrency that does not. | Council | [research/arcgis-datastore-model.md](research/arcgis-datastore-model.md) §4, [ADR-005](adr/ADR-005-api-architecture.md) §3.8 |
| Q-19 | **Deferred, not rejected.** Does the platform need an in-process compute engine (DuckDB) for capability gaps? [ADR-008](adr/ADR-008-query-engine.md) §4.3 declines to adopt one before we can name the queries it would rescue. **The evidence that reopens it is a list of real refused queries users legitimately need.** Recorded as dissent in ADR-008 §12. | Council | ADR-008 |
| Q-44 | What is the write surface? OGC API Features Part 4 is still a **draft**. Options: implement the draft and track a moving spec, ship our own additive extension and converge later, or both. [ADR-005](adr/ADR-005-api-architecture.md) §3.7 recommends the second. **Amended 2026-08-12 by Q-17: there are now two write surfaces**, ours and ArcGIS `applyEdits`. Both must map onto one internal write model — a genuine test of whether ADR-005's protocol-neutral interface is neutral, and also double the surface to keep correct. | Council | ADR-005 |
| Q-43 | What is the schema-drift polling interval, and what does it cost against a large registered database with many layers? If too expensive, fall back to check-on-error plus TTL. | Council | [data-model.md](data-model.md) §3, A-023 |
| Q-39 | If a registered source happens to be writable, do we offer hosted-grade capability there automatically or only on explicit opt-in? Automatic is friendlier and surprises DBAs. Opt-in is safer and gets forgotten. | Council | [data-model.md](data-model.md) §5 |
| Q-33 | If a datastore exists, does hosting copy data or replicate it continuously? Copy is simple and goes stale; continuous replication is a synchronisation product nobody asked for. Copy with explicit refresh is the likely answer, but it must be chosen rather than defaulted into. | Council | publishing (§38) |
| Q-34 | Are pre-generalised geometry tables per zoom level a datastore-only feature, or attempted on registered sources where we happen to have write access? | Council | ADR-008, ADR-010 |
| Q-35 | Which runtime schema changes do we offer, per dialect? Needs DDL cost and locking behaviour verified on all three engines first. Classification proposed in [research/runtime-schema-evolution.md](research/runtime-schema-evolution.md) §5. | Council | publishing (§38), ADR-008 |
| Q-36 | Can the service definition describe fields the physical table does not have — computed, aliased, role-hidden? Cheap to allow now, expensive to retrofit. **First concrete requirement found 2026-08-12:** a registered read-only source with a wrong declared SRID cannot be corrected at source, so the definition must be able to override it ([geometry-crs-policy.md](geometry-crs-policy.md) §2). That makes this a yes rather than a maybe. | Council | data model (§37, §38) |
| Q-37 | What is the contract for a request in flight across a schema change? | Council | ADR-007, publishing (§38) |
| Q-66 | **Re-scoped 2026-08-14 to the feature path: there is no in-process tile path any more** ([ADR-021](adr/ADR-021-tile-encoding.md)). Re-scoped rather than closed, because finding 11 — a request's cost floor is set by the largest geometry overlapping it — is engine-independent and applies to feature queries exactly as it did to tiles. Original: **Does the provider interface hand back geometry objects on the tile path, or coordinates?** — **promoted 2026-08-12 from curiosity to the main line of work.** Run 3 measured ~139 bytes allocated per vertex read, roughly three to four copies of every coordinate (WKB bytes, `Coordinate` objects, clipped copy, transformed copy), and showed the resulting GC pause is what caps throughput. Opened by [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) finding 10: after two rounds of optimisation the geometry work we own is 21.5 ms of a 323 ms z12 request, and the dominant remaining cost is WKB parsing building an NTS geometry graph that exists only to be discarded one stage later. Reading WKB straight into tile space would remove it — at the cost of a second, coordinate-shaped provider return type and a tile path that no longer shares a representation with the feature path. That is A-006 under pressure. | Council | ADR-003 §5b, ADR-008, provider architecture (§27) |
| Q-58c | **Domains, subtypes and editor tracking** — the rest of the FeatureServer data model. Split out of Q-58 when [ADR-013](adr/ADR-013-feature-service-data-model.md) answered identity, relationships and attachments. Editor tracking interacts with A-027, since it records who changed what and we do not see writes that bypass us. | Council | [ADR-013](adr/ADR-013-feature-service-data-model.md), §28 |
| Q-89 | **Does v1 build ADR-005's protocol-neutral internal interface, or the ArcGIS surface directly?** Opened by Q-88's inversion. That interface was justified by carrying many faces — ADR-005 §3.2a called six faces *"the hardest available test"* of whether it is genuinely neutral. **v1 has one face.** An abstraction exercised by a single implementation is not an abstraction, which is ADR-005's own phrasing about a different subject. Either build the ArcGIS surface directly and extract the interface when OGC arrives in v2, accepting a refactor; or build it now on faith, which answers §82's *what concrete problem does this solve?* with *a problem we will have later*. **Recommendation: the first.** ADR-005's to answer when it re-closes. | Council | **ANSWERED 2026-08-20 by measurement, and the recommendation was right.** Five faces now read one query model — ArcGIS FeatureServer, VectorTileServer and MapServer, WFS 2.0, WMS 1.3.0/1.1.1 and OGC API Features — which is one short of the six §3.2a called the hardest available test. **Zero SQL in any of the four adapter projects**, eight `FeatureQuery` construction sites and all of them in the host, and `TierBoundaryTests` still passes: no protocol type reaches Tier 1. **The interface was never built on faith and never needed to be.** It was extracted twice, each time when a second caller asked: `AttributePredicate`/`PredicateSql` when WFS became the second consumer of the where-clause emitter, and `GeoJsonWriter` when OGC API Features became the second writer of GeoJSON. Each extraction was a day, not a refactor. **What building one face first did cost is four names**: `LayerDefinition.ObjectIdColumn`, `LayerDefinition.IsArcGisServable`, `FeatureQuery.ObjectIds` and `FeatureEdits.ObjectId` are one protocol's vocabulary sitting in the domain — [D-124](architecture-debt.md). §8's rule held for the architecture and leaked in the naming, which is a smaller bill than a speculative abstraction would have been and is the evidence this question was waiting for. **Recorded in:** ADR-005, [v1-scope.md](v1-scope.md) §4 |
| Q-86 | **§82 answer, per newly-scoped capability** — **sweep S3, severe.** [CLAUDE.md](../CLAUDE.md) §6 is binding: *for every proposed technology, what concrete problem does this solve?* Q-78 and Q-83 moved roughly thirty capabilities into scope across two messages and answered that question **per list, not per capability**. For SensorThings, EDR, 3D Tiles, OData, gRPC, MCP, PMTiles and WPS no answer is recorded anywhere. Not an argument to reverse anything — parity is a legitimate answer where a real client speaks the protocol — but a list-level answer is exactly how the weakest item inherits the strongest justification, which is Q-17a's failure at fifteen times the scale. Owed: one line each, in [protocol-surface.md](protocol-surface.md). Some will read *parity; no known client in the target market*, which is weaker, honest, and useful when the schedule needs cutting. | Council | [reviews/contradiction-sweep-1.md](reviews/contradiction-sweep-1.md) S3 |
| Q-87 | **Where does DuckDB execute — job worker or request path?** — **sweep S4.** [ADR-009](adr/ADR-009-raster-engine.md) states flatly that *the serving container ships no GDAL*, A-016 adopts it as a rule, and ADR-001 §C7 leans on it. **DuckDB's spatial extension bundles GDAL**, so serving file-backed layers from the request path puts GDAL back in the serving container through a side door — invalidating A-016, weakening C7, and growing the air-gapped checklist a GDAL bill of materials it does not have. **Recommendation: job worker only.** Files are converted at registration, which is already ADR-009's model for raster and Q-52's for import formats; serving a file directly is a convenience we have not justified. | Council | [reviews/contradiction-sweep-1.md](reviews/contradiction-sweep-1.md) S4, A-016, Q-15 |
| Q-92 | **Resolved by [ADR-018](adr/ADR-018-authorization-and-roles.md), 2026-08-13.** Q-59 is answered, authorization is implemented, and ADR-017 is unblocked. Original: **Q-59 has stopped being merely open and is now blocking.** Authentication is implemented and resolves a principal on every request; nothing consults it, because there are no roles to consult. That is not a gap that can be closed by writing more authentication. **It also blocks the admin API (ADR-017)**, which is the largest remaining friction in the product — D-09 has publishing a layer going through hand-written SQL and a connection string sealed outside the server — because publish and register endpoints without authorization would be strictly worse than the SQL. **The narrowest useful answer** is probably three roles: administrator, publisher, and viewer, with ADR-015 §7's separate grant for publishing *code* rather than data. That is a guess and is recorded as one. | Council | Q-59, [architecture-debt.md](architecture-debt.md) D-09 and D-11, [ADR-017](adr/ADR-017-admin-api.md), ADR-015 §7 |
| Q-91 | **How does a streaming response report a failure that happens after bytes are on the wire?** Opened by D-07. The query endpoint streams so that a 50,000-feature result is never materialised (A-037), and that is the same property that makes a mid-result failure unreportable: the status line is long gone. **Three shapes exist.** *Abort the connection* — what we do now; a client sees an incomplete transfer, which HTTP chunked encoding does make detectable, but no reason. *A terminal error object* — close the `features` array and emit `"error"` as a final property, which stays valid JSON and which no ArcGIS client will look for. *HTTP trailers* — correct, and unsupported by most clients over HTTP/1.1. **The question is not which is nicest but which one a real ArcGIS client notices**, and that is answerable by experiment rather than by argument: point the JS API and Pro at a server that fails mid-stream and observe. Until then the honest position is that we truncate. | Council | [architecture-debt.md](architecture-debt.md) D-07, ADR-005, ADR-017 §6 |
| Q-74 | **What is the Python SDK, and how does data cross the boundary?** Gates Q-17b. Three shapes: give Python a database connection (bypasses our authz, capability model and connection budget — rejected on sight); have the SDK call our own API (respects authz, works remotely, slow for bulk); or materialise the input to a file the tool reads and collect its output the same way (fast, no connection held, no authz bypass because we did the reading). **The third points somewhere specific:** the interchange format wants to be one Python's geospatial stack reads natively, which means **GeoParquet or Arrow** — `geopandas`, `pyarrow` and `shapely` all read them without a shim. **This finally gives GeoParquet a concrete problem to solve.** It was an early owner interest with no justification in the architecture, and Q-53 declined warehouse providers, leaving it homeless. It is not a provider — it is the boundary between our runtime and theirs. **Narrowed 2026-08-19: that problem is still the only one it has.** For a few hours the geodatabase import looked like a second — [ADR-038](adr/ADR-038-how-a-geodatabase-becomes-a-service.md) inherited GeoParquet as the reader-to-host boundary from ADR-037 — and it turned out not to be one: both ends are .NET, so the features cross as newline-delimited JSON with base64 WKB on the pipe the two processes already have, and no Parquet reader is needed in the serving process. The reader still *can* write GeoParquet (`convert`) and nothing calls it. So GeoParquet's justification rests entirely on the Python SDK, which is v2 — and if Q-17b ever lands elsewhere, GeoParquet is homeless again rather than merely unused. | Council | Q-17b, ADR-011, §82 |
| Q-77 | **ANSWERED 2026-08-21 by [ADR-043](adr/ADR-043-imageserver-and-the-raster-face.md) §3.4: yes it gets built, and the line is stated before either half is — which is what this question asked for. **Tier 2, adopted:** decoding a TIFF, walking its tiles and overviews, decompressing, resampling, warping. **Tier 1, ours:** which overview answers a request, how values become colours — stretch, ramp, no-data — and how the raster composites under the vector layers. That is the same line ADR-041 drew for vectors: the adopted library fills the triangles, we decide what to fill. **And the balance this question assumed has inverted**, which is why it is answerable now: ADR-009 §0 called `exportImage` larger than the vector renderer, and since then the canvas, the pixel transform, the scale model, format encoding and the export endpoint have all shipped. The delivery half exists. **Original follows.** Does the raster rendering engine get built, and where is the Tier 1 line?** Gates the expensive half of Q-17c. `exportImage`, raster functions and dynamic mosaicking all need server-side raster rendering — a separate problem from ADR-004's vector rendering, not the same engine. GDAL already does much of the mechanical work (warp, VRT-as-mosaic, overviews) and lives in the job worker under A-016, which makes it Tier 2 raster I/O. But applying a stretch or a colour ramp to pixels is **cartographic logic, which [build-vs-adopt-policy.md](build-vs-adopt-policy.md) puts in Tier 1**. The line probably falls between *assembling and warping pixels* (adopted) and *deciding what colour they are* (ours), and that needs stating before either is built. | Council | [ADR-009](adr/ADR-009-raster-engine.md), [ADR-004](adr/ADR-004-rendering-engine.md), build-vs-adopt |
| Q-15 | What does "air-gapped" concretely require — offline PROJ grids, GDAL driver data, font bundles, no telemetry? | Council | Currently a slogan in the master prompt; needs a concrete checklist. |
| Q-122 | **ANSWERED 2026-08-21 by taking it: run the CITE engine locally, which is what this question recommended.** `ogccite/ets-wfs20` in a container against the running server, four times in one afternoon. **405 of 420 pass**, up from 297 of 391 on 2026-08-19, and the evidence is [cite-wfs20-2026-08-21.rdf](reviews/cite-wfs20-2026-08-21.rdf) rather than a sentence. **The answer's shape matters more than the number.** This question said *a suite that names which clauses fail is worth more than a QGIS that happens to work: one client working is a data point, a suite is the population*. That held exactly: five causes accounted for 91 of the first 104 failures, four were small and are fixed, and the fifth was a **500** where a caller's date filter was reported as a missing PostGIS. None of the five could have been found by a client that happened not to send those requests. **What is not answered is the standing part**: this was run by hand on one machine, and [D-63](architecture-debt.md) means nothing runs it again. A number that has to be re-earned by remembering is the same class of thing this question objected to. See [ADR-039 §6b](adr/ADR-039-wfs-is-the-first-surface-after-v1.md). **And a second suite run 2026-08-23, this time OGC API Features and against every collection rather than a sample:** `ogccite/ets-ogcapi-features10`, **1,268 passed, 6 failed, 78 untested**, evidence in [cite-ogcapi10-2026-08-23.rdf](reviews/cite-ogcapi10-2026-08-23.rdf). The earlier runs reported 306 of 332 because `noofcollections` was left at a sample; `-1` quadruples the assertions and is what a number should be earned against. **The one assertion that had been red since 2026-08-20 is green** ([Q-133](#)). **The six failures are the suite's own bound and this is the third time that distinction has mattered.** All six are one assertion — *numberMatched (5433) does not match the number of features in all responses (50)* — on the three collections above fifty features that the suite did not skip. The request log settles it: for `tr_il` the suite asked for five pages, stopped, and compared its fifty against 5,433; for `tr_yer` another test walked all 143 pages and got all 1,421. **Nothing was changed in the server on the strength of those six**, and a conformance test now walks every page of every collection so the question can be answered without the container. **And the WFS suite is green 2026-08-23: 420 of 420, 0 failed.** [cite-wfs20-2026-08-23.rdf](reviews/cite-wfs20-2026-08-23.rdf), against 405 of 420 two days before. Nine distinct defects, listed in [ADR-039 §6c](adr/ADR-039-wfs-is-the-first-surface-after-v1.md), and **the one worth naming here is the one this repository had argued itself into keeping**: §6b defended broken date literals on the grounds that both front ends failed identically, and the third front end had been doing it right all along ([Q-124](#)). A conformance suite is worth having because it does not read your reasoning. **And the WMS suite re-run the same day, with the recommended profile the earlier runs left out: 191 of 202, then 194.** [cite-wms13-2026-08-23.rdf](reviews/cite-wms13-2026-08-23.rdf). `recommended=recommended` is one query-string parameter and it surfaced five things the capabilities document had never carried, none of which anybody had noticed — **which is this row's own argument arriving a third time**: a suite is worth more than a client that happens to work, and a suite run with half its profile is a suite half read. Three were free because the server already knew the answer, contact information is configuration and absent by design, and `MetadataURL` stays failed because its mandatory `type` names a metadata standard this server has no document for and declaring one it does not follow would satisfy the suite and lie to a harvester. Details in [ADR-041](adr/ADR-041-the-map-renderer.md) condition 6. **The six dataset failures are now proven rather than inferred** — the run's own request records show the suite sending `LAYERS=` empty because it could not find its own eight layer names. **The standing problem is unchanged and is now the only one left on this row.** Two green suites, both earned by hand in a container, neither re-runnable by anything in this repository. `WfsCiteRepairTests` and `OgcExtentConformanceTests` assert every repair through the suite that does run, which is the best available answer and is not the same as the suite running. **What this adds to the standing half of this row:** the answer is still re-earned by hand, and the recipe is now written down — the container needs the development certificate in its own JVM truststore and `--add-host` for the name the certificate carries, because `host.docker.internal` is not one of its subject alternative names. Two facts that cost twenty minutes to rediscover. **Original follows.** **Is there an external conformance route for WFS, and do we take it?** Opened 2026-08-19 with [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md). Our entire conformance story is **164 tests we wrote against our own reading of a specification** — which proves this server is consistent with our reading, and says nothing about whether a client connects. [peer-capability-comparison.md](research/peer-capability-comparison.md) §2.1 already named that the gap that bears on the thesis, and **WFS is the first surface in this product where an official suite exists at all**: the peer publishes 1117 of 1117 across thirteen OGC CITE suites, WFS 1.0/1.1/2.0 among them, run by dedicated CI workflows (`VERIFY` the suites against OGC's own team engine rather than against their README, which is the only source read so far). **Three shapes.** *Run the CITE engine locally* against a running server — the honest one, and it costs an image and a harness; *run nothing and say so* in the capabilities document, which is at least not a false claim; or *write our own suite and call it conformance*, which is what we already do and is the thing §2.1 objects to. **Recommendation: the first, and before ADR-039's condition 1 rather than after it.** A suite that names which clauses fail is worth more than a QGIS that happens to work: one client working is a data point, a suite is the population. **Nothing gates this** — it needs no decision, only time — which is precisely why it is the kind of item that never gets done unless it is written down. | Council | [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md), Q-94, [peer-capability-comparison.md](research/peer-capability-comparison.md) §2.1, [D-63](architecture-debt.md) |
| Q-123 | **Which coordinate references are latitude-first, and where does that answer come from?** Opened 2026-08-19 with [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md). Under `urn:ogc:def:crs:EPSG::4326` the axis order is EPSG's own — latitude, then longitude — and a server that writes longitude first produces coordinates a conforming client silently transposes. **There is no error anywhere; the features are in the sea off Somalia**, which is the same symptom as an unknown extent and is why it is so often misdiagnosed. `WfsNames.IsLatitudeFirst` knows exactly one code — 4326, the default this surface advertises and the one nearly every client asks for — and writes everything else easting-first. **That is right for every projected system and wrong for any other geographic one**: 4258 (ETRS89), 4269 (NAD83) and Turkey's own geographic codes are latitude-first in EPSG and would be written the wrong way round. **The real answer is in the database.** `spatial_ref_sys.srtext` says whether a definition is geographic, which is the property that decides this, and reading it means a round trip per reference — cheap if remembered, since a deployment uses a handful of references and they never change. **Recommendation: a small cache in front of `spatial_ref_sys`, filled on first use**, rather than a table of codes in our source, because a table of codes is a copy of someone else's register that goes stale silently. Not urgent while v1's layers are 4326 and 3857, and it becomes urgent the first time a customer publishes a national geographic grid. | Council | **NARROWED 2026-08-20 by [correctness gate 2](reviews/correctness-gate-2.md), which found the gap was worse than this row said.** `IsLatitudeFirst` answered true for 4326 alone, so **EPSG:4258 (ETRS89) — the standard geographic system across most of Europe — was written longitude-first on WFS and WMS**, a valid 200 with every coordinate transposed and no error anywhere. The rule is now the EPSG geographic 2D block, 4000–4999, which covers 4326, 4258, 4269 and 4267. **What is still open is smaller and stated exactly**: geographic systems numbered outside the block, and the handful of *projected* systems the authority defines northing-first (EPSG:2180 and several Nordic grids), which are still written easting-first. **And the obvious fix is unavailable**: this deployment's `spatial_ref_sys.srtext` carries no `AXIS` clauses at all, so the database cannot be asked — checked, not assumed. **Recorded in:** [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md) §5, `WfsNames`, Q-96 |
| Q-124 | **ANSWERED 2026-08-23, and the answer was in a surface this row did not count.** This row says *either surface* and lists three options for teaching one of them; there were three front ends, and **the OGC API Features face had been converting dates correctly since it was written** — `DateTimeOffset.TryParse` with `AssumeUniversal \| AdjustToUniversal`, bound as a `DateTimeOffset`, which Npgsql maps to `timestamptz` and PostgreSQL compares with a `timestamp` column as well through its own implicit cast. That is this row's second option, *bind a type PostgreSQL will cast from*, already in production and already measured. **So the recommendation was wrong and the reasoning behind it was not.** Widening `FieldType` for one provider's benefit is still the wrong move, and teaching the provider is still where the knowledge would ideally live — it is simply not needed, because a `DateTimeOffset` carries enough. **Both other front ends convert now.** `FilterReader` reads a `fes:Literal` against a `FieldType.Date` column as a moment; `WhereClause.TryParse` takes an optional map of column types, because it knew names and not types and that was the whole of what was missing from the ArcGIS side. Optional, so a caller that does not know the types gets the behaviour it had. Measured on `hosted/wms_time_demo`: a WFS `PropertyIsEqualTo` against `observed_at` returns the feature, and `where=observed_at = '2026-08-02'` counts it. **UTC when the literal does not say, on all three surfaces**, because a server whose answers move when its time zone is reconfigured is worse than one that is arguably an hour off. **Found by CITE rather than by anybody reading this row**, which had stood five days with *most of the filtering anybody wants* in it: [ADR-039 §6c](adr/ADR-039-wfs-is-the-first-surface-after-v1.md). **Original follows.** **How does a date literal reach a comparison, on either surface?** Opened 2026-08-19. `WhereClause`'s grammar has no date literal, so an ArcGIS `where` comparing a timestamp column binds text and the database refuses it; `FilterReader` reads a `fes:Literal` against a `FieldType.Date` column and does the same, **deliberately, so that the two front ends give the same answer to the same question**. The result is that neither surface can filter by date, which for an incident layer or an alert feed is most of the filtering anybody wants. **The difficulty is not parsing.** It is that Npgsql maps `DateTime` by its `Kind`: unspecified goes to `timestamp` and UTC to `timestamptz`, and `LayerDescription` records neither — `FieldType.Date` is one value covering both, and §2's *deliberately coarse* note says why. So the choice is to widen the field type, to bind a type PostgreSQL will cast from, or to have the provider convert at the point where it knows the column. **Recommendation: the third**, because it is the only one that keeps the knowledge where the knowledge is; the first widens a domain type for one provider's benefit, which is what [ADR-005](adr/ADR-005-api-architecture.md) §3's protocol-neutral interface is supposed to prevent. | Council | [ADR-008](adr/ADR-008-query-engine.md), [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md), `WhereClause`, `FilterReader` |
| Q-125 | **Where does a layer's extent in WGS 84 come from, for a document that must stay cheap?** Opened 2026-08-19. `ows:WGS84BoundingBox` is how a WFS client decides where to put the map before it has read a single feature, and it is longitude/latitude in WGS 84 **by definition** — so a layer in a national grid needs its extent projected before it can be written. The capabilities document therefore omits the box for every layer that is not already 4326, and a client loses the initial zoom for those layers rather than being given the wrong one, which is the right way round but is still a loss. **Two costs meet here.** Projecting is one round trip per layer; and the extent itself is only known after a `DescribeAsync`, which is another — so at the stated scale of 100–1,000 services a complete capabilities document is one to two thousand round trips for a page a client fetches once and caches. Today the describe is done only for 4326 layers, which is the subset that can produce a box at all. **Recommendation: remember the projected extent beside the layer rather than computing it per request** — it changes when the data changes, and the import and edit paths already know when that is. That makes this a catalogue question rather than a WFS one, which is why it is recorded here and not as a debt against the writer. | Council | [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md) §5, `CapabilitiesDocument`, `ServiceContexts`, D-17 |
| Q-126 | **Does *add the server* have to work in ArcGIS Pro, or is *add the service* enough?** Opened 2026-08-19, from the first real ArcGIS Pro session against this server. Pro's **New ArcGIS Server** connection does not use the REST surface at all: it probes `GET /admin/generateToken` and `POST /services` with a SOAP body, both of which this server answers 404, and it never reaches `/rest/info`. So the connection cannot be created. **Adding one service by URL works completely** — `Add Data → Data From Path` with `/rest/services/hosted/tr_il/FeatureServer/0` drove the whole surface: layer document, service document, then paged queries with `outFields=*`, an `outSR` given as a full spatial-reference object rather than a code, a polygon `geometry` filter with `spatialRel=esriSpatialRelIntersects`, `resultOffset`/`resultRecordCount` and `orderByFields`. Every one answered 200. **This is a product question, not a defect.** [Q-16](#)'s user is somebody whose ArcGIS licence lapses and whose existing clients must keep working; *browse the server and pick a layer* is a different workflow from *paste a service URL*, and only the second one is available. Making the first work means the ArcGIS Server **admin API** and a **SOAP** service catalogue, which [ADR-005](adr/ADR-005-api-architecture.md) never scoped and which [CLAUDE.md](../CLAUDE.md) §5's clean room makes expensive to reverse-engineer. **Recommendation: do not build them, and say so where a migrating user will read it.** The gap is one sentence of documentation and a paste-able URL; the alternative is two protocol surfaces for a browse affordance. **What would change this:** an owner or a customer saying the browse workflow is the thing they are buying, which is exactly the kind of claim this register exists to make somebody state out loud. **ANSWERED IN PART by the owner, 2026-08-20:** *bunun yapılmasını isteyeceğim senden. GET /admin/generateToken çok önemli.* So the browse workflow is wanted, and the first of the two missing pieces is built — `/admin/generateToken` answers both the credential-less probe and a real sign-in, in the administrative API's own error shape, over the same `LoginService` as every other door. **The second piece is untouched and is the larger one:** `POST /services` with a SOAP body. **ANSWERED 2026-08-20, and the answer is that the question had a third option.** Pro's *server* connection still does not work and the SOAP catalogue is still not built — but its **portal** connection does, and that is the workflow the owner asked for: Pro signs in, lists every item, and adds a layer to a map. [ADR-040](adr/ADR-040-the-portal-surface-is-how-arcgis-pro-connects.md) took that road for reasons that were argued and then measured. **So this row closes on the requirement rather than on the mechanism**, and the SOAP surface stays unbuilt with its cost recorded instead of paid. It returns only if somebody needs the *server* connection specifically, which nothing so far does. | **[OWNER]** | [Q-16](#), [Q-49](#), [ADR-005](adr/ADR-005-api-architecture.md), [peer-capability-comparison.md](research/peer-capability-comparison.md) §2.1 |
| Q-127 | **What does a portal group mean here, and whose item is a published service?** Opened 2026-08-20 with [ADR-040](adr/ADR-040-the-portal-surface-is-how-arcgis-pro-connects.md), from two fields the surface answers with a shape rather than a truth. **Groups:** `/sharing/rest/community/groups` returns an empty list. [ADR-036](adr/ADR-036-groups.md)'s groups are real and a service can be shared with one — so *this portal has no groups* is false, and a 404 would have said *this is not a portal*, which is worse. What is missing is not code but a decision: a portal group carries its own membership, its own owner and its own item sharing, and whether ours map onto that or merely resemble it has never been asked. **Ownership:** an item's `owner` is the caller's name when the caller owns the service and the product's name otherwise, because this surface has no member directory and inventing one to fill a field would publish the user list through a door nobody reviewed. The consequence is visible: Pro's *My Content* shows every item the caller may see rather than the ones they own, and the two are the same set only on a single-operator deployment. **Neither is urgent and both are the kind of thing that becomes permanent by being useful.** The trigger is a second operator: the moment two people publish on one server, *My Content* is wrong for both of them. | Council, and the group mapping **[OWNER]** | [ADR-040](adr/ADR-040-the-portal-surface-is-how-arcgis-pro-connects.md), [ADR-036](adr/ADR-036-groups.md), [ADR-018](adr/ADR-018-authorization-and-roles.md) |
| Q-128 | **Does a style layer's `filter` need to be evaluated when drawing?** Opened 2026-08-20 with [ADR-041](adr/ADR-041-the-map-renderer.md). `SymbologyPlan` **refuses** a canonical document whose layer carries a `filter`, because MapLibre's filter language is a second expression dialect and drawing every feature of a layer whose style says to draw some of them is a map that is wrong without looking wrong. This server's own converter never emits one, so the refusal is only reachable by an author pasting a hand-written style — and then it is a refusal at read time for a document accepted at write time, which is [A-072](architecture-assumptions.md)'s premise inverted. **Either evaluate filters or refuse them at write time; the present state is the one arrangement that is wrong.** | 2026-08-20 | [ADR-033](adr/ADR-033-symbology.md), `SymbologyPlan` |
| Q-129 | **Which column is a layer's time, when it has more than one date?** Opened 2026-08-20 with WMS-T. The dimension is derived from the schema — exactly one `Date` column, or no dimension at all — because nothing in this server stores *which* column carries phenomenon time. A table with `created_at` and `observed_at` therefore publishes no time dimension, which is honest and useless; a table with only `created_at` publishes a dimension over the wrong column, which is worse. **The fix is a declared time field**, and it is a migration, an admin endpoint and a console control rather than a line. Recorded at the moment the derivation was chosen, not after somebody animated the wrong column. | 2026-08-20 | [ADR-041](adr/ADR-041-the-map-renderer.md) §5, `TimeDimension` |
| Q-130 | **Should `TIME` accept a list or a periodicity?** Opened 2026-08-20. WMS allows `TIME=a,b,c` and `TIME=start/end/P1D`; both ask for several maps and `GetMap` returns one. This server refuses them rather than answering with the first value, because a map of a moment the client did not ask about looks exactly like the one they did. **The question is whether anything real sends them** — an animation client asks for frames one at a time, and the list form is mostly used against servers that return an animated GIF. | 2026-08-20 | `TimeWindow` |
| Q-131 | **What does a legend show for a style that classifies?** Opened 2026-08-20. `GetLegendGraphic` draws one swatch, resolved against a feature with no attributes — the fallback branch of every expression. A `match` over a column has as many legend entries as the column has values, and enumerating them means reading the data on a request that is meant to be cheap and is asked once per layer per client. **A cartographer would call the present answer wrong rather than incomplete**, because a classified layer's legend is the whole point of a legend. | 2026-08-20 | [ADR-041](adr/ADR-041-the-map-renderer.md), `LegendGraphic` |
| Q-132 | **Does OGC API Features Part 3 — CQL2 filtering — get built?** Opened 2026-08-20 with [ADR-042](adr/ADR-042-ogc-api-features.md). Part 1's attribute filtering is equality on one property at a time, which is enough for a form and not for a query. CQL2 is a language with its own grammar, two encodings and a spatial and temporal function set — **and it is where [ADR-008](adr/ADR-008-query-engine.md)'s expression tree would earn its third caller**, after the ArcGIS `where` parser and the WFS filter reader. That makes it worth doing properly rather than partly, and it makes the tree's design testable by a third client that had no say in it. | 2026-08-20 | [ADR-042](adr/ADR-042-ogc-api-features.md) §3, [ADR-008](adr/ADR-008-query-engine.md) |
| Q-133 | **ANSWERED 2026-08-23: yes, and by repairing the other end.** A bounding box means the same thing in every reference system it can be written in, and neither of the two repairs this row listed is how it got there. **The defect was never in the filter.** It was that the published extent did not contain its own data once round-tripped, and the epsilon was a way of hiding that from one direction only. So the extent is published rounded outward to six decimals — about a tenth of a metre — and **every filter is compared exactly**. Part 1 §7.13 asks for an extent and does not ask for it to be tight: an extent is an upper bound, and a generous one costs a client nothing while an exact one that excludes its own features costs it everything. **The alternative — widen both spellings — also makes them agree**, and does it by making every filter in the product ten centimetres wrong, a false positive in the one operation a client uses to decide what is inside something. A document is the right place for an approximation; a filter is not, because a filter is a question with an exact answer. **And the repair was wrong once, found by measuring rather than by reading.** Rounding where the document is written left the filter clamping the request to the *un*rounded extent, which erased the rounding before the transformation and put the original defect straight back: the six-feature layer answered its own extent with four. **The invariant is that the extent a client is given is the extent the server clamps to.** `verifyBboxCrsParameter` is green — [cite-ogcapi10-2026-08-23.rdf](reviews/cite-ogcapi10-2026-08-23.rdf), 1,268 passed against all thirteen collections — and `OgcExtentConformanceTests` states the invariant from outside, checked by disabling the rounding and watching two of its three tests fail with the layers named. **Original follows.** **Should a bounding box mean the same thing in every reference system it can be written in?** Opened 2026-08-20 by the one CITE assertion still red. A `bbox` in the layer's own CRS is compared exactly; the same box in CRS84 is transformed, and a transformed comparison is not exact — so this server widens it by a micro-degree, without which a client sending a collection's own extent back got **nothing** from three of five layers. **The widening makes the two spellings disagree about a feature within ten centimetres of the edge**, which is what `verifyBboxCrsParameter` catches. Either both paths widen, or neither does and the exact-edge case is accepted as lost. **The second is not obviously wrong** — an edge test on a transformed coordinate is meaningless — but it has to be chosen rather than inherited. | 2026-08-20 | [ADR-042](adr/ADR-042-ogc-api-features.md) §7 |
| Q-134 | ~~**Why does ArcGIS Pro's raster reader refuse an ImageServer this server publishes, when the ArcGIS Maps SDK draws it?**~~ **ANSWERED 2026-08-22.** **Two conditions, both necessary, neither documented anywhere this project could read**: `allowAnalysis` must be `true`, and `capabilities` must contain the word `Tilemap`. With either missing, `arcpy.Raster(url)` answers `ERROR 000732: does not exist or is not supported`. With both present, ArcGIS Pro 3.6 opens the service, describes it, and draws it, and **every step of a nine-step probe then matches Esri's own `WorldElevation3D/Terrain3D/ImageServer` exactly**. **The measured table, because a single working configuration proves less than the grid around it:** `Image` + analysis&nbsp;true → refused; `Image,Tilemap` + true → **accepted**; `Image,Mensuration` + true → refused; `Image,Catalog` + true → refused; `Image,Tilemap,Mensuration` + true → **accepted**; `Image,Tilemap,Mensuration` + false → refused; `Image` + false → refused. **So it is `Tilemap` specifically and not merely a second token** — the two other capability words a reader might plausibly gate on were tried and neither works. **What settled it was a method, and the three attempts before it were guesses.** Forty structural fields, then real measured band statistics, then forty-odd field values changed one at a time by hand: all reasonable, all wrong, and each cost a round. The method that worked was to serve Esri's own document from this host over plain HTTP and watch arcpy accept it — which removed TLS, the self-signed certificate, tokens, the `/arcgis` path prefix and every other route from the question in a single experiment — and then delta-debug from **our** document upward, taking Esri's values in halves until the smallest set that flips a refusal into an acceptance is left. **The first bisect ran the other direction and stopped at its first answer**, reporting `allowAnalysis` and ending the loop with candidates still unexamined. That is a defect in the harness rather than in the finding: `allowAnalysis` really is necessary, it is simply not sufficient, and a harness that stops at one cause cannot tell those apart. Reversing the direction gives the minimal *fix* rather than a sufficient *cause*, which is the more useful of the two. **`allowAnalysis` was false because the flag was misread**, and that is the part worth keeping. It sounds like *this server performs analysis* and it means *this service may be used as input to analysis*: its pixels can be read for an arbitrary extent, size and reference, which is what `exportImage` does. True is the honest answer and false was a cautious guess about somebody else's vocabulary. It is true in the code now. **`Tilemap` was the harder half, because the fix and the lie were the same keystroke.** ADR-043 condition 5 forbids advertising a capability that is not served, and this face had no `tilemap` route, no tiling scheme and `tileInfo: null` — a client reading the claim would have had no scheme in which to name a tile, so the word would have been not merely unserved but unaddressable. **Pro never calls the operation**, which the mimic proved: a server answering only `/rest/info` and the service document, 404ing everything else, was accepted. So nobody would ever have caught it, which is the argument every untrue capability string has ever had and the one correctness gate 2 already rejected once. **Answered by serving the operation instead** — [ADR-044](adr/ADR-044-tiles-are-served-because-the-claim-had-to-be-true.md), the owner's decision of 2026-08-22: a tiling scheme, `tilemap`, and `tile`, which is `exportImage` at a fixed size over a computed extent and is byte-identical to it. The capabilities string is `Image,Tilemap` and both words answer. **The conformance suite now asks each word to answer rather than reading it out of the document**, and a word it does not recognise fails the build. **Four defects of our own were found on the way and all four are fixed**, each by replaying Pro's actual request rather than by reading the code. The request came out of a proxy put in front of the server — **which the server's own log already had, and believing otherwise was [D-138](architecture-debt.md), written and withdrawn the same day.** The log path in the development script was relative and the script `cd`-ed before using it, so the file beside the script was two days stale while the real one in the build directory held 8997 request lines. `bbox` arrives as an envelope object and this server read only the four-number spelling. `bboxSR` arrives as `{"wkid":102100,"latestWkid":3857}` — the retired Web Mercator code and the live one together — and the parser kept every digit after the first `wkid` and made 1021003857 of them. `format=None` is a client saying *your choice* and was refused by name, which is the `jpgpng` mistake one client later. And an operation this face does not serve answered with an empty-bodied 404, which ArcGIS Pro asked for forty times in one workflow; Esri's own server answers HTTP 200 with an error envelope, and so does this one now. **One reported failure was not ours and the control is why that is known.** `CopyRaster` fails with `ERROR 999999` against this server — and identically against Esri's own Terrain3D, because an image service with `allowCopy: false` does not offer a download. **`Describe.pixelType` was missing for ours and present for theirs, and the same delta debugging found the cause in one pass: `bandCount`.** Esri's service is single-band and ours was three-band; arcpy exposes a single `pixelType` on the describe object only for single-band rasters. Registering a single-band coverage and re-running the probe closed it — nine steps, no difference. The probe runs both services side by side now for exactly this reason: without the control column, a step no image service passes reads as a defect in this one. | Council | [ADR-043](adr/ADR-043-imageserver-and-the-raster-face.md) condition 1, [D-137](architecture-debt.md), [D-138](architecture-debt.md), Q-94 |

## Answered

| # | Question | Answer | Date | Recorded in |
|---|---|---|---|---|
| Q-04 | **Answered where it was carried, and moved here 2026-08-19 so the heading is the fact** — `tools/status-page.py` classifies by section and counted a struck-through row as *withdrawn*, which is a different thing from answered. Original: **What is the concrete DB connection budget at 1,000 services, per provider?** ADR-007 §4.8 gave the formula and the policy — pools per data source not per service, shrink-to-zero when idle, global cap per worker — and the numbers were missing. | **MEASURED 2026-08-19 for the one provider v1 has — [benchmarks/connection-budget/RESULTS.md](../benchmarks/connection-budget/RESULTS.md).** [ADR-007](adr/ADR-007-service-runtime.md) §4.8 gave the formula on 2026-08-12 and §10 condition 3 says it must be produced with real numbers before any deployment guidance is published; this counts them on a running server. **Per worker process: `(data sources + 1) × 100` potential, `≈1.3 × concurrent requests` in practice, `8` at rest.** Four findings, and three of them change something. **(a) Pooling really is per data source** — 48 clients over one layer peak at 64 backends and the same 48 over six layers add five more, not five times more. **(b) The formula counts one pool too few.** A request draws on *two*: the layer's data source and the **platform store**, which every query touches for the per-request catalogue read (D-17 put it there; D-30 priced it at 1.8 ms and 70% of a small query). So it is `nodes × workers × (data sources + 1) × pool size`. **(c) The pool size is 100 and nobody chose it** — no connection string sets `Maximum Pool Size`, so every pool takes Npgsql's default. §4.8's own worked example uses 5. That makes the baseline deployment 200 potential connections and *1,000 services over five registered databases* **700 per worker** — against a default PostgreSQL's `max_connections` of 100, so **one worker with two busy data sources can exhaust the database by itself**, and §4.8's *"at fifty data sources it is 1,000, which is not [survivable]"* is optimistic twentyfold. **And nothing enforces it:** the global cap per worker §4.8 requires is not built, so the failure at the ceiling is `FATAL: sorry, too many clients already` on an arbitrary request rather than a queue. **(d) Shrink-to-zero already works, and further than the code claims** — `LayerConnections` records it as not implemented; measured, 79 backends fall to **0 in 184 seconds** on Npgsql's own pruning, because `MinPoolSize` is 0 by default. But **the floor on an idle server is 8, not 0, and it is our own polling**: the two job workers claim every two seconds, round-robin over the platform-store pool, so no connection in it can ever reach its idle lifetime — [D-110](architecture-debt.md). **What this does not settle:** pool size 100 is read from the absence of a setting rather than measured (the plateau needs 100 concurrent queries at one pool; the run stopped at 48 clients); one worker and one node, so those factors are arithmetic rather than observation; and what happens *at* the cap cannot be measured until the cap exists. **Per provider**, the shape is kept and only PostgreSQL is filled in — Oracle and SQL Server price sessions differently and are not in v1. What transfers is the structure, including the `+ 1`, which is ours and not the provider's. | 2026-08-19 | ADR-007 §4.8 and §10 condition 3, [benchmarks/connection-budget/RESULTS.md](../benchmarks/connection-budget/RESULTS.md) |
| Q-82 | **Read-only or read/write for MySQL, MariaDB and DuckDB?** Q-50a made Oracle and SQL Server **full read/write**, deliberately and as the most expensive of three options. Extending that precedent to MySQL and MariaDB means two more transaction models, two more concurrency semantics and two more definitions of a conflict, all under A-027 — which is already the load-bearing assumption for editing. DuckDB is different again: it is single-writer by design, so read/write is not merely expensive but arguably wrong. **Recommendation: read-only for all three at first**, matching Honua, with MySQL promoted later if anyone asks. That breaks the *all providers are equal* symmetry, and the capability report (ADR-008 §2) is exactly the mechanism for saying so out loud. | **Deferred with [v1-scope](../docs/v1-scope.md) §3a, recorded 2026-08-15.** Not a live question: v1 is PostGIS only, and §3a defers Oracle, SQL Server, MySQL, MariaDB and DuckDB together as the largest single simplification available to the project. The recommendation in the question — read-only for all three at first — stands as the starting position when the first of them returns, and it needs an ADR then rather than an answer now. | 2026-08-15 | [v1-scope](v1-scope.md) §3 |
| Q-95 | **Should serving survive a platform-store outage, and is it worth caching an authorization decision to get that?** Opened 2026-08-14 by D-17's partial fix. Today every feature request reads the catalogue, so if PostgreSQL is down the server stops serving even layers it has answered a thousand times — the isolation [ADR-019](adr/ADR-019-portal-server-split.md) spent when it fused the tiers. Caching the catalogue entry would buy that back and would also cache the sharing scope and the started/stopped status: **a layer made private would stay readable, and a service stopped mid-incident would keep answering, for as long as the cache lived.** With two servers over one store no local invalidation fixes it, because the mutation happens in the other process. So the question is not *can we cache it* but **which failure is worse** — an outage that stops serving, or a window in which a revoked permission is still honoured. There may also be a third answer nobody has costed: serve stale-but-narrowed, honouring only the *last known* scope and refusing anything that would widen access. | **Answered 2026-08-15 by the owner: serve public-only while blind.** While the platform store is unreachable the server answers from the last catalogue entry it saw, but only for services whose remembered sharing was `Public`; everything else is refused with a 503 that says why. A stale `Public` grant discloses nothing that was not already given away, which makes it the one remembered permission worth acting on. Resolves the [ADR-007](adr/ADR-007-service-runtime.md) §4.3 contradiction by amending §4.3. | 2026-08-15 | [ADR-026](adr/ADR-026-serving-through-a-platform-store-outage.md) |
| Q-90 | **Which geometry vocabulary does the SDK expose — and can it round-trip?** **Owner direction, 2026-08-13: ArcGIS geometry types at the SDK boundary.** Consistent with the architecture — Tier 1 stays Simple Features and the SDK is an edge like the API is — and consistent with the positioning, since an ArcPy refugee expects `Polyline`, `Polygon` with rings and `SpatialReference` rather than `MultiPolygon`. **The tension to resolve:** Q-74 chose GeoParquet/Arrow for the data boundary explicitly *because geopandas, pyarrow and shapely read them natively* — so the format was chosen for Python GIS developers and the types are being chosen for ArcPy refugees, and those are different populations. **The hard requirement either way, and it is testable:** domain → SDK → domain must be **lossless**. **Half of it is now tested, 2026-08-14, at the API boundary rather than the SDK one** — `ArcGisRoundTripAgainstPostgisTests` takes real geometry from PostGIS, writes it as ArcGIS JSON, reads it back and asks PostGIS whether anything changed, using `ST_OrderingEquals` and comparing part and ring counts. It holds across 300 real polygons, multi-ring polygons, lines and points, and a three-part MultiPolygon keeps its three parts. **One thing the round trip legitimately changes:** ArcGIS requires clockwise shells, so a counter-clockwise one comes back reversed — the comparison is therefore against `ST_ForcePolygonCW` of the original, which is a representation change and not a loss. The SDK boundary is still untested because the SDK does not exist; the conversion beneath it now is. ArcGIS `Polygon` is a flat bag of rings with winding order carrying the part structure, so a tool that reads a `MultiPolygon`, nudges one vertex and writes back must not silently merge two parts — re-classification on the way in is the lossy direction (ADR-005 §3.3c). **Shape that satisfies both:** ArcGIS-familiar names and methods with the explicit part structure preserved underneath, so `rings` is what an ArcPy user sees and nothing is lost on the return. Deferred with the SDK itself (Q-88 cut GPServer from v1); recorded now because it is cheap to state and expensive to discover. | **Deferred with the SDK, [v1-scope](../docs/v1-scope.md) §3c, recorded 2026-08-15.** The owner's direction stands — ArcGIS geometry types at the SDK boundary — and half the requirement is already tested at the API boundary by `ArcGisRoundTripAgainstPostgisTests`. There is no SDK to test the other half against and will not be in v1. | 2026-08-15 | [v1-scope](v1-scope.md) §3 |
| Q-75 | **How is user-supplied Python sandboxed?** — **the publish-rights half is answered by [ADR-015](adr/ADR-015-authentication.md) §7, 2026-08-13: not the publisher role.** Publishing data and publishing executable code that runs on our server are different risks and a permission covering both is wrong in one direction or the other. Code publication defaults to administrators. The sandbox itself remains open. Gates Q-17b, and it is the largest security surface in the product by a wide margin — arbitrary code execution by design, against a server holding the organisation's spatial data. Out-of-process is settled by ADR-006's own reasoning (job workers provide isolation) and by the fact that a segfault in a C extension must not take a worker with it. What is not settled: process user and container boundary, CPU / memory / wall-clock / disk limits, whether tools get network access at all, and **who may publish a tool** — the publisher role from the self-service model, or administrators only. My instinct is administrators only at first; a publisher uploading data is a different risk from a publisher uploading code. | **Deferred with [v1-scope](../docs/v1-scope.md) §3c, recorded 2026-08-15**, which names this question by number. User-supplied code — GPServer, the Python SDK, the sandbox, the curated wheel set — is out of v1, so the sandbox has nothing to sandbox. The publish-rights half stays answered by [ADR-015](adr/ADR-015-authentication.md) §7: not the publisher role. | 2026-08-15 | [v1-scope](v1-scope.md) §3 |
| Q-72 | **What is the project's governance and maintenance story?** Created by Q-49's answer, and absent from §55 which covers licence obligations only. Giving a server to the world obliges a release process, a documented security-response path with a contact and an embargo policy, a contribution policy and CLA-or-not decision, an issue triage commitment, and a statement of what is *not* supported. A public server product with no security contact is a liability rather than a gift. | **Answered 2026-08-15 by the owner: minimal governance, written before the repository goes public.** A security contact with real dates, DCO sign-off rather than a CLA, and no triage or support commitment — because a commitment one person cannot keep costs more than an absent one. What is deliberately not written is named as absent rather than left to be inferred. | 2026-08-15 | [ADR-025](adr/ADR-025-governance-and-maintenance.md), [SECURITY.md](../SECURITY.md), [CONTRIBUTING.md](../CONTRIBUTING.md) |
| Q-94 | **ANSWERED IN PART 2026-08-19, and by the other protocol.** The owner's instruction — *"WFS'i destekleme ile başlayalım"* — makes WFS the first surface built after v1 ([ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md)), which answers this row's *never-could-afford* half: the QGIS population gets a front door, and it is the one this row named as their reflex. **Two things it does not do, and they are why the row stays open.** It does not pay recommendation (a) — *verify the QGIS path against a real QGIS rather than assuming it* — which is now owed **twice**, for the ArcGIS surface it was written about and for the WFS surface chosen instead; ADR-039 condition 1 carries it, and nothing has yet been pointed at this server. And it does not take recommendation (b): the register's advice was **OGC API Features first**, on cost, and it was overridden by ordering rather than refuted — recorded as dissent in ADR-039 §10 rather than quietly dropped. **Amended 2026-08-19: somebody walked through one of them.** ArcGIS Pro 3.6 connected to `/wfs`, listed every feature type and drew two of them. That is the first time any real client has reached this server, and it closes the *nothing has ever been pointed at us* half of this row. **Amended again the same day: the ArcGIS REST surface has now been driven too.** ArcGIS Pro added `/rest/services/hosted/tr_il/FeatureServer/0` through *Add Data → Data From Path* and worked the whole surface — layer document, service document, then paged queries carrying `outFields=*`, an `outSR` given as a full spatial-reference object rather than a code, a polygon `geometry` filter with `esriSpatialRelIntersects`, `resultOffset`, `resultRecordCount` and `orderByFields`. Every request answered 200. **So the sentence this row was written around — *nothing has ever been pointed at us* — is no longer true of either surface.** What it cost to find out: three wrong turns recorded as [Q-126](#) and [D-121](architecture-debt.md), and a token endpoint that had been advertised and never built. **The row still does not close**, because its own recommendation (a) names **QGIS**, and QGIS is not installed on any machine here. Two clients working is evidence about the documents; it is not evidence about the population §1 says this product is for. GDAL has also read the WFS surface end to end ([tools/wfs-client-probe.py](../tools/wfs-client-probe.py)); QGIS, the client this row names, is still unmeasured. Original follows: **Largely dissolved the same day it was opened, by the owner clarifying that the licence in question is ArcGIS *Enterprise*, not ArcGIS Pro.** The question assumed the market had never used ArcGIS and therefore ran QGIS. It has not: the target is an organisation with ArcGIS clients and ArcGIS-shaped data that cannot afford the *server*. **ArcGIS Pro is a likely primary client, so Q-88's ArcGIS-first ordering is strongly right rather than questionable, and OGC API Features stays in v2 as scoped.** What survives, and is now more urgent rather than less: the ArcGIS REST *discovery* surface did not exist at all — no `/rest/info`, no service or layer metadata — so no ArcGIS client could add a service. **Built and verified 2026-08-14 by a conformance suite** that walks the exact request sequence an ArcGIS client makes — `/rest/info`, catalogue, service document, layer document, query — and asserts each document carries what the client needs to reach the next. 17 tests, over HTTP against a running process rather than in-process, and with no reference to our own assemblies: a conformance test that asserts against the same constant the server reads agrees with the server while both are wrong. **Verified to fail** with no server, with nothing listening, and against a deliberately reintroduced regression. The QGIS check remains worth doing as a second client, at much lower priority. Original question follows: **Who is the client, and does OGC come back into v1?** The sharpest consequence of the 2026-08-14 positioning, and it points at [Q-88](#), the one decision that has ever *removed* scope. **v1 is ArcGIS-first and defers OGC API Features to v2**, and [v1-scope.md](v1-scope.md) §4 justifies that inversion by saying it is *consistent with why the product exists* — Q-49's *everything an ArcGIS Server shop needs in order to leave.* **That justification now covers only half the stated market.** The *can-no-longer-pay* half has ArcGIS data and clients and needs exactly what v1 builds. The *never-could* half — the larger one — runs **QGIS**, and a QGIS user's reflex is WFS and WMS, not `/rest/services`. **The mitigating fact, which is why this is a question and not a reversal:** QGIS ships an *ArcGIS REST Server* connection type and consumes ArcGIS Feature Services natively, and `esri-leaflet` and OpenLayers do too — all free. So the never-could population *can* consume our v1 surface; the question is whether they will look for it. **Recommendation: do not reverse Q-88.** ArcGIS-first is largely built and the compatibility work is not wasted. But (a) verify the QGIS path against a real QGIS rather than assuming it — it is now the **primary client** and nothing has ever been pointed at us; and (b) move OGC API Features from *v2* to *the first thing in v2*, because for most of the market it is the front door rather than a later protocol. **What would reverse Q-88:** discovering the QGIS path does not work well, which is a day of testing rather than an argument. | See the question cell — the answer was recorded there while the row sat under *Open, not blocking*. | 2026-08-14 | ADR-005 |
| Q-93 | **Answered by the owner, 2026-08-14: no.** *"I don't want to change existing ArcGIS users. I want to reach people who don't want to pay, or cannot pay, for ArcGIS licences."* Federating into somebody's Portal means they are still paying Esri for the Portal, which is the one deployment shape the positioning excludes by definition. **Closed rather than deferred** — it is not a sequencing question, it is out of scope. Original: **Should Graticula be federatable into an existing ArcGIS Portal?** Opened by [ADR-019](adr/ADR-019-portal-server-split.md) §6, which decided we are one fused deployable and explicitly did *not* decide this. **It is the difference between replacing ArcGIS Enterprise and slotting into it**, and it is a market question before it is a technical one. An organisation already running Portal — having paid for it, trained on it, and built apps against it — is not a candidate to replace it, but may well be a candidate to put a free serving tier underneath it. **What it would take:** accept Portal-issued tokens, expose the Server admin API Portal expects during federation, and register ourselves as a site. **What it would cost:** the compatibility surface stops being *unmodified clients keep working* (Q-07, Q-17) and becomes *another vendor's server keeps working with us*, which is a much stricter contract and one they can change. **Also worth noting it cuts against Q-49's positioning** — a federated serving tier under somebody's Portal is a component, not the gift to the world the owner described. Recorded rather than answered. | See the question cell — the answer was recorded there while the row sat under *Open, not blocking*. | 2026-08-14 | ADR-019 |
| Q-96 | **RESOLVED 2026-08-15 by owner direction, implemented the same day.** *"The imported shapefiles need to stay in their own projection. If we use a 3857 basemap, it shall be projected on the fly."* **Transform anything, on the tile path.** A layer now keeps the reference it arrived in — the importer no longer transforms on the way in — and `PostGisTileSource` moves the tile envelope once into the layer's reference for the index filter, then transforms each surviving row on the way out. Measured at 74.6 ms against 21.6 ms, paid once per tile by the cache. **The third option in the original question — report the accuracy PROJ used — is not built**, and is now [D-32](architecture-debt.md): a national grid whose shift grids are absent gets a ballpark transformation from PROJ, silently and by metres. Original: **Should the tile path reproject, and how far?** Opened 2026-08-14 when a 4326 layer published for tiles was found to answer 204 for every tile on Earth, silently — `ST_TileEnvelope` is Web Mercator, `&&` compares boxes in different units, `ST_AsMVTGeom` clips everything away, and **PostGIS raises nothing**. The endpoint now refuses SRID ≠ 3857 with a message, so the silence is fixed either way. What is open is whether it should instead transform. **Measured, not guessed:** `ST_Transform` in the select with the tile envelope transformed once into the layer's own reference — so the spatial index is still used — costs **74.6 ms against 21.6 ms** on the same tile, 3.5×, and produces the same tile. So it is affordable, and with a tile cache in front it is paid once. **The reason it is a question rather than a change** is that not all transformations are alike: 4326 → 3857 is a closed formula with no datum shift, while a national grid such as EPSG:27700 needs shift grids PROJ may not have installed — and PROJ's default behaviour when they are missing is to fall back to a ballpark transformation that moves features by metres without failing. A tile server that quietly draws a country a few metres from where it is would be worse than one that refuses. Three answers are available: refuse everything but 3857 (today); transform only where the transformation is grid-free; transform anything and report the accuracy PROJ actually used. | See the question cell — the answer was recorded there while the row sat under *Open, not blocking*. | 2026-08-15 | ADR-021 |
| Q-97 | **RESOLVED 2026-08-15 by owner decision, and implemented the same day** — worker process plus pre-flight, [ADR-022](adr/ADR-022-geometry-server.md) §9. The bound is a process with a ten-second deadline and a 1 GB heap ceiling; the candidate-pair pre-flight at 100,000 is a filter and is documented as one, because finding 16 measured it under-predicting fourteenfold. NetTopologySuite is referenced by the worker and by nothing else, so an overlay cannot allocate in the server's heap. Verified against the input that took the host down: the 6,408-vertex comb pair now answers 400 in under a second and the server keeps serving. Original: **How does GeometryServer offer general overlay without offering a denial of service?** Opened 2026-08-14 by A-042's invalidation. Measured: a **6,408-vertex** adversarial comb pair costs **153 s and 16.7 GB** against **312 ms and 17 MB** for a real **72,919-vertex** national outline — and the run that produced it pushed the host into swap and killed the Docker daemon. One unauthenticated request would do that. Vertex count does not discriminate; candidate segment pairs predict far better (83 ms foresees a 17 s operation) and still under-predict the adversarial case 14×. **No input-derived cap bounds the work**, and OverlayNG has no cooperative cancellation, so a wall-clock cap cannot interrupt it in-process. Four options, not exclusive: **(1)** a separate process per overlay killed on a deadline — the only one that is actually a bound, costing a process launch and a serialisation boundary; **(2)** a pre-flight candidate-pair estimate, cheap and leaky; **(3)** authentication and a privilege, which narrows who can do it rather than stopping it; **(4)** ship only the linear-cost operations — project, lengths, areas, label points, simplify — and not `intersect`, `difference`, `union` or `cut`. | See the question cell — the answer was recorded there while the row sat under *Open, not blocking*. | 2026-08-15 | ADR-022 |
| Q-98 | **RESOLVED 2026-08-15 by owner decision, implemented the same day** — a bounded exception ([ADR-024](adr/ADR-024-shapefile-import.md)). Archives are opened by exactly one class under five bounds: 256 MB total and per member enforced *while reading* rather than believed from the header, 32 members, a 100× compression-ratio refusal, and an extension allow-list that does not include `.zip` — so a nested archive is impossible rather than depth-limited. The DBF encoding is **asked for rather than guessed** when the file does not declare it, because reading cp1254 as UTF-8 produces text rather than an error. The reader is ours (Tier 1), verified against a corpus written by pyshp and by PostGIS. Original: **Do we accept shapefiles, and if so what happens to the no-decompression rule?** Opened 2026-08-14 by hosted-data import. [security.md](security.md)'s upload section says archives are never opened — *decompression bombs are not our problem if we never decompress* — and a shapefile is a ZIP of at least three files (`.shp`, `.shx`, `.dbf`, usually `.prj`). So GeoJSON shipped and shapefile did not. **This is not a small omission for the audience.** The people this product is for mostly have shapefiles; telling them to convert first is telling them to install QGIS, which is the friction the product exists to remove. Three answers: accept the rule and never take shapefiles; write a bounded exception (a hard uncompressed-size ceiling, a member-count ceiling, refuse nested archives, never write to disk); or accept them only through a path that already has a trusted operator behind it. The middle one is what every other tool does and the rule was written to avoid. | See the question cell — the answer was recorded there while the row sat under *Open, not blocking*. | 2026-08-15 | ADR-024 |
| Q-68 | **Answered 2026-08-14 by measurement, and the answer is no — [ADR-021](adr/ADR-021-tile-encoding.md).** [Run 4](../benchmarks/mvt-generation/RESULTS.md) measured exactly what this question asked for. Round-trip count was the whole surviving case and it is real — 256x fewer at z12→z16 — and it bought nothing: read-once was **slower on median** (144.0 against 170.2 tiles/s) in its own best case, and at z12→z14 the per-round ratio ranged 0.61 to 1.84, which is this machine's noise floor rather than a result. What did separate the paths came from counters rather than a wall clock and was stable across every run: **124-245x the allocation**, and a GC pause reaching **35.5% at concurrency 4 against 0.0%** — A-037 appearing in the workload that was supposed to be the encoder's best case. Cost of the decision: none in production code, because `/benchmarks` is never promoted and none was written. **What is untested and recorded as ADR-021 condition 1:** `ST_AsMVT` puts every byte of tile cost inside the datastore, the benchmark machine's datastore had headroom, and ADR-019 makes it mandatory and shared. | **No, and by measurement** — [ADR-021](adr/ADR-021-tile-encoding.md). Read-once was slower on median in its own best case, and the only stable separation was 124-245x the allocation with a GC pause reaching 35.5%. The reasoning is in the Question cell, where this row kept it. | 2026-08-14 | **Recorded in:** [build-vs-adopt-policy.md](build-vs-adopt-policy.md), ADR-008 §4.8, ADR-010 §6 |
| Q-70 | Is PostgreSQL now mandatory, and do we accept that? | **Yes, accepted.** Follows arithmetically from Q-69 + Q-32: the datastore is required and it is PostGIS-only. **This reverses Q-22 and reinstates A-009**, which was invalidated earlier the same day — recorded as a reversal rather than smoothed over, because the reason given for invalidating it (a PostgreSQL requirement is a barrier for ArcGIS-on-Oracle shops) was correct and has simply been outweighed. What outweighs it: Q-32 ships the datastore as a *managed appliance* we install, configure, back up and upgrade, so the ask is **run our container**, not **acquire a PostgreSQL DBA**. **What it deletes:** the SQLite platform-store dialect, one set of migrations, one job-claim implementation, and roughly half the platform-store CI matrix — the same class of cut as Q-51, which is notable because Q-51's own reasoning (*SQLite already solves the no-PostgreSQL requirement completely*) is what this voids. **What it costs, and it is real:** an organisation with a no-PostgreSQL policy cannot run this product at all. That was A-018's entire point and it is now accepted as a lost segment. **What it restores:** [CLAUDE.md](../CLAUDE.md) §6 has always stated the baseline deployment target as `graticula → PostgreSQL/PostGIS`. The architecture and the stated baseline now agree for the first time. | 2026-08-12 | [product-context.md](product-context.md), [ADR-002](adr/ADR-002-primary-data-architecture.md) §4b |
| Q-69 | Is the datastore still optional? | **No. It is mandatory.** Every deployment runs it. Q-32 shipped it as an *optional* managed appliance; Q-67 then made it the only source of tiles, and the owner has now made it required outright rather than required-if-you-want-tiles. **What this simplifies:** there is one deployment shape instead of two, the datastore stops being a conditional branch through deployment profiles (§53), backup, upgrade and the air-gapped checklist, and the "datastore-only deployment" and "registered-only deployment" cases collapse into one baseline plus optional registered sources. **What it opens:** Q-70, which is not a detail — a mandatory PostGIS datastore means PostgreSQL is mandatory, and that reverses Q-22. | 2026-08-12 | [data-model.md](data-model.md), ADR-002, deployment profiles (§53) |
| Q-57 | How is a stable feature identity produced, given registered tables we do not control? | **Declared, not inferred.** Hosted layers get `globalid uuid` plus a monotonic `objectid` by construction. For registered layers the administrator **nominates** the identity column in the service definition (the Q-36 mechanism) — we do not synthesise from `row_number()`, which is not stable, and we do not maintain a side mapping table, which would be state about somebody else's table and would drift on their first edit. **The asymmetry that matters:** OGC API Features accepts a string `id`, ArcGIS FeatureServer requires a unique integer OID. A registered table keyed by UUID or text is fully servable natively and **not servable through the compatibility layer** without DDL. The compatibility layer has a stricter data requirement than the product it wraps. | 2026-08-13 | [ADR-013](adr/ADR-013-feature-service-data-model.md) §2 |
| Q-58a | Do we support related records, and how are relationships modelled? | **Yes, v1. Declared in our service definition, not reverse-engineered from Esri system tables.** Reading `GDB_ITEMS` / `GDB_ITEMRELATIONSHIPS` was rejected on three grounds: it is reverse-engineering Esri internals (§5), it only works when the source is a geodatabase, and it breaks silently whenever Esri changes the layout. Declaration is **strictly more capable**: it works on a plain PostGIS, Oracle or SQL Server schema with ordinary foreign keys, and an administrator can relate two tables that were never designed to be related. `queryRelatedRecords` becomes one `IN` query with pushdown rather than N+1. | 2026-08-13 | [ADR-013](adr/ADR-013-feature-service-data-model.md) §3 |
| Q-58b | Do we support attachments, and where do the bytes live? | **Yes, v1. In the database, Esri's model** — owner decision, chosen over object storage with proxied delivery. Transactional with the edit, backed up with the data, byte-compatible with a migrated `__ATTACH` table, and one storage tier rather than two. **Two hard conditions come with it.** Streaming is mandatory: run 3 measured 80.9% GC pause at 18% CPU on a 20 MB/request workload, and an attachment is arbitrarily large and user-chosen, so materialising one reproduces A-037's ceiling on demand. And database storage exposes the connection pool — a slow client holding a streaming connection is slowloris pointed at ADR-007 §4.8's budget, mitigated by a separate bounded pool (A-041). Dissent and a concrete revisit trigger recorded in [ADR-013](adr/ADR-013-feature-service-data-model.md) §9. | 2026-08-13 | [ADR-013](adr/ADR-013-feature-service-data-model.md) §4 |
| Q-03 | Where does each class of geometry work execute: our code, the adopted engine, or the database? | **Three tiers, in cost order** — [ADR-003](adr/ADR-003-geometry-engine.md) §6a, 2026-08-13. **Push down to the provider** (bbox filter, clip, simplify, supported predicates); **ours on flat arrays** (rectangle clip, tile simplify, tile-space transform, MVT encode); **adopted NTS** (overlay, buffer, validity, precise predicates, all of GeometryServer). Pushdown is first because finding 11 measured a z16 tile reading **201,580 vertices to emit 2,080** — a tile's cost floor is set by the largest geometry overlapping it, so *the cheapest geometry operation is the one that never crosses the wire*. §6b gives three conditions for owning a primitive: an exploitable special case, the hot path, and renderable-is-sufficient correctness. All three required, and GeometryServer fails all three, which is why it is entirely adopted. Scope is **single-provider**; cross-engine consistency is A-043, deferred with the second provider as its trigger. | 2026-08-13 | [ADR-003](adr/ADR-003-geometry-engine.md) |
| Q-71 | What is the one-artefact story now? | **Three images, and the count is not the interesting part** — [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md), 2026-08-13. Server (no GDAL, no Python), datastore (a thin derived PostGIS image, because Q-32 promised an appliance *we* back up and we cannot promise that about an image we do not build), and job-worker (GDAL, PROJ grids, Python and a curated wheel set). Not split further: §82 says no until size or scheduling actually hurts, recorded as a trigger. **The interesting number is what lives in a volume**, and the state inventory gained three entries nobody had listed — certificates, sessions, and Python tool source. | 2026-08-13 | [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §2–§3 |
| Q-13 | What is the upgrade and rollback story? | **Answered by [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md), 2026-08-13, after being named in six documents without an owner.** **The version handshake refuses; it does not migrate** — auto-migration on startup is how a stale container silently rewrites a newer schema, and the result looks like corruption rather than a mistake. Migration is explicit, takes a backup first, and reports what it will do. **Migrations are expand-and-contract**, which closes N9 — standard discipline, and impossible to retrofit after the first migration that breaks it, which is why it costs nothing today and cannot wait. **Rollback is supported to exactly one prior version and only before contract runs**; beyond that, recovery is restore-from-backup. That is honest rather than generous: once contract runs, rollback would be data loss dressed as a procedure. Contract is never automatic and never same-session, so the door stays open until the operator closes it. | 2026-08-13 | [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §4–§6 |
| Q-76 | Can Python tools install dependencies? | **No — decided by [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §7's rule that nothing is fetched at runtime.** The job-worker image carries a **curated wheel set** we version and patch. The question was framed as convenience versus offline and offline wins, because an air-gapped install cannot reach PyPI and Q-15 assumes none. The cost is real and recorded as A-049: the wheel set becomes ours to maintain, and if users routinely need packages we did not anticipate, either it grows unmanageably or the offline promise breaks. Escape hatch is a customer-built worker image, which shifts the burden rather than removing it. | 2026-08-13 | [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §7 |
| Q-88 | What is v1? | **Decided by the owner, 2026-08-13, and it is the first time scope in this project has gone down.** **PostGIS only** — hosted datastore plus registered customer PostGIS — with **ArcGIS FeatureServer, VectorTileServer and GeometryServer**, an admin API, TLS, authentication, packaging and free migration tooling. Full statement: [v1-scope.md](v1-scope.md). **What it removes:** every other database (killing A-043's six geometry engines, Q-20, ADR-008's per-dialect table, two-thirds of A-047 and A-027, D-05, ADR-008 condition 1a, and review findings A9 and P10); rendering and everything needing it (dissolving Q-85 and letting ADR-009 re-close); user-supplied Python (deferring Q-17b, Q-74–Q-76, letting ADR-006 re-close, and removing review finding O3 from v1); and the rest of the protocol surface including the two engines that *"do not exist in any form"*. | 2026-08-13 | [v1-scope.md](v1-scope.md) |
| Q-79 | Were SensorThings and 3D Tiles chosen, or swept in? | **Swept in — answered by omission, 2026-08-13.** Q-88's v1 excludes both. The observation store and the 3D/terrain pipeline, the two engines [protocol-surface.md](protocol-surface.md) §1 recorded as having no foundation in anything decided, are not built. The question was raised precisely because they may have arrived by pointing at a list rather than being chosen; they had. | 2026-08-13 | [v1-scope.md](v1-scope.md) §3d |
| Q-85 | Is rendering in v1, or in scope but gated? | **Dissolved by Q-88, 2026-08-13.** Rendering and every capability that needed it — WMS, MapServer, ImageServer, OGC API Maps and Coverages, WCS — are out of v1. ADR-004 stays `DEFERRED` and no longer contradicts an in-scope capability, which was the severe finding (sweep S2, review A7). Q-77's Tier 1 line defers with it. | 2026-08-13 | [v1-scope.md](v1-scope.md) §3b |
| Q-55 | Where does TLS belong in the architecture? | **Answered by [ADR-014](adr/ADR-014-tls-and-certificates.md), 2026-08-13. Closes failure-scenario N8 (severe) and discharges blocker B3.** Four surfaces, not one: the serving endpoint, data-source connections, outbound COG fetches, and internal traffic. **We terminate TLS ourselves by default; a reverse proxy is supported and never required** — the same reasoning as Q-69, that the ask is *run our container*, not *also acquire an nginx administrator*, for a primary user who is a GIS administrator rather than a web infrastructure engineer. HTTPS from first start with a generated self-signed certificate; plain HTTP only behind a flag that warns at every startup. **The load-bearing requirement is rotation without restart**, and not mainly for convenience: restarting a worker to load a certificate evicts every warm service context, triggering exactly the cold-start storm ADR-007 §4.4's affinity routing exists to prevent. Certificate expiry becomes a supervisor duty with 30/7/1-day lead time — a total outage with a known date is inexcusable to be surprised by — **and it supplies one of F5's three missing 2 AM scenarios.** Forwarded headers are honoured only from configured trusted proxies, default empty. Air-gapped: no ACME, soft-fail revocation, internal CA support. Opened A-044, A-045. | 2026-08-13 | [ADR-014](adr/ADR-014-tls-and-certificates.md) |
| Q-73 | What licence does the project ship under? | **Apache-2.0.** Chosen over MIT for the express patent grant, which MIT lacks and which matters for infrastructure: contributors and users are protected from patent assertions by contributors. Chosen over AGPL because the owner said unrestricted, and AGPL is a restriction — a real one that some enterprises ban outright. **The consequence, accepted:** anyone may fork this, close it, and sell it as a managed service. For a project whose stated purpose is *give it to the world*, that is the purpose rather than a leak. `LICENSE` and `NOTICE` added 2026-08-13; the repository was public and legally all-rights-reserved until then. Note this is the **outbound** licence — the earlier *copyleft acceptable* decision was about inbound dependencies and is unaffected. | 2026-08-13 | `LICENSE`, `NOTICE`, [DEPENDENCY-LICENSES.md](../DEPENDENCY-LICENSES.md) |
| Q-49 | What does this do that GeoServer cannot, for whom, and why is that worth the migration cost? | **Answered by the owner, 2026-08-13, and the question turned out to be two questions.** *Why does it exist:* **"I will give this to the world."** Open source, public, unrestricted. That is sufficient and complete — QGIS, GeoServer and PostGIS all began the same way, and a gift needs no market case. **It also dissolves the §81 exit criterion** that required testing Q-49 with real GIS teams: that criterion assumed a commercial-style justification the project does not need. *What makes someone switch:* **not** "better capabilities than GeoServer", which is currently false and getting more so — vector-first, Q-47, Q-67, Q-70 and Q-28 each removed a capability GeoServer has, and it also has WCS, WPS, CSW, SLD and a plugin ecosystem we will not build. **Motive corrected by the owner 2026-08-14: the job is not persuasion, it is the invoice.** We are not arguing anybody out of ArcGIS — existing paying users are explicitly not the target — we are catching the ones already falling, plus the far larger group who never could afford it at all. The second group changes what *switching* even means, since they have nothing to switch from; see Q-94. Original phrasing: The true and testable version is **everything an ArcGIS Server shop needs in order to leave, which GeoServer does not provide**: full ArcGIS FeatureServer compatibility including `applyEdits` (Q-17), free migration tooling (Q-16), a real service runtime with affinity routing, never-degrade-silently capability reports, and self-service publishing. Narrow, defensible, and it prioritises the roadmap where "better than GeoServer" does not. Opens Q-72 and Q-73. | 2026-08-13 | [competitive-position.md](competitive-position.md) §6, [product-context.md](product-context.md) |
| Q-67 | Can vector tiles be served from registered databases? | **No. Tiles come only from hosted data, strictly — data that lives in the datastore as system of record.** Registered Oracle, SQL Server and foreign PostGIS layers get feature services and full ArcGIS FeatureServer compatibility, and never tiles. Owner decision, taken against the measured alternative rather than in advance of it: [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) run 3 showed the registered-source tile path at 28.3 req/s and 80.9% GC pause against 96.3 req/s and 0.3% for a PostGIS-native path. **What it resolves:** A-039 and the tile half of A-021 dissolve, [D-05](architecture-debt.md) shrinks from the most consequential gap in the evidence to a feature-query question, three dialect tile implementations disappear, and A-037's GC ceiling stops being a tile problem. **What it costs:** an organisation on Oracle must copy data to get tiles, not merely point at it — accepted deliberately. **What it aligns:** the two user types now map cleanly onto the two paths. Publishers host and get tiles; GIS administrators register from desktop and get features. **What it opens:** Q-68 and Q-69. | 2026-08-12 | [product-context.md](product-context.md), ADR-008 §4.8, [data-model.md](data-model.md) |
| Q-06a | Who is the primary user? | **The GIS administrator.** Promotes the admin API, service lifecycle observability, RBAC and good defaults to first-class requirements. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-06b | What is the day-one workload? | **Features first, then vector tiles.** Confirms the §71–§73 sequencing and the walking-skeleton target. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-25 | Do we support the MapLibre GL Style Spec as a style format? | **Effectively yes.** The vector-first decision makes the client the renderer, and MapLibre style is the format it speaks. Styles are stored and served, not evaluated. Formal confirmation belongs in ADR-004. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-26 | How is cross-tile label consistency achieved? | **REOPENED 2026-08-20**, exactly as [competitive-position §6a](competitive-position.md) predicted it would be. It was closed on the grounds that labels are placed client-side; the WMS and MapServer faces render labels on the server, so the platform has the problem again. **Concretely:** `LabelPlacer` decides placement per request against that request's pixels, so a client tiling its GetMap calls can see a name drawn in one tile and dropped in the neighbour, and a name near a seam can be drawn twice in slightly different places. **Not solved and not hidden** — [ADR-041](adr/ADR-041-the-map-renderer.md) §7 names it, and the honest scope is that WMS here is correct for a single full-extent request and approximate for a tiled client. The usual answers are a placement pass over a buffer wider than the request, or a shared placement cache keyed by scale; neither is written. | 2026-08-12, reopened 2026-08-20 | [ADR-041](adr/ADR-041-the-map-renderer.md), [product-context.md](product-context.md) |
| Q-31 | Does the feature service expose provider capability differences, or hide them? | **Expose them.** A capability report is published per layer, and unsupported operations are refused with an explanation rather than answered slowly by dragging data back. The organising principle is *never degrade silently*. Conditional on the capability report shipping with the first refusal. | 2026-08-12 (an earlier answer on 2026-08-12 was withdrawn as a misreading of the ArcGIS model) | [ADR-008](adr/ADR-008-query-engine.md) §2 |
| Q-02 | What is the modern equivalent of ArcSOC? | **Workers are sized to the machine, not to the catalogue.** A small number of multi-tenant request workers, threaded, with service contexts bound lazily and evicted LRU under a bounded per-worker budget; separate isolated job workers for geoprocessing, GDAL registration and plugins. Isolation on the workload axis, not the service axis. Services do not start, so there is no thundering herd. `ACCEPTED WITH CONDITIONS`. | 2026-08-12 | [ADR-007](adr/ADR-007-service-runtime.md) |
| Q-27 | How is per-layer authorization enforced when imagery is delivered as COG? | **Proxy by default; signed URLs as an optional optimisation.** The earlier framing overstated the cost — COG range requests are view-proportional, a few hundred kilobytes for what is on screen, so proxying is comparable to serving tiles. The proxy also works on every backend including a plain filesystem, gives us a place for rate limits and audit logging, and its responses are cacheable. Signed URLs work only where the store supports signing and cache nowhere we control. | 2026-08-12 | [ADR-009](adr/ADR-009-raster-engine.md) §2.4 |
| Q-10 | Do we ship a third-party plugin system, or only internal extension points? | **Internal extension points only, for now.** No SDK, no public contract, no versioning for external consumers. The trigger that reopens it is specific: someone needing a provider, format or operation we have decided not to build. If it reopens, geoprocessing comes first because job workers already provide the isolation, and data providers come last because they sit in the request path with no cheap isolation story. | 2026-08-12 | [ADR-006](adr/ADR-006-plugin-model.md) |
| Q-38 | Can a layer move between registered and hosted, keeping its identity? | **No.** Editing is done against the database directly, with QGIS. | 2026-08-12 | [data-model.md](data-model.md) |
| Q-40 | Do we accept data uploads / hosting at all? | **Yes.** The datastore is a first-class store and an organisation may run entirely hosted. It may also have no GIS database of its own, so datastore-only is a supported deployment. | 2026-08-12 | [data-model.md](data-model.md) §4 |
| Q-28 | Can GDAL-backed providers be made optional, so a PostGIS-only deployment is one artefact? | **Yes, by construction, if we adopt the rule that the serving container ships no GDAL** — it lives only in the job worker image. The argument does not need a precedent: it is a rule about the *artefact* rather than about where code runs, which is what makes it checkable at build time instead of by inspection, and it is cleaner than our own ADR-009 §2.2 placement for that reason alone. Restores weight to ADR-001's C7, shrinks the attack surface, and shrinks the air-gapped checklist. **Strengthened 2026-08-16, and the stronger form is available to us:** a peer implementation reaches the same end by keeping GDAL out of the *managed dependency graph entirely* — no GDAL or OSGeo package reference anywhere in the solution, and the worker image built on `ghcr.io/osgeo/gdal` shelling out to `gdalwarp` and `ogr2ogr` as command-line tools. That is a stricter rule than ours and a mechanically verifiable one: a package reference is a thing a test can forbid, where an image boundary is a thing a human has to notice. | 2026-08-12, strengthened 2026-08-16 | ADR-001 C7, ADR-009 §2.2, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-51 | Do SQL Server and Oracle need to be platform stores? | **No — conclusion stands, but the reasoning below is void.** **Amended 2026-08-13 (sweep S7):** Q-70 deleted SQLite entirely, so *SQLite already solves it* is no longer a reason for anything. They are not platform stores because **there is exactly one platform store, PostgreSQL**. Original reasoning, retained and now false: Cut. SQLite already meets the no-PostgreSQL requirement, being embedded and holding a few thousand non-spatial rows; the remaining justification was an unevidenced policy requirement. They stay first-class **providers and datastores**, where the requirement is real. Removes two dialect implementations, two migration paths, two job-claim implementations and half the CI matrix. | 2026-08-12 | [ADR-002](adr/ADR-002-primary-data-architecture.md) §4a, [reviews/fresh-challenger-review-2.md](reviews/fresh-challenger-review-2.md) G6 |
| Q-29 | Which platform stores ship in v1? | **Both, because there are only two.** Dissolved by Q-51. | 2026-08-12 | [ADR-002](adr/ADR-002-primary-data-architecture.md) §4a |
| Q-47 | Is WMS in the compatibility layer in scope for v1? | **Answered again 2026-08-20: WMS exists, and v1 scope is still No.** The owner asked for it on 2026-08-20 and it was built — 1.3.0 and 1.1.1 with WMS-T, [ADR-041](adr/ADR-041-the-map-renderer.md) — as work **outside** v1, the way [ADR-039](adr/ADR-039-wfs-is-the-first-surface-after-v1.md) §1 recorded for WFS. So *WMS-client migration is unsupported at v1* below is now a statement about the scope boundary and not about the software: a WMS client can be pointed at this server today. **The owner's stated objection is not withdrawn** — it was *"I hate WMS. Super slow"*, and the answer to the speed half is a measurement rather than a rebuttal: see [benchmarks/map-rendering/RESULTS.md](../benchmarks/map-rendering/RESULTS.md). The original 2026-08-12 answer follows, unedited. **No.** ADR-004 stays deferred and the F7 contradiction is resolved by removing the requirement rather than by un-deferring the ADR. WMS-client migration is unsupported at v1 and must be documented as such. **Strengthened 2026-08-13:** the owner rejects WMS **on its merits** — *"I hate WMS. Super slow."* — rather than merely cutting it for scope, and prefers a REST-style rendered map service in the manner of an ArcGIS MapService, with a symbology model better than SLD. Re-confirmed on being asked directly that this is a stated direction and **does not change v1 scope**; ADR-004 stays `DEFERRED`. Recorded in ADR-004 §0 so that a future revisit does not default to *add WMS*. | 2026-08-12 | [ADR-004](adr/ADR-004-rendering-engine.md), [product-context.md](product-context.md) |
| Q-32 | Is the datastore in v1, and on which engines? | **v1, PostGIS only, optional.** Shipped as a managed component we install, configure, back up and upgrade - the ArcGIS Data Store model - so the customer never operates it. **A-022 retired.** The three-engine datastore was an over-application of the no-mandatory-PostgreSQL decision: that objection was about the operational burden of running a database, and a managed appliance removes most of it. An organisation that still refuses runs registered-only and loads data into their own database with QGIS, which is the workflow already endorsed for editing. | 2026-08-12 | [data-model.md](data-model.md) |
| Q-54 | Does the runtime supervisor (§21) have a design? | **Yes** - [runtime-supervisor.md](runtime-supervisor.md). Per-node supervisor process, tiny, serving no requests, with the platform keeping it alive and workers surviving its death for re-adoption. Not the distinguished node §4.10 forbids, because ArcSOC's SOM was site-wide and this is local. Governing principle: a management-plane failure must not become a data-plane failure. The hard parts - memory-growth detection, adoption protocol, routing placement - are named as Q-63 to Q-65 rather than assumed. | 2026-08-12 | [runtime-supervisor.md](runtime-supervisor.md), ADR-007 §4.13 |
| Q-56 | What is the policy for a single geometry too large to serve? | **Three tiers rather than one limit.** Over the limit because of many features: paginate. Over the limit because of one feature: return that feature alone with a warning header, since the limit protects the server and a single feature the user asked for is not an attack. One feature over the absolute cap: refuse *that feature* by id, not the query. Refusing the whole query because one feature in a layer is large is the wrong granularity. | 2026-08-12 | [geometry-crs-policy.md](geometry-crs-policy.md) §6 |
| Q-08 | Does the platform own its data, or serve data registered elsewhere? | **Neither is the default. Both are first class, chosen per layer by a lifecycle test** taken from Esri's guidance: *does this layer's data have a life outside the service?* If yes, register it. If the service essentially **is** the data, host it - and accept that we then own its lifecycle including deletion. Recorded with the honest caveat that the two modes are not symmetric in difficulty: everything hard is on the registered side. | 2026-08-12 | [data-model.md](data-model.md) |
| Q-17 | Do we serve an ArcGIS-compatible REST surface? | **Yes - full FeatureServer including edits.** Query plus `applyEdits`, `addFeatures`, `updateFeatures`, `deleteFeatures`. The backend exists because Q-50a gave us write capability on all three engines, and it is what makes a migration complete rather than stalling at the last mile: definition import moves the server, this keeps the *clients* working. ~~**Not** MapServer, ImageServer, GeometryServer or GPServer - those produce rendered images, which vector-first removed and Q-47 kept out of v1.~~ Raises Q-57 and Q-58. **AMENDED 2026-08-13 — the exclusion list was wrong, and wrong in a way that matters.** Two of those four do not produce rendered images. GeometryServer returns geometry as JSON; GPServer returns whatever tool was published to it. Four services were excluded under one justification that applies to two of them. Corrected per-service: **GeometryServer is now IN scope** (§below), **GPServer stays out for a different and better reason**, ImageServer and MapServer stay out on the original grounds. | 2026-08-12, amended 2026-08-13 | ADR-005 3.3, compatibility layer |
| Q-52 | Which formats are serving providers and which are import sources? | **Answered 2026-08-13 by Q-83, which put the whole list in scope and forced the enumeration this question had been asking for since it was opened.** **Import sources** (converted at registration, never served from the file): Shapefile, GeoPackage, **File Geodatabase**, KML, GPX, WKT, GeoJSON, FlatGeobuf, GeoParquet. **Output formats** (serialised from a query result): GeoJSON, MVT, FlatGeobuf, GeoParquet, GeoArrow. **Serving providers** remain databases only — PostGIS, SQL Server, Oracle, MySQL, MariaDB — plus DuckDB as the file-query engine (Q-81), which is what lets a file be queried without becoming a provider. **The split §27 got wrong is now explicit: a format is not a provider.** Two consequences: **A-038 is confirmed** — File Geodatabase has no managed .NET reader, so GDAL is not avoidable and A-016's job-worker placement stands; and **GeoParquet now has three independent justifications** (Q-74's Python SDK boundary, Q-81's DuckDB engine, and this list), having had none a week ago. | 2026-08-13 | ADR-008, [A-038](architecture-assumptions.md), Q-74, Q-81 |
| Q-83 | Is everything still marked *Open* on the capability matrix in scope? | **Yes, owner decision 2026-08-13.** The honest reading matters more than the decision: **most of these are not new scope, they are known gaps being confirmed.** Auth (API key, OIDC, SAML, SCIM), TLS, OpenTelemetry, health probes and OpenAPI were already recorded as not-started or severe gaps — several are already blockers B3 and B4 in the [exit plan](phase-0-exit-plan.md). Protocol conformance suites were flagged as untenable to omit once Q-78 claimed 29 faces. Cron scheduling is small. A versioned release is ours to cut. **Two things are genuinely new scope: the format lists (Q-52, answered below) and geocoding (Q-84).** | 2026-08-13 | [protocol-surface.md](protocol-surface.md), [phase-0-exit-plan.md](phase-0-exit-plan.md) |
| Q-84 | Do we build a geocoder (ArcGIS GeocodeServer)? | **In scope by Q-83, and it is a new subsystem rather than an endpoint — flagged alongside Q-79's two.** Geocoding needs address parsing and normalisation, reference data, candidate matching with scoring, reverse geocoding and batch operation. **Address parsing is locale-specific**, and a product built in Turkey serving Turkish addresses cannot reuse a US-centric parser — that is a feature, since most open geocoders are Anglocentric, but it is also real work. **The sharper question is reference data, and it is the same shape as GPServer's toolbox:** a geocoder without street centrelines or address points is an empty shell. Three options — build the engine and require the customer to bring their own reference data (Esri's address-locator model, and the only one that works air-gapped); integrate an external service such as Nominatim or Pelias (fails Q-15 offline and adds a dependency we do not control); or ship reference data ourselves (a licensing and freshness commitment nobody wants). **Recommendation: the first**, which also matches how an ArcGIS shop already works. | Council, with the reference-data choice **[OWNER]** | Q-83, Q-15, ADR-006 |
| Q-80 | Are MySQL and MariaDB providers? | **Yes, owner decision 2026-08-13 — and as two providers, not one.** They were listed as a single row on the capability matrix and that is the Q-17a bundling error again. **MySQL 8 and MariaDB are not the same engine for spatial purposes.** MySQL 8 has a reasonably complete OGC function set backed by **Boost.Geometry**, SRID-aware columns and R-tree indexes on InnoDB. MariaDB's spatial support diverged and sits closer to MySQL 5.7 in coverage. They need separate capability declarations and separate conformance runs, or one of them will silently claim the other's abilities. **Also inherited: MySQL 8 follows the SRS definition's axis order**, so EPSG:4326 is latitude-first — a well-known footgun and a direct concern for [geometry-crs-policy.md](geometry-crs-policy.md). Write scope is Q-82. | 2026-08-13 | ADR-008, [geometry-crs-policy.md](geometry-crs-policy.md) |
| Q-81 | Is DuckDB a provider? | **In scope, but not as a registered-source provider — as the file-format query engine.** Q-53's objection to warehouses was that they are analytical stores, not serving stores, and DuckDB is an embedded OLAP engine with a single-writer model: pointing concurrent tile and feature requests at it is the same mistake in a smaller package. **Where it genuinely fits is where Honua puts it — embedded and read-only** — and it fits even better than their framing suggests, because it reads GeoParquet, FlatGeobuf, Shapefile and GeoJSON natively and speaks Arrow. That is the same boundary **Q-74** just chose for the Python SDK, and it is a candidate answer to **Q-52**'s never-enumerated import format list and to **A-038**'s GDAL question. One engine for *files*, rather than a reader per format. **The cost is Q-20, which named this exact risk before DuckDB was in scope.** | 2026-08-13 | ADR-008, Q-52, Q-74, [A-038](architecture-assumptions.md) |
| Q-78 | Do we pursue full protocol parity with Honua? | **Yes, owner decision 2026-08-13** — sixteen further protocols in scope: WCS 2.0.1, WPS 2.0, OGC API Maps / Tiles / Coverages / Processes / Records / Styles / EDR, SensorThings v1.1, OData v4, STAC API, 3D Tiles, Terrain-RGB, PMTiles, gRPC and MCP. **The count is the wrong unit and [protocol-surface.md](protocol-surface.md) says why.** Twenty-nine protocol faces sit over **ten engines**, eight of which are already decided or in flight. Five of the sixteen are near-free standard surfaces over things already built (Tiles, Styles, PMTiles, Records, MCP); five are real work on chosen foundations (OData, gRPC, STAC, Processes, WPS); three are gated on the reopened raster engine or the deferred renderer; and **two are new products rather than new endpoints** — Q-79. **The valuable side effect:** six faces over the feature engine is the hardest available test of [ADR-005](adr/ADR-005-api-architecture.md)'s protocol-neutral internal interface, which was asserted and never proven. If it is real, faces five and six are cheap. If it leaks, this finds out, and finding out is worth more than the protocols. | 2026-08-13 | [protocol-surface.md](protocol-surface.md) |
| Q-17b | GPServer — in scope, and Python-based? | **Yes to both, owner decision 2026-08-13, reversing the answer given hours earlier in Q-17a.** GPServer was declined because *we have a job system with no toolbox*. **The toolbox is not ours to write — users write it, in Python.** That is the ArcPy and PyQGIS model and it turns GPServer from an empty shell into an extension point. **It reopens [ADR-006](adr/ADR-006-plugin-model.md) by the exact route ADR-006 predicted**: its stated trigger was *someone needing an operation we decided not to build*, and it named geoprocessing as the first candidate precisely because job workers already provide isolation. **Sequenced behind the Python SDK** — owner's condition, and correct: a GPServer without a defined SDK is a protocol with nothing behind it. Opens Q-74, Q-75, Q-76. | 2026-08-13 | ADR-005 §3.3a, [ADR-006](adr/ADR-006-plugin-model.md), [ADR-011](adr/ADR-011-job-system.md) |
| Q-17c | ImageServer — in scope? | **Yes, owner decision 2026-08-13**, reversing Q-17a and **reopening [ADR-009](adr/ADR-009-raster-engine.md)**, whose central decision was *serve COG and let the client render*. **Decided per operation, applying Q-17a's own lesson rather than repeating its mistake** — ImageServer is not one capability, it is roughly three at very different costs. **Near-free** (a COG range read or a GDAL call we already make): `identify`, `getSamples`, `computeHistograms`, `computeStatisticsHistograms`, `download`, and mosaic footprint `query`, which is a feature query over the catalogue and needs nothing new. **Medium**: tiled access to already-processed imagery. **Expensive, and the real decision**: `exportImage`, raster function chains, and dynamic mosaicking — these need a raster rendering engine, a processing-chain evaluator, and a mosaic dataset model with footprints, overviews, priorities and seamlines. That last group is plausibly the largest single capability in the whole matrix, larger than the vector renderer. Sequencing and the Tier 1/Tier 2 line are Q-77. | 2026-08-13 | [ADR-009](adr/ADR-009-raster-engine.md), ADR-005 §3.3a |
| Q-17a | Which ArcGIS service types beyond FeatureServer do we serve? | **Decided per-service 2026-08-13**, replacing Q-17's blanket exclusion. **GeometryServer — IN.** It is a REST wrapper over capability [ADR-003](adr/ADR-003-geometry-engine.md) already commits us to holding in-process: `project` and `findTransformations` are PROJ, which we need regardless; `buffer`, `intersect`, `difference`, `union`, `convexHull`, `cut`, `densify`, `generalize`, `areasAndLengths`, `distance`, `relation` and `labelPoints` are all NetTopologySuite, already in-runtime. Near-zero marginal cost. **And it serves Q-17's own purpose**: ArcGIS Enterprise portals configure a geometry service URL that client applications call, so omitting it partially defeats *existing clients keep working* — `VERIFY`, since ArcGIS JS API 4.x performs much of this client-side via `geometryEngine`, while Pro, Web AppBuilder apps and portal configuration still reference one. **GPServer — OUT, for a better reason.** It is a container for published geoprocessing tools and we have a job system with no toolbox; exposing it would be an empty shell. This is precisely [ADR-006](adr/ADR-006-plugin-model.md)'s stated trigger for reopening the plugin model — someone needing an operation we decided not to build. **ImageServer — OUT**, tied to [ADR-009](adr/ADR-009-raster-engine.md): it needs server-side raster rendering and dynamic mosaicking, which vector-first removed. **MapServer — OUT of v1**, tied to [ADR-004](adr/ADR-004-rendering-engine.md) §0, which the owner wants eventually and deferred deliberately. | 2026-08-13 | ADR-005 §3.3, [ADR-003](adr/ADR-003-geometry-engine.md) |
| Q-16 | Is migration tooling in scope, or API compatibility only? | **Inventory plus definition import, and free.** Two separable steps, a decomposition taken from Honua: (1) scan an existing GeoServer or ArcGIS Server and report what exists, what we can bring across and what we cannot — cheap, high trust, and useful *before* anyone commits; (2) import service and layer definitions. Data stays where it is, since Q-50a made registered sources fully capable. Styles convert where they can and report where they cannot. **Free deliberately**, against a competitor who charges for the equivalent — see Q-49. **2026-08-14: its value concentrates rather than disappears.** The positioning excludes voluntary migrators, so nobody will run this to *evaluate* us. The user is somebody whose licence lapses on a date, who needs their existing services to keep working, and who is not shopping. That makes the inventory scan more valuable, not less — it is the first honest answer they get about whether landing here is possible at all. | 2026-08-12 | [product-context.md](product-context.md), ADR-005 |
| Q-50 | How do we serve Oracle and SQL Server data? | **(a) Full read/write providers.** Chosen deliberately over read-only providers and over migration tooling. Registered Oracle and SQL Server layers are editable through our API with the same capability as PostGIS, subject to granted rights. **This is the most capable and most expensive of the three options**, and it was the existing design by default rather than by choice — now it is chosen. What it commits us to: three transaction semantics with three isolation models and three definitions of a conflict; A-027 stays load-bearing; Q-41 becomes likely rather than optional; and ADR-008's write-path gap must be filled before implementation. | 2026-08-12 | [ADR-008](adr/ADR-008-query-engine.md), [product-context.md](product-context.md) |
| Q-42 | Is feature editing through our API in scope? | **Yes.** An earlier answer of "no" was a wrong inference: the owner's statement about editing happening in the database concerned **table structure**, not feature data. Schema changes on registered layers happen at the source; feature data editing goes through our API. §28's CRUD, batch editing, editor tracking and optimistic concurrency are all in scope. | 2026-08-12 (corrected same day) | [data-model.md](data-model.md) §5, [ADR-005](adr/ADR-005-api-architecture.md) §3.8 |
| Q-33 | Does hosting copy once or track the source? | **Dissolved.** Hosted data is the system of record, not a copy of anything. Derived artefacts are openly derived and have a refresh policy. | 2026-08-12 | [data-model.md](data-model.md) §1 |
| Q-21 | Does the Query AST target more than one SQL dialect from day one? | ~~**Yes, necessarily.**~~ **VOID 2026-08-16 — the question no longer has a subject, and it took two cuts to get there.** v1-scope left one engine, PostGIS, so *more than one dialect* is not a day-one property of anything; and ADR-008 §4a-i records that what shipped compiles the attribute filter straight to PostGIS SQL, so there is no dialect-neutral stage for a second dialect to target. **Both halves of the original answer are now false and neither was refuted** — they were removed by scope. Original text, retained because the reasoning was sound when the product had three engines: *Yes, necessarily. Oracle Spatial and SQL Server Spatial are first-class alongside PostGIS. Capability negotiation is core, not a refinement.* Capability negotiation itself survives and is still core; it is the *dialect* claim that is void. Reopens with [D-40](architecture-debt.md)'s trigger. | 2026-08-12, void 2026-08-16 | [ADR-008](adr/ADR-008-query-engine.md) §4a-i, [D-40](architecture-debt.md), [research/multi-database-consequences.md](research/multi-database-consequences.md) |
| Q-22 | Is a PostgreSQL-free deployment profile a goal? | ~~**Yes.**~~ **REVERSED 2026-08-12 by Q-70.** No: a mandatory PostGIS datastore makes PostgreSQL a hard dependency of every deployment. The original answer and its reasoning are kept rather than deleted — the reasoning was sound and was outweighed, not refuted. Original text: *Yes. PostgreSQL is not mandatory. Reframed into Q-29 (which stores ship when). A-009 invalidated.* | 2026-08-12 | [product-context.md](product-context.md), [ADR-002](adr/ADR-002-primary-data-architecture.md) §4a |
| Q-05 | Does platform metadata live in PostgreSQL, in files, or both? | **A relational store is the source of truth; files are an export/import format.** Amended same day: the store is portable across SQLite, PostgreSQL, SQL Server and Oracle. The platform database is logically distinct from data source databases, which may be foreign and read-only. `ACCEPTED WITH CONDITIONS` — see the conditions in ADR-002 §9. | 2026-08-12 | [ADR-002](adr/ADR-002-primary-data-architecture.md) |
| Q-07 | Is displacing existing ArcGIS Server / GeoServer deployments a goal? | **Yes.** The compatibility layer (§51) is therefore a product requirement, not an option — still strictly outside the core domain, still strictly clean room. Raises Q-16 and Q-17. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-106 | What is the reference implementation's real licence? | **Open, and narrowed 2026-08-16: the console is Apache-2.0, the server is still unknown.** `honua-io/honua-console` states Apache-2.0 in its own public repository, which is evidence about the project's posture and settles nothing about the server — a split-licence open-core product is exactly the shape where the permissive half tells you least about the other. The rest stands. **Open, and it is now load-bearing.** [ADR-030](adr/ADR-030-reading-the-reference-implementation.md) opened the reference's `src/` to reading by owner decision on 2026-08-16, before this was known. The checkout's own `LICENSE` file says Elastic License 2.0 and the owner states that is not the real licence, so the file cannot answer it. The answer changes what is permitted rather than merely how it is described: **AGPL or GPL** makes the code compatible with this project's copyleft position and reusable with attribution, ending the question; **Apache or MIT** likewise, with a [DEPENDENCY-LICENSES.md](../DEPENDENCY-LICENSES.md) entry; **source-available but proprietary** leaves clean room binding at full strength and makes ADR-030's first revisit trigger live. Until it is answered the exposure accepted by ADR-030 is unbounded rather than measured — that is the dissent recorded in ADR-030 §10, not a resolved concern. | **[OWNER]** — asked 2026-08-16, unanswered | [ADR-030](adr/ADR-030-reading-the-reference-implementation.md) §3, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-110 | **Does deferring the renderer mean our own console cannot draw a map?** | **Open, and it was decided by accident.** [ADR-004](adr/ADR-004-rendering-engine.md) is `DEFERRED` and [v1-scope](v1-scope.md) §3b cut rendering — a decision about what we serve *customers*. On 2026-08-16 it turned out to have also decided what our own admin console can show: with no renderer, the console's basemap is either a third party's rendered tiles (ADR-020 §4c, and OpenStreetMap's usage policy says not theirs), 196 KB of vendored Natural Earth that answers only *where in the world is this*, or vector data the operator imports and we cut tiles from — which works, and is boundaries rather than a map. **Nobody chose that consequence; it fell out.** Three ways to close it, in ascending cost: accept a third-party basemap as the documented default and say so in the deployment guide; ship a documented import path for an OSM extract so an operator's own tiles are the basemap (the pipeline already does this — ADR-020 §4d); or un-defer enough of ADR-004 to render a ground ourselves, which is a renderer, a style and a full extract, and is not small. **Recommendation: the second**, because it is built already and needs only writing down — but the first is what most deployments will actually do, and pretending otherwise is how a guide gets ignored. | Council, with the deployment default **[OWNER]** | [ADR-004](adr/ADR-004-rendering-engine.md), [ADR-020](adr/ADR-020-admin-console-and-service-status.md) §4c, §4d, [v1-scope.md](v1-scope.md) §3b |
| Q-109 | Does `havingClause` come back, parsed? | **Open, and the capability is absent until it is answered.** [D-41](architecture-debt.md) removed the clause because it was reaching SQL unparsed; `supportsHavingClause` is now `false` and the parameter is refused. **The grammar needed is small** — an aggregate function over a field the layer actually has, or a declared `outStatisticFieldName`, compared against a numeric literal, with `AND`/`OR`/`NOT` and parentheses. That is narrower than `WhereClause` already handles, and it would reuse its shape exactly: identifiers matched against real columns, operators from a fixed table, literals bound. **The reason it is a question rather than a task:** nobody has asked for it. ArcGIS Pro sends `havingClause` only from the statistics dialogue, a client can filter returned aggregate rows itself, and `where` already narrows rows before aggregation, which covers most of what people actually want. §82 says an unrequested capability waits. **Recommendation: wait for the first real request, then build the parser rather than the pass-through.** What must not happen is the pass-through returning because a client complained about a 400. | Council | [D-41](architecture-debt.md), [ADR-008](adr/ADR-008-query-engine.md) §4a-ii, `WhereClause` |
| Q-108 | ~~**Now that a managed File Geodatabase reader is known to be possible, do we write one?**~~ **ANSWERED by the owner 2026-08-18: no — the worker.** *"workerla gidelim o zaman"*, after all four routes were on the table and the fourth (a migration converter outside the deployment) was the one closest to their stated preference for no GDAL at all. So the decision is taken with its cost visible rather than by default: GDAL lives in the Python worker image, the serving container carries none ([A-016](architecture-assumptions.md) intact), and a deployment that refuses the worker loses geodatabase import and nothing else. [ADR-037](adr/ADR-037-job-workers-come-in-two-kinds.md) is the decision; this row is the survey behind it. Original: | **Open, and it is a cost question rather than a possibility one.** [A-038](architecture-assumptions.md) said GDAL was unavoidable because no managed .NET reader for `.gdb` exists; a peer has one, so the assumption is invalidated and the honest replacement is *what would ours cost*. **The prize is specific:** vector import with no GDAL anywhere, which shrinks the air-gapped checklist, restores ADR-001 C7 further than A-016 already did, and removes an image from the deployment. **The cost is specific too:** `.gdb` is a reverse-engineered binary format — table headers, varint field encoding, geometry blobs, and the long tail of writer quirks across ArcGIS versions — and its failure mode is a silently mis-read field rather than an exception. GDAL's `OpenFileGDB` driver is the public reference implementation and the reason this is doable in the clean room at all. **A fourth route was missed until the owner asked again, and it is the only GDAL-free one that keeps the feature:** *"onu gdalsız python ile kapatamaz mıyız?"* sent the survey through Python as well as .NET — the answer is the same, every open-source route links GDAL, and two libraries advertising *"pure Python"* or *"no ArcPy"* turn out to mean *no subprocess* rather than *no native library* ([file-geodatabase-readers.md](research/file-geodatabase-readers.md) §8c). But asking the question that way exposed that §1–§8 had only ever asked *which library does the server load*. **A one-shot migration converter, run by the customer, outside the deployment**, puts GDAL in a tool nobody operates — and [v1-scope](v1-scope.md) §3 already has migration tooling in scope. Its weakness is that it is a thin wrapper over one `ogr2ogr` command, so what ships is a recipe and a support burden, landing on the customer least able to absorb it. **That trade is the owner's.** And note what does *not* transfer: [ADR-009](adr/ADR-009-raster-engine.md) §2.1 gets raster off GDAL because COG is already the served format, so nothing needs reading — a geodatabase is never our storage or served format, so it always needs reading. **Recommendation reversed 2026-08-18 by the owner's two later inputs — *"gdal da ekleyebiliriz"* and *"illa .net olmak zorunda değil"*: do not write one at all.** `pyogrio` in the Python worker Q-17b already brings reads a `.gdb` in one line, GDAL-backed and Arrow-native, and Arrow is the boundary [Q-74](#) had already chosen for the other direction. **Writing our own managed reader is now the wrong project rather than a deferred one**, because its whole prize was removing GDAL: GDAL is acceptable, and the remaining prize — one fewer runtime — is *lost* by writing .NET, since Python arrives with geoprocessing regardless. This also opens [Q-120](#): the job worker's language is decided nowhere. **Superseded reasoning below, kept because the survey it rests on is still the evidence.** Original: **not for v1.** Q-52 already routes `.gdb` through the job worker, and that worker needs GDAL for raster regardless, so the dependency is not removed by winning this — only narrowed. Reopen when a deployment forbids the worker image, or when raster leaves the worker. **Surveyed 2026-08-18 at the owner's request, in [file-geodatabase-readers.md](research/file-geodatabase-readers.md), and the owner added the constraint that decides it:** *"bulunan şey gdal'a bağımlı olmasın ama."* With that, the library question has no answer — **there is no ready-made, open-source, GDAL-free `.gdb` reader for .NET**, across three searches; the two .NET routes that exist depend on Esri's closed SDK or are commercial. So the constraint resolves this row to *write our own*, arrived at from the other direction. **The note's cost estimate is the useful part**: `spark-gdb` is a pure managed reader built from the same public specification by one author, it reached Point/Polyline/Polygon with XYZM, and its own TODO list stops before **multi-part geometries, XML fields, Blob fields and rasters** — last commit January 2016. Below, the earlier survey — *"peki gdb alırken kullanabileceğimiz açık kaynaklı bir kütüphane yok mu?"* — and the survey moves the recommendation from *probably not* to *clearly not*, for three reasons it did not have before. **First, `OpenFileGDB` does far more than this row assumed.** It carries no proprietary dependency (*"does not depend on a third-party library"*), reads ArcGIS 9.x and writes 10+ since **GDAL 3.6**, and supports **relationship classes since 3.6, attribute domains since 3.3, raster layers since 3.7, and curves**. This row and A-038 both described it as *the public reference implementation* for reading; it is a read-write implementation of most of the format. **Second, no public managed .NET reader surfaced at all.** The two .NET routes that exist both fail our constraints: [GeoDataToolkit](https://github.com/g-a-freitas/GeoDataToolkit) abstracts *"the ESRI APIs details"* and therefore depends on Esri's closed *"free as in beer"* FileGDB API, and [Aspose.GIS](https://docs.aspose.com/gis/net/gdb-file-esri/) is commercial, which §7's licensing stance rules out for a product given away. **Stated as an absence of evidence rather than a proof:** a US-only web search on one day did not find one, and the peer's own reader (A-038) is not public. So *writing our own* means being the first public managed implementation of a reverse-engineered format — which is a much larger claim than *the peer did it, so it is possible*. **Third, the obstacle this project recorded against relationships dissolves.** `RelationshipEndpoints` and migration 1229 both say reading relationship classes out of a geodatabase's `GDB_ITEMS` is Esri internals that CLAUDE.md §5 forbids; GDAL exposes relationships as an API, so that route exists without touching anybody's internals. It does not change ADR-013's decision — relationships stay ours and engine-independent — it removes one stated reason from the argument, which is worth knowing when that argument is next made. **And two limits are now recorded rather than discovered by a customer:** `OpenFileGDB` cannot read `.gdb` compressed as **SDC** (Smart Data Compression) or **CDF** (Compressed Data Format), and support for tables with sparse 64-bit `OBJECTID`s is *"read-only and incomplete"*. | **[OWNER]** — raised 2026-08-16 | [A-038](architecture-assumptions.md), Q-52, Q-15, ADR-009, [reference-reading-log.md](research/reference-reading-log.md) |
| Q-107 | Do the registers keep naming the reference product? | **Open.** The name appears 48 times across 19 documents, and four answers are sourced to it by name — Q-16 (migration decomposition), Q-28 (GDAL out of the serving image), Q-78 (protocol parity, an owner decision), Q-81 (DuckDB embedded and read-only) — as is [ADR-029](adr/ADR-029-affinity-routing-is-not-the-default.md). Two facts make this a question rather than a preference: the local checkout has been anonymised, and the two research documents that carried the underlying analysis are **deleted in the working tree**, leaving **21 broken links** to `research/honua-server.md` and `research/honua-capability-matrix.md`. So the reasoning behind four decided answers currently points at nothing. Three options: restore the research documents, rewrite the four answers to stand on their own reasoning without the citation, or scrub the name and accept that the provenance of those four answers becomes unrecoverable. **Recommendation: the second** — a decision that cannot survive losing its source citation was not properly argued in the first place. | **[OWNER]** — raised 2026-08-16 | [ADR-030](adr/ADR-030-reading-the-reference-implementation.md) §1 |
