# Open Questions Register

Do not hide uncertainty (§61). A question belongs here from the moment it is
noticed, not once it becomes convenient to answer.

**Ownership:** questions marked **[OWNER]** are product or business decisions and
are the only ones escalated to the project owner (§84). Everything else the
council investigates, prototypes, benchmarks and decides on its own.

---

## Blocking Phase 0

| # | Question | Owner | Resolves in |
|---|---|---|---|
| Q-01 | Which language, on measured evidence? | Council | ADR-001 |
| Q-03 | Where does each class of geometry work execute: our code, the adopted engine, or the database? | Council | ADR-003, ADR-008 |
| Q-04 | What is the concrete DB connection budget at 1,000 services, **per provider**? [ADR-007](adr/ADR-007-service-runtime.md) §4.8 gives the formula and the policy — pools per data source not per service, shrink-to-zero when idle, global cap per worker. The numbers are still missing and are a stated condition. | Council | ADR-007, `benchmarks/connection-budget/` |
| Q-18 | What genuinely justifies a server over static cloud-native publishing **and over a stateless thin server plus a capable database**? Sharpened by [research/postgis-thin-servers.md](research/postgis-thin-servers.md): pg_tileserv deletes publishing, authorization, geoprocessing and MVT encoding with one constraint. Each subsystem we keep must be individually defensible. | Council | [product-context.md](product-context.md), assessment §8-§10 |
| Q-20 | **How many distinct geometry engines end up evaluating our predicates, and how do we prevent behavioural divergence?** — **escalated 2026-08-13, and the register called this one in advance.** It was written as *PostGIS's, ours, possibly DuckDB's* before DuckDB was in scope. Q-80 and Q-81 put it in scope and add two more. The count is now **six**: PostGIS's GEOS, DuckDB's GEOS, MySQL 8's **Boost.Geometry**, MariaDB's, SQL Server's own implementation, Oracle's SDO_GEOM, plus NetTopologySuite's JTS port in our own process. **These disagree at the edges** — on validity, on precision, on what touches, on empty-geometry results — and the same query can return different answers on different providers. **This is a correctness problem, not a performance one, and ADR-008 §2's never-degrade-silently does not cover it**: refusing an unsupported operation is honest, quietly returning a different answer is not. Needs `experiments/geometry-oracle` and a documented conformance position per provider. | Council | ADR-003, ADR-008, Q-80, Q-81, `experiments/geometry-oracle` |
| Q-23 | Can a PROJ `PJ` transformation object be used from more than one thread, or is it thread-affine? Decides whether prepared transformations are shared or duplicated per thread — tile and render hot path, and it changes what L1 costs. | Council | ADR-010, [research/dependency-thread-safety.md](research/dependency-thread-safety.md) §4 |
| Q-24 | ~~GDAL 3.10 for read-only raster thread safety?~~ **Closed.** [ADR-009](adr/ADR-009-raster-engine.md) puts GDAL on job workers at registration time only, so raster thread safety is no longer a request-path concern at all. File-based *vector* providers still hit GDAL per request and RFC 101 does not help them, which is now tracked under ADR-008 rather than here. | — | closed 2026-08-12 |
| Q-24-old | ~~Do we require GDAL 3.10 or later for read-only raster thread safety?~~ **Largely moot after the vector-first decision** — GDAL leaves the request path for imagery. Retained only because file-based *vector* providers still hit GDAL per request, and RFC 101 is raster-only, so it does not help them. | Council | ADR-009, deployment |

## Open, not blocking

| # | Question | Owner | Notes |
|---|---|---|---|
| Q-09 | Should the platform be polyglot (§80.2)? | Council | Distinct from ADR-001. Most likely relevant to geoprocessing extensions. Must not be used to dodge the core language decision. |
| Q-11 | Where does the service catalog's identity model come from — who mints stable IDs, and how do renames work (§37)? | Council | Cheap to get right now, expensive later. |
| Q-12 | How is monitoring cardinality bounded at 1,000 services? | Council | **Elevated by [ADR-007](adr/ADR-007-service-runtime.md) §5.** With worker, process and connection counts flat in service count, cardinality and catalog scale become the *only* things that grow. At 10,000 services this is the binding constraint, not the runtime. |
| Q-13 | What is the upgrade and rollback story, including DB schema migrations (§80.35–37)? | Council | **Needs its own ADR before Phase 1** (fresh-challenger review G8). Named in six documents, designed in none. An upgrade must handle schema migration across every supported store, export/import format compatibility, **plan identity stability or every cache silently invalidates** (Q-45), service definition compatibility, the API version surface and its extensions, and rolling worker replacement. For a product whose user is an administrator, the first upgrade is when trust is earned or lost permanently. Originally forced early by ADR-002 §4.5 — the platform owns a schema from day one, so this can no longer be deferred. Also now covers the export/import format's compatibility guarantees. |
| Q-14 | Which CRS set must be supported at launch, and is on-the-fly reprojection a hot path or a convenience? | Council | Affects caching keys and tile pipeline design. |
| Q-48 | How are the platform store and the datastore backed up and restored **consistently**? They are two databases, possibly two engines, and restoring a Tuesday platform store against a Wednesday datastore produces services pointing at tables that do not match their definitions. Needs either a documented ordering with a quiescing step, or a version stamp shared across both so a mismatched restore is detected rather than silently wrong. Raised by F10. | Council | deployment, ADR-002, data-model |
| Q-59 | What is the role set, and what capability does each carry? At minimum: viewer, publisher (may create hosted content), GIS administrator (may register sources and publish from them), platform administrator. Are roles fixed or custom? ArcGIS separates licensed *user types* from *roles*; we have no licensing tiers, so we probably need only roles - but that should be decided rather than assumed. | Council | §42, security |
| Q-60 | Are **groups** needed in v1? Sharing scopes are private, group, organisation, public. Groups are the expensive one - membership, nesting, who may create them - and without them sharing collapses to private, organisation or public, which may be enough to start. | Council | §42, security |
| Q-61 | How are **per-user datastore quotas** enforced, and what happens at the limit? Without quotas one publisher fills the store. Interacts with the L3 cache budget (ADR-010) - they compete for the same disk - and with the failure mode when a publish is refused mid-upload. | Council | ADR-010, data-model, §49 |
| Q-62 | How do an owner-set sharing scope and an admin-set role compose when they disagree? The safe default is that the more restrictive wins, but safe and expected are not the same, and an owner who shares publicly and finds it invisible will report a bug. | Council | security §2.0 |
| Q-63 | Where does the routing decision live, given the supervisor is deliberately not on the data path? Front router process, `SO_REUSEPORT` with a shared warm map, or accept-and-bind-locally which abandons affinity. Constrained by the supervisor design: whatever routes must observe supervisor state, so a dead worker is detected before routing rather than after. Should be settled by `experiments/affinity-routing`. | Council | [runtime-supervisor.md](runtime-supervisor.md) §9, ADR-007 §4.4 |
| Q-64 | How is a memory leak distinguished from a legitimately growing cache, with no baseline? A high level is not a fault - a worker with many warm contexts is doing its job, and recycling it destroys the value it accumulated. Growth without corresponding load is the signal, and that needs history a fresh deployment lacks. Starting crude (hard ceiling plus rate check) and saying so. | Council | [runtime-supervisor.md](runtime-supervisor.md) §6, ADR-007 §4.7 |
| Q-65 | What is the worker adoption protocol after a supervisor restart, and how is it made idempotent? Workers must be discoverable, re-attachable without spawning duplicates, and any mid-flight drain or quiesce must have recorded state so the new supervisor can finish or abandon it deliberately. Most likely part to be got subtly wrong. | Council | [runtime-supervisor.md](runtime-supervisor.md) §8 |
| Q-55 | **TLS appears nowhere in the architecture** — not in security, deployment or air-gapped planning — yet it applies to our serving endpoint, data source connections, object storage, and the COG proxy's outbound fetches. Air-gapped means no ACME, so rotation is manual and must not require a restart. Found by [failure-scenarios.md](failure-scenarios.md) N8. | Council | security, deployment, ADR-009, Q-15 |
| Q-52 | Which file formats are **serving providers** and which are **import sources**? Proposed line: serving formats designed for range-request access (COG, and later GeoParquet and FlatGeobuf) are providers; interchange formats (Shapefile, KML, GPX, File Geodatabase, GeoJSON files) are imported into the datastore as a job. §27 currently lists interchange formats as providers, which is wrong. The GDAL rule in Q-28 gives a clean test: anything needing GDAL cannot be a serving provider. | Council | [research/honua-server.md](research/honua-server.md) §2a, §27 |
| Q-53 | Do we ever support data warehouses (Snowflake, Databricks, Redshift)? **Recommend no**, because every query costs money and the architecture has no cost dimension — caching, seeding and job retry all assume compute is roughly free, and §49 counts features, bytes and time but not currency. The prerequisite is a cost model, not a connector. | Council | [research/honua-server.md](research/honua-server.md) §5a, §49 |
| Q-30 | How is cross-node change notification done without `LISTEN`/`NOTIFY`? Polling a change-sequence column is the portable default. [ADR-010](adr/ADR-010-caching.md) §7 makes this concrete and uncomfortable: the poll interval bounds the invalidation delay, and for the "wrong" class of invalidation that window is a period during which incorrect data is served. **The number must be documented, not described as "eventual".** | Council | ADR-002 §4a.4, ADR-010 §7, §45 |
| Q-46 | Can a fast job return its result directly instead of a job identifier and a polling URL? Registration is a job for good reasons, but returning a polling URL for a two-second operation makes a worse first five minutes for every new user. [ADR-011](adr/ADR-011-job-system.md) §10 flags this as worth designing rather than accepting. | Council | ADR-011, admin API (§39) |
| Q-45 | What is the plan-identity stability contract? Cache keys derive from compiled plan identity, so a planner change silently invalidates every cached tile in a deployment. That has to be a deliberate, visible, released event rather than a surprise on upgrade. | Council | ADR-010 §8, ADR-008 |
| Q-41 | Do we offer an optional companion schema in a registered database where granted rights, to hold versioning and editor-tracking bookkeeping? **Important again now that editing is in scope.** It is how ArcGIS gives referenced data advanced capability, and it is the difference between concurrency that survives direct database writes and concurrency that does not. | Council | [research/arcgis-datastore-model.md](research/arcgis-datastore-model.md) §4, [ADR-005](adr/ADR-005-api-architecture.md) §3.8 |
| Q-19 | **Deferred, not rejected.** Does the platform need an in-process compute engine (DuckDB) for capability gaps? [ADR-008](adr/ADR-008-query-engine.md) §4.3 declines to adopt one before we can name the queries it would rescue. **The evidence that reopens it is a list of real refused queries users legitimately need.** Recorded as dissent in ADR-008 §12. | Council | ADR-008 |
| Q-44 | What is the write surface? OGC API Features Part 4 is still a **draft**. Options: implement the draft and track a moving spec, ship our own additive extension and converge later, or both. [ADR-005](adr/ADR-005-api-architecture.md) §3.7 recommends the second. **Amended 2026-08-12 by Q-17: there are now two write surfaces**, ours and ArcGIS `applyEdits`. Both must map onto one internal write model — a genuine test of whether ADR-005's protocol-neutral interface is neutral, and also double the surface to keep correct. | Council | ADR-005 |
| Q-43 | What is the schema-drift polling interval, and what does it cost against a large registered database with many layers? If too expensive, fall back to check-on-error plus TTL. | Council | [data-model.md](data-model.md) §3, A-023 |
| Q-39 | If a registered source happens to be writable, do we offer hosted-grade capability there automatically or only on explicit opt-in? Automatic is friendlier and surprises DBAs. Opt-in is safer and gets forgotten. | Council | [data-model.md](data-model.md) §5 |
| Q-33 | If a datastore exists, does hosting copy data or replicate it continuously? Copy is simple and goes stale; continuous replication is a synchronisation product nobody asked for. Copy with explicit refresh is the likely answer, but it must be chosen rather than defaulted into. | Council | publishing (§38) |
| Q-34 | Are pre-generalised geometry tables per zoom level a datastore-only feature, or attempted on registered sources where we happen to have write access? | Council | ADR-008, ADR-010 |
| Q-35 | Which runtime schema changes do we offer, per dialect? Needs DDL cost and locking behaviour verified on all three engines first. Classification proposed in [research/runtime-schema-evolution.md](research/runtime-schema-evolution.md) §5. | Council | publishing (§38), ADR-008 |
| Q-36 | Can the service definition describe fields the physical table does not have — computed, aliased, role-hidden? Cheap to allow now, expensive to retrofit. **First concrete requirement found 2026-08-12:** a registered read-only source with a wrong declared SRID cannot be corrected at source, so the definition must be able to override it ([geometry-crs-policy.md](geometry-crs-policy.md) §2). That makes this a yes rather than a maybe. | Council | data model (§37, §38) |
| Q-37 | What is the contract for a request in flight across a schema change? | Council | ADR-007, publishing (§38) |
| Q-68 | **Do we keep our own MVT encoder now that every tile source is PostGIS?** Q-67 removed the reason it existed. [build-vs-adopt-policy.md](build-vs-adopt-policy.md) makes the tiling pipeline Tier 1 — *written by us, always* — and `ST_AsMVT` beat our path 96.3 to 69.9 req/s under load, so there is no performance argument for ours either. **One argument survives and is measurable:** `ST_AsMVT` is one database round trip per tile, whereas reading once and encoding many tiles in process (a z12 read covers 16 z14 tiles) is only possible with our own encoder, and that is exactly what seeding does. Decide by measuring read-once-encode-many against 16 `ST_AsMVT` calls, not by arguing the policy. | Council | [build-vs-adopt-policy.md](build-vs-adopt-policy.md), ADR-008 §4.8, ADR-010 §6 |
| Q-72 | **What is the project's governance and maintenance story?** Created by Q-49's answer, and absent from §55 which covers licence obligations only. Giving a server to the world obliges a release process, a documented security-response path with a contact and an embargo policy, a contribution policy and CLA-or-not decision, an issue triage commitment, and a statement of what is *not* supported. A public server product with no security contact is a liability rather than a gift. | **[OWNER]** | §55, deployment, `SECURITY.md`, `CONTRIBUTING.md` |
| Q-71 | **What is the one-artefact story now?** Q-70 makes PostgreSQL a hard dependency, and Q-28 / A-016 established that the serving container ships no GDAL. So the baseline is two containers we ship — server and datastore — plus a job-worker image carrying GDAL. That is coherent, but nothing yet says how they are packaged, upgraded together, or version-matched, and the air-gapped checklist (Q-15) has to cover all three. | Council | deployment profiles (§53), Q-15, ADR-002 |
| Q-66 | **Does the provider interface hand back geometry objects on the tile path, or coordinates?** — **promoted 2026-08-12 from curiosity to the main line of work.** Run 3 measured ~139 bytes allocated per vertex read, roughly three to four copies of every coordinate (WKB bytes, `Coordinate` objects, clipped copy, transformed copy), and showed the resulting GC pause is what caps throughput. Opened by [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) finding 10: after two rounds of optimisation the geometry work we own is 21.5 ms of a 323 ms z12 request, and the dominant remaining cost is WKB parsing building an NTS geometry graph that exists only to be discarded one stage later. Reading WKB straight into tile space would remove it — at the cost of a second, coordinate-shaped provider return type and a tile path that no longer shares a representation with the feature path. That is A-006 under pressure. | Council | ADR-003 §5b, ADR-008, provider architecture (§27) |
| Q-58c | **Domains, subtypes and editor tracking** — the rest of the FeatureServer data model. Split out of Q-58 when [ADR-013](adr/ADR-013-feature-service-data-model.md) answered identity, relationships and attachments. Editor tracking interacts with A-027, since it records who changed what and we do not see writes that bypass us. | Council | [ADR-013](adr/ADR-013-feature-service-data-model.md), §28 |
| Q-15 | What does "air-gapped" concretely require — offline PROJ grids, GDAL driver data, font bundles, no telemetry? | Council | Currently a slogan in the master prompt; needs a concrete checklist. |

## Answered

| # | Question | Answer | Date | Recorded in |
|---|---|---|---|---|
| Q-70 | Is PostgreSQL now mandatory, and do we accept that? | **Yes, accepted.** Follows arithmetically from Q-69 + Q-32: the datastore is required and it is PostGIS-only. **This reverses Q-22 and reinstates A-009**, which was invalidated earlier the same day — recorded as a reversal rather than smoothed over, because the reason given for invalidating it (a PostgreSQL requirement is a barrier for ArcGIS-on-Oracle shops) was correct and has simply been outweighed. What outweighs it: Q-32 ships the datastore as a *managed appliance* we install, configure, back up and upgrade, so the ask is **run our container**, not **acquire a PostgreSQL DBA**. **What it deletes:** the SQLite platform-store dialect, one set of migrations, one job-claim implementation, and roughly half the platform-store CI matrix — the same class of cut as Q-51, which is notable because Q-51's own reasoning (*SQLite already solves the no-PostgreSQL requirement completely*) is what this voids. **What it costs, and it is real:** an organisation with a no-PostgreSQL policy cannot run this product at all. That was A-018's entire point and it is now accepted as a lost segment. **What it restores:** [CLAUDE.md](../CLAUDE.md) §6 has always stated the baseline deployment target as `gis-server → PostgreSQL/PostGIS`. The architecture and the stated baseline now agree for the first time. | 2026-08-12 | [product-context.md](product-context.md), [ADR-002](adr/ADR-002-primary-data-architecture.md) §4b |
| Q-69 | Is the datastore still optional? | **No. It is mandatory.** Every deployment runs it. Q-32 shipped it as an *optional* managed appliance; Q-67 then made it the only source of tiles, and the owner has now made it required outright rather than required-if-you-want-tiles. **What this simplifies:** there is one deployment shape instead of two, the datastore stops being a conditional branch through deployment profiles (§53), backup, upgrade and the air-gapped checklist, and the "datastore-only deployment" and "registered-only deployment" cases collapse into one baseline plus optional registered sources. **What it opens:** Q-70, which is not a detail — a mandatory PostGIS datastore means PostgreSQL is mandatory, and that reverses Q-22. | 2026-08-12 | [data-model.md](data-model.md), ADR-002, deployment profiles (§53) |
| Q-57 | How is a stable feature identity produced, given registered tables we do not control? | **Declared, not inferred.** Hosted layers get `globalid uuid` plus a monotonic `objectid` by construction. For registered layers the administrator **nominates** the identity column in the service definition (the Q-36 mechanism) — we do not synthesise from `row_number()`, which is not stable, and we do not maintain a side mapping table, which would be state about somebody else's table and would drift on their first edit. **The asymmetry that matters:** OGC API Features accepts a string `id`, ArcGIS FeatureServer requires a unique integer OID. A registered table keyed by UUID or text is fully servable natively and **not servable through the compatibility layer** without DDL. The compatibility layer has a stricter data requirement than the product it wraps. | 2026-08-13 | [ADR-013](adr/ADR-013-feature-service-data-model.md) §2 |
| Q-58a | Do we support related records, and how are relationships modelled? | **Yes, v1. Declared in our service definition, not reverse-engineered from Esri system tables.** Reading `GDB_ITEMS` / `GDB_ITEMRELATIONSHIPS` was rejected on three grounds: it is reverse-engineering Esri internals (§5), it only works when the source is a geodatabase, and it breaks silently whenever Esri changes the layout. Declaration is **strictly more capable**: it works on a plain PostGIS, Oracle or SQL Server schema with ordinary foreign keys, and an administrator can relate two tables that were never designed to be related. `queryRelatedRecords` becomes one `IN` query with pushdown rather than N+1. | 2026-08-13 | [ADR-013](adr/ADR-013-feature-service-data-model.md) §3 |
| Q-58b | Do we support attachments, and where do the bytes live? | **Yes, v1. In the database, Esri's model** — owner decision, chosen over object storage with proxied delivery. Transactional with the edit, backed up with the data, byte-compatible with a migrated `__ATTACH` table, and one storage tier rather than two. **Two hard conditions come with it.** Streaming is mandatory: run 3 measured 80.9% GC pause at 18% CPU on a 20 MB/request workload, and an attachment is arbitrarily large and user-chosen, so materialising one reproduces A-037's ceiling on demand. And database storage exposes the connection pool — a slow client holding a streaming connection is slowloris pointed at ADR-007 §4.8's budget, mitigated by a separate bounded pool (A-041). Dissent and a concrete revisit trigger recorded in [ADR-013](adr/ADR-013-feature-service-data-model.md) §9. | 2026-08-13 | [ADR-013](adr/ADR-013-feature-service-data-model.md) §4 |
| Q-82 | **Read-only or read/write for MySQL, MariaDB and DuckDB?** Q-50a made Oracle and SQL Server **full read/write**, deliberately and as the most expensive of three options. Extending that precedent to MySQL and MariaDB means two more transaction models, two more concurrency semantics and two more definitions of a conflict, all under A-027 — which is already the load-bearing assumption for editing. DuckDB is different again: it is single-writer by design, so read/write is not merely expensive but arguably wrong. **Recommendation: read-only for all three at first**, matching Honua, with MySQL promoted later if anyone asks. That breaks the *all providers are equal* symmetry, and the capability report (ADR-008 §2) is exactly the mechanism for saying so out loud. | **[OWNER]** | ADR-008, Q-50a, A-027 |
| Q-79 | **Do SensorThings and 3D Tiles belong in this product?** Fourteen of the sixteen protocols added by Q-78 are *faces* over engines already decided. **These two are not.** SensorThings v1.1 is a different domain model — Things, Datastreams, Observations, FeaturesOfInterest — an IoT observation store with its own storage shape, temporal indexing and write path, touching nothing in the current architecture. 3D Tiles and Terrain-RGB need 3D tiling and terrain mesh generation, with no foundation in either the vector tile pipeline or the raster engine. OGC API EDR straddles both. **Recorded for confirmation rather than assumed**, because they may have been swept in by pointing at a list rather than chosen individually — which is precisely the failure Q-17a taught, where four services travelled under one justification that was false for two of them. | **[OWNER]** | [protocol-surface.md](protocol-surface.md) §4 |
| Q-74 | **What is the Python SDK, and how does data cross the boundary?** Gates Q-17b. Three shapes: give Python a database connection (bypasses our authz, capability model and connection budget — rejected on sight); have the SDK call our own API (respects authz, works remotely, slow for bulk); or materialise the input to a file the tool reads and collect its output the same way (fast, no connection held, no authz bypass because we did the reading). **The third points somewhere specific:** the interchange format wants to be one Python's geospatial stack reads natively, which means **GeoParquet or Arrow** — `geopandas`, `pyarrow` and `shapely` all read them without a shim. **This finally gives GeoParquet a concrete problem to solve.** It was an early owner interest with no justification in the architecture, and Q-53 declined warehouse providers, leaving it homeless. It is not a provider — it is the boundary between our runtime and theirs. | Council | Q-17b, ADR-011, §82 |
| Q-75 | **How is user-supplied Python sandboxed?** Gates Q-17b, and it is the largest security surface in the product by a wide margin — arbitrary code execution by design, against a server holding the organisation's spatial data. Out-of-process is settled by ADR-006's own reasoning (job workers provide isolation) and by the fact that a segfault in a C extension must not take a worker with it. What is not settled: process user and container boundary, CPU / memory / wall-clock / disk limits, whether tools get network access at all, and **who may publish a tool** — the publisher role from the self-service model, or administrators only. My instinct is administrators only at first; a publisher uploading data is a different risk from a publisher uploading code. | **[OWNER]** for the publish-rights half | Q-17b, [security.md](security.md), ADR-006, ADR-007 |
| Q-76 | **Can Python tools install dependencies, and what does that do to air-gapped installs?** Gates Q-17b. If tools may `pip install`, we have a supply-chain surface and Q-15's offline checklist breaks — an air-gapped site cannot reach PyPI. If they may not, the shipped environment must carry a curated wheel set, which we then own, version and patch. There is no third option that is both convenient and offline. | Council | Q-15, Q-17b, deployment (§53) |
| Q-77 | **Does the raster rendering engine get built, and where is the Tier 1 line?** Gates the expensive half of Q-17c. `exportImage`, raster functions and dynamic mosaicking all need server-side raster rendering — a separate problem from ADR-004's vector rendering, not the same engine. GDAL already does much of the mechanical work (warp, VRT-as-mosaic, overviews) and lives in the job worker under A-016, which makes it Tier 2 raster I/O. But applying a stretch or a colour ramp to pixels is **cartographic logic, which [build-vs-adopt-policy.md](build-vs-adopt-policy.md) puts in Tier 1**. The line probably falls between *assembling and warping pixels* (adopted) and *deciding what colour they are* (ours), and that needs stating before either is built. | Council | [ADR-009](adr/ADR-009-raster-engine.md), [ADR-004](adr/ADR-004-rendering-engine.md), build-vs-adopt |
| Q-73 | What licence does the project ship under? | **Apache-2.0.** Chosen over MIT for the express patent grant, which MIT lacks and which matters for infrastructure: contributors and users are protected from patent assertions by contributors. Chosen over AGPL because the owner said unrestricted, and AGPL is a restriction — a real one that some enterprises ban outright. **The consequence, accepted:** anyone may fork this, close it, and sell it as a managed service. For a project whose stated purpose is *give it to the world*, that is the purpose rather than a leak. `LICENSE` and `NOTICE` added 2026-08-13; the repository was public and legally all-rights-reserved until then. Note this is the **outbound** licence — the earlier *copyleft acceptable* decision was about inbound dependencies and is unaffected. | 2026-08-13 | `LICENSE`, `NOTICE`, [DEPENDENCY-LICENSES.md](../DEPENDENCY-LICENSES.md) |
| Q-49 | What does this do that GeoServer cannot, for whom, and why is that worth the migration cost? | **Answered by the owner, 2026-08-13, and the question turned out to be two questions.** *Why does it exist:* **"I will give this to the world."** Open source, public, unrestricted. That is sufficient and complete — QGIS, GeoServer and PostGIS all began the same way, and a gift needs no market case. **It also dissolves the §81 exit criterion** that required testing Q-49 with real GIS teams: that criterion assumed a commercial-style justification the project does not need. *What makes someone switch:* **not** "better capabilities than GeoServer", which is currently false and getting more so — vector-first, Q-47, Q-67, Q-70 and Q-28 each removed a capability GeoServer has, and it also has WCS, WPS, CSW, SLD and a plugin ecosystem we will not build. The true and testable version is **everything an ArcGIS Server shop needs in order to leave, which GeoServer does not provide**: full ArcGIS FeatureServer compatibility including `applyEdits` (Q-17), free migration tooling (Q-16), a real service runtime with affinity routing, never-degrade-silently capability reports, and self-service publishing. Narrow, defensible, and it prioritises the roadmap where "better than GeoServer" does not. Opens Q-72 and Q-73. | 2026-08-13 | [competitive-position.md](competitive-position.md) §6, [product-context.md](product-context.md) |
| Q-67 | Can vector tiles be served from registered databases? | **No. Tiles come only from hosted data, strictly — data that lives in the datastore as system of record.** Registered Oracle, SQL Server and foreign PostGIS layers get feature services and full ArcGIS FeatureServer compatibility, and never tiles. Owner decision, taken against the measured alternative rather than in advance of it: [benchmarks/mvt-generation/RESULTS.md](../benchmarks/mvt-generation/RESULTS.md) run 3 showed the registered-source tile path at 28.3 req/s and 80.9% GC pause against 96.3 req/s and 0.3% for a PostGIS-native path. **What it resolves:** A-039 and the tile half of A-021 dissolve, [D-05](architecture-debt.md) shrinks from the most consequential gap in the evidence to a feature-query question, three dialect tile implementations disappear, and A-037's GC ceiling stops being a tile problem. **What it costs:** an organisation on Oracle must copy data to get tiles, not merely point at it — accepted deliberately. **What it aligns:** the two user types now map cleanly onto the two paths. Publishers host and get tiles; GIS administrators register from desktop and get features. **What it opens:** Q-68 and Q-69. | 2026-08-12 | [product-context.md](product-context.md), ADR-008 §4.8, [data-model.md](data-model.md) |
| Q-06a | Who is the primary user? | **The GIS administrator.** Promotes the admin API, service lifecycle observability, RBAC and good defaults to first-class requirements. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-06b | What is the day-one workload? | **Features first, then vector tiles.** Confirms the §71–§73 sequencing and the walking-skeleton target. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-25 | Do we support the MapLibre GL Style Spec as a style format? | **Effectively yes.** The vector-first decision makes the client the renderer, and MapLibre style is the format it speaks. Styles are stored and served, not evaluated. Formal confirmation belongs in ADR-004. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-26 | How is cross-tile label consistency achieved? | **Closed, not answered.** Labels are placed client-side. This is no longer a problem the platform has. | 2026-08-12 | [product-context.md](product-context.md) |
| Q-31 | Does the feature service expose provider capability differences, or hide them? | **Expose them.** A capability report is published per layer, and unsupported operations are refused with an explanation rather than answered slowly by dragging data back. The organising principle is *never degrade silently*. Conditional on the capability report shipping with the first refusal. | 2026-08-12 (an earlier answer on 2026-08-12 was withdrawn as a misreading of the ArcGIS model) | [ADR-008](adr/ADR-008-query-engine.md) §2 |
| Q-02 | What is the modern equivalent of ArcSOC? | **Workers are sized to the machine, not to the catalogue.** A small number of multi-tenant request workers, threaded, with service contexts bound lazily and evicted LRU under a bounded per-worker budget; separate isolated job workers for geoprocessing, GDAL registration and plugins. Isolation on the workload axis, not the service axis. Services do not start, so there is no thundering herd. `ACCEPTED WITH CONDITIONS`. | 2026-08-12 | [ADR-007](adr/ADR-007-service-runtime.md) |
| Q-27 | How is per-layer authorization enforced when imagery is delivered as COG? | **Proxy by default; signed URLs as an optional optimisation.** The earlier framing overstated the cost — COG range requests are view-proportional, a few hundred kilobytes for what is on screen, so proxying is comparable to serving tiles. The proxy also works on every backend including a plain filesystem, gives us a place for rate limits and audit logging, and its responses are cacheable. Signed URLs work only where the store supports signing and cache nowhere we control. | 2026-08-12 | [ADR-009](adr/ADR-009-raster-engine.md) §2.4 |
| Q-10 | Do we ship a third-party plugin system, or only internal extension points? | **Internal extension points only, for now.** No SDK, no public contract, no versioning for external consumers. The trigger that reopens it is specific: someone needing a provider, format or operation we have decided not to build. If it reopens, geoprocessing comes first because job workers already provide the isolation, and data providers come last because they sit in the request path with no cheap isolation story. | 2026-08-12 | [ADR-006](adr/ADR-006-plugin-model.md) |
| Q-38 | Can a layer move between registered and hosted, keeping its identity? | **No.** Editing is done against the database directly, with QGIS. | 2026-08-12 | [data-model.md](data-model.md) |
| Q-40 | Do we accept data uploads / hosting at all? | **Yes.** The datastore is a first-class store and an organisation may run entirely hosted. It may also have no GIS database of its own, so datastore-only is a supported deployment. | 2026-08-12 | [data-model.md](data-model.md) §4 |
| Q-28 | Can GDAL-backed providers be made optional, so a PostGIS-only deployment is one artefact? | **Yes, by construction, if we adopt the rule that the serving container ships no GDAL** — it lives only in the job worker image. Observed in Honua Server and cleaner than our own ADR-009 §2.2 placement, because it is a rule about the artefact rather than about where code runs. Restores weight to ADR-001's C7, shrinks the attack surface, and shrinks the air-gapped checklist. | 2026-08-12 | [research/honua-server.md](research/honua-server.md) §2b |
| Q-51 | Do SQL Server and Oracle need to be platform stores? | **No.** Cut. SQLite already meets the no-PostgreSQL requirement, being embedded and holding a few thousand non-spatial rows; the remaining justification was an unevidenced policy requirement. They stay first-class **providers and datastores**, where the requirement is real. Removes two dialect implementations, two migration paths, two job-claim implementations and half the CI matrix. | 2026-08-12 | [ADR-002](adr/ADR-002-primary-data-architecture.md) §4a, [reviews/fresh-challenger-review-2.md](reviews/fresh-challenger-review-2.md) G6 |
| Q-29 | Which platform stores ship in v1? | **Both, because there are only two.** Dissolved by Q-51. | 2026-08-12 | [ADR-002](adr/ADR-002-primary-data-architecture.md) §4a |
| Q-47 | Is WMS in the compatibility layer in scope for v1? | **No.** ADR-004 stays deferred and the F7 contradiction is resolved by removing the requirement rather than by un-deferring the ADR. WMS-client migration is unsupported at v1 and must be documented as such. **Strengthened 2026-08-13:** the owner rejects WMS **on its merits** — *"I hate WMS. Super slow."* — rather than merely cutting it for scope, and prefers a REST-style rendered map service in the manner of an ArcGIS MapService, with a symbology model better than SLD. Re-confirmed on being asked directly that this is a stated direction and **does not change v1 scope**; ADR-004 stays `DEFERRED`. Recorded in ADR-004 §0 so that a future revisit does not default to *add WMS*. | 2026-08-12 | [ADR-004](adr/ADR-004-rendering-engine.md), [product-context.md](product-context.md) |
| Q-32 | Is the datastore in v1, and on which engines? | **v1, PostGIS only, optional.** Shipped as a managed component we install, configure, back up and upgrade - the ArcGIS Data Store model - so the customer never operates it. **A-022 retired.** The three-engine datastore was an over-application of the no-mandatory-PostgreSQL decision: that objection was about the operational burden of running a database, and a managed appliance removes most of it. An organisation that still refuses runs registered-only and loads data into their own database with QGIS, which is the workflow already endorsed for editing. | 2026-08-12 | [data-model.md](data-model.md) |
| Q-54 | Does the runtime supervisor (§21) have a design? | **Yes** - [runtime-supervisor.md](runtime-supervisor.md). Per-node supervisor process, tiny, serving no requests, with the platform keeping it alive and workers surviving its death for re-adoption. Not the distinguished node §4.10 forbids, because ArcSOC's SOM was site-wide and this is local. Governing principle: a management-plane failure must not become a data-plane failure. The hard parts - memory-growth detection, adoption protocol, routing placement - are named as Q-63 to Q-65 rather than assumed. | 2026-08-12 | [runtime-supervisor.md](runtime-supervisor.md), ADR-007 §4.13 |
| Q-56 | What is the policy for a single geometry too large to serve? | **Three tiers rather than one limit.** Over the limit because of many features: paginate. Over the limit because of one feature: return that feature alone with a warning header, since the limit protects the server and a single feature the user asked for is not an attack. One feature over the absolute cap: refuse *that feature* by id, not the query. Refusing the whole query because one feature in a layer is large is the wrong granularity. | 2026-08-12 | [geometry-crs-policy.md](geometry-crs-policy.md) §6 |
| Q-08 | Does the platform own its data, or serve data registered elsewhere? | **Neither is the default. Both are first class, chosen per layer by a lifecycle test** taken from Esri's guidance: *does this layer's data have a life outside the service?* If yes, register it. If the service essentially **is** the data, host it - and accept that we then own its lifecycle including deletion. Recorded with the honest caveat that the two modes are not symmetric in difficulty: everything hard is on the registered side. | 2026-08-12 | [data-model.md](data-model.md) |
| Q-17 | Do we serve an ArcGIS-compatible REST surface? | **Yes - full FeatureServer including edits.** Query plus `applyEdits`, `addFeatures`, `updateFeatures`, `deleteFeatures`. The backend exists because Q-50a gave us write capability on all three engines, and it is what makes a migration complete rather than stalling at the last mile: definition import moves the server, this keeps the *clients* working. ~~**Not** MapServer, ImageServer, GeometryServer or GPServer - those produce rendered images, which vector-first removed and Q-47 kept out of v1.~~ Raises Q-57 and Q-58. **AMENDED 2026-08-13 — the exclusion list was wrong, and wrong in a way that matters.** Two of those four do not produce rendered images. GeometryServer returns geometry as JSON; GPServer returns whatever tool was published to it. Four services were excluded under one justification that applies to two of them. Corrected per-service: **GeometryServer is now IN scope** (§below), **GPServer stays out for a different and better reason**, ImageServer and MapServer stay out on the original grounds. | 2026-08-12, amended 2026-08-13 | ADR-005 3.3, compatibility layer |
| Q-52 | Which formats are serving providers and which are import sources? | **Answered 2026-08-13 by Q-83, which put the whole list in scope and forced the enumeration this question had been asking for since it was opened.** **Import sources** (converted at registration, never served from the file): Shapefile, GeoPackage, **File Geodatabase**, KML, GPX, WKT, GeoJSON, FlatGeobuf, GeoParquet. **Output formats** (serialised from a query result): GeoJSON, MVT, FlatGeobuf, GeoParquet, GeoArrow. **Serving providers** remain databases only — PostGIS, SQL Server, Oracle, MySQL, MariaDB — plus DuckDB as the file-query engine (Q-81), which is what lets a file be queried without becoming a provider. **The split §27 got wrong is now explicit: a format is not a provider.** Two consequences: **A-038 is confirmed** — File Geodatabase has no managed .NET reader, so GDAL is not avoidable and A-016's job-worker placement stands; and **GeoParquet now has three independent justifications** (Q-74's Python SDK boundary, Q-81's DuckDB engine, and this list), having had none a week ago. | 2026-08-13 | ADR-008, [A-038](architecture-assumptions.md), Q-74, Q-81 |
| Q-83 | Is everything still marked *Open* on the capability matrix in scope? | **Yes, owner decision 2026-08-13.** The honest reading matters more than the decision: **most of these are not new scope, they are known gaps being confirmed.** Auth (API key, OIDC, SAML, SCIM), TLS, OpenTelemetry, health probes and OpenAPI were already recorded as not-started or severe gaps — several are already blockers B3 and B4 in the [exit plan](phase-0-exit-plan.md). Protocol conformance suites were flagged as untenable to omit once Q-78 claimed 29 faces. Cron scheduling is small. A versioned release is ours to cut. **Two things are genuinely new scope: the format lists (Q-52, answered below) and geocoding (Q-84).** | 2026-08-13 | [protocol-surface.md](protocol-surface.md), [phase-0-exit-plan.md](phase-0-exit-plan.md) |
| Q-84 | Do we build a geocoder (ArcGIS GeocodeServer)? | **In scope by Q-83, and it is a new subsystem rather than an endpoint — flagged alongside Q-79's two.** Geocoding needs address parsing and normalisation, reference data, candidate matching with scoring, reverse geocoding and batch operation. **Address parsing is locale-specific**, and a product built in Turkey serving Turkish addresses cannot reuse a US-centric parser — that is a feature, since most open geocoders are Anglocentric, but it is also real work. **The sharper question is reference data, and it is the same shape as GPServer's toolbox:** a geocoder without street centrelines or address points is an empty shell. Three options — build the engine and require the customer to bring their own reference data (Esri's address-locator model, and the only one that works air-gapped); integrate an external service such as Nominatim or Pelias (fails Q-15 offline and adds a dependency we do not control); or ship reference data ourselves (a licensing and freshness commitment nobody wants). **Recommendation: the first**, which also matches how an ArcGIS shop already works. | Council, with the reference-data choice **[OWNER]** | Q-83, Q-15, ADR-006 |
| Q-80 | Are MySQL and MariaDB providers? | **Yes, owner decision 2026-08-13 — and as two providers, not one.** They were listed as a single row on the capability matrix and that is the Q-17a bundling error again. **MySQL 8 and MariaDB are not the same engine for spatial purposes.** MySQL 8 has a reasonably complete OGC function set backed by **Boost.Geometry**, SRID-aware columns and R-tree indexes on InnoDB. MariaDB's spatial support diverged and sits closer to MySQL 5.7 in coverage. They need separate capability declarations and separate conformance runs, or one of them will silently claim the other's abilities. **Also inherited: MySQL 8 follows the SRS definition's axis order**, so EPSG:4326 is latitude-first — a well-known footgun and a direct concern for [geometry-crs-policy.md](geometry-crs-policy.md). Write scope is Q-82. | 2026-08-13 | ADR-008, [geometry-crs-policy.md](geometry-crs-policy.md) |
| Q-81 | Is DuckDB a provider? | **In scope, but not as a registered-source provider — as the file-format query engine.** Q-53's objection to warehouses was that they are analytical stores, not serving stores, and DuckDB is an embedded OLAP engine with a single-writer model: pointing concurrent tile and feature requests at it is the same mistake in a smaller package. **Where it genuinely fits is where Honua puts it — embedded and read-only** — and it fits even better than their framing suggests, because it reads GeoParquet, FlatGeobuf, Shapefile and GeoJSON natively and speaks Arrow. That is the same boundary **Q-74** just chose for the Python SDK, and it is a candidate answer to **Q-52**'s never-enumerated import format list and to **A-038**'s GDAL question. One engine for *files*, rather than a reader per format. **The cost is Q-20, which named this exact risk before DuckDB was in scope.** | 2026-08-13 | ADR-008, Q-52, Q-74, [A-038](architecture-assumptions.md) |
| Q-78 | Do we pursue full protocol parity with Honua? | **Yes, owner decision 2026-08-13** — sixteen further protocols in scope: WCS 2.0.1, WPS 2.0, OGC API Maps / Tiles / Coverages / Processes / Records / Styles / EDR, SensorThings v1.1, OData v4, STAC API, 3D Tiles, Terrain-RGB, PMTiles, gRPC and MCP. **The count is the wrong unit and [protocol-surface.md](protocol-surface.md) says why.** Twenty-nine protocol faces sit over **ten engines**, eight of which are already decided or in flight. Five of the sixteen are near-free standard surfaces over things already built (Tiles, Styles, PMTiles, Records, MCP); five are real work on chosen foundations (OData, gRPC, STAC, Processes, WPS); three are gated on the reopened raster engine or the deferred renderer; and **two are new products rather than new endpoints** — Q-79. **The valuable side effect:** six faces over the feature engine is the hardest available test of [ADR-005](adr/ADR-005-api-architecture.md)'s protocol-neutral internal interface, which was asserted and never proven. If it is real, faces five and six are cheap. If it leaks, this finds out, and finding out is worth more than the protocols. | 2026-08-13 | [protocol-surface.md](protocol-surface.md) |
| Q-17b | GPServer — in scope, and Python-based? | **Yes to both, owner decision 2026-08-13, reversing the answer given hours earlier in Q-17a.** GPServer was declined because *we have a job system with no toolbox*. **The toolbox is not ours to write — users write it, in Python.** That is the ArcPy and PyQGIS model and it turns GPServer from an empty shell into an extension point. **It reopens [ADR-006](adr/ADR-006-plugin-model.md) by the exact route ADR-006 predicted**: its stated trigger was *someone needing an operation we decided not to build*, and it named geoprocessing as the first candidate precisely because job workers already provide isolation. **Sequenced behind the Python SDK** — owner's condition, and correct: a GPServer without a defined SDK is a protocol with nothing behind it. Opens Q-74, Q-75, Q-76. | 2026-08-13 | ADR-005 §3.3a, [ADR-006](adr/ADR-006-plugin-model.md), [ADR-011](adr/ADR-011-job-system.md) |
| Q-17c | ImageServer — in scope? | **Yes, owner decision 2026-08-13**, reversing Q-17a and **reopening [ADR-009](adr/ADR-009-raster-engine.md)**, whose central decision was *serve COG and let the client render*. **Decided per operation, applying Q-17a's own lesson rather than repeating its mistake** — ImageServer is not one capability, it is roughly three at very different costs. **Near-free** (a COG range read or a GDAL call we already make): `identify`, `getSamples`, `computeHistograms`, `computeStatisticsHistograms`, `download`, and mosaic footprint `query`, which is a feature query over the catalogue and needs nothing new. **Medium**: tiled access to already-processed imagery. **Expensive, and the real decision**: `exportImage`, raster function chains, and dynamic mosaicking — these need a raster rendering engine, a processing-chain evaluator, and a mosaic dataset model with footprints, overviews, priorities and seamlines. That last group is plausibly the largest single capability in the whole matrix, larger than the vector renderer. Sequencing and the Tier 1/Tier 2 line are Q-77. | 2026-08-13 | [ADR-009](adr/ADR-009-raster-engine.md), ADR-005 §3.3a |
| Q-17a | Which ArcGIS service types beyond FeatureServer do we serve? | **Decided per-service 2026-08-13**, replacing Q-17's blanket exclusion. **GeometryServer — IN.** It is a REST wrapper over capability [ADR-003](adr/ADR-003-geometry-engine.md) already commits us to holding in-process: `project` and `findTransformations` are PROJ, which we need regardless; `buffer`, `intersect`, `difference`, `union`, `convexHull`, `cut`, `densify`, `generalize`, `areasAndLengths`, `distance`, `relation` and `labelPoints` are all NetTopologySuite, already in-runtime. Near-zero marginal cost. **And it serves Q-17's own purpose**: ArcGIS Enterprise portals configure a geometry service URL that client applications call, so omitting it partially defeats *existing clients keep working* — `VERIFY`, since ArcGIS JS API 4.x performs much of this client-side via `geometryEngine`, while Pro, Web AppBuilder apps and portal configuration still reference one. **GPServer — OUT, for a better reason.** It is a container for published geoprocessing tools and we have a job system with no toolbox; exposing it would be an empty shell. This is precisely [ADR-006](adr/ADR-006-plugin-model.md)'s stated trigger for reopening the plugin model — someone needing an operation we decided not to build. **ImageServer — OUT**, tied to [ADR-009](adr/ADR-009-raster-engine.md): it needs server-side raster rendering and dynamic mosaicking, which vector-first removed. **MapServer — OUT of v1**, tied to [ADR-004](adr/ADR-004-rendering-engine.md) §0, which the owner wants eventually and deferred deliberately. | 2026-08-13 | ADR-005 §3.3, [ADR-003](adr/ADR-003-geometry-engine.md) |
| Q-16 | Is migration tooling in scope, or API compatibility only? | **Inventory plus definition import, and free.** Two separable steps, a decomposition taken from Honua: (1) scan an existing GeoServer or ArcGIS Server and report what exists, what we can bring across and what we cannot — cheap, high trust, and useful *before* anyone commits; (2) import service and layer definitions. Data stays where it is, since Q-50a made registered sources fully capable. Styles convert where they can and report where they cannot. **Free deliberately**, against a competitor who charges for the equivalent — see Q-49. | 2026-08-12 | [product-context.md](product-context.md), ADR-005 |
| Q-50 | How do we serve Oracle and SQL Server data? | **(a) Full read/write providers.** Chosen deliberately over read-only providers and over migration tooling. Registered Oracle and SQL Server layers are editable through our API with the same capability as PostGIS, subject to granted rights. **This is the most capable and most expensive of the three options**, and it was the existing design by default rather than by choice — now it is chosen. What it commits us to: three transaction semantics with three isolation models and three definitions of a conflict; A-027 stays load-bearing; Q-41 becomes likely rather than optional; and ADR-008's write-path gap must be filled before implementation. | 2026-08-12 | [ADR-008](adr/ADR-008-query-engine.md), [product-context.md](product-context.md) |
| Q-42 | Is feature editing through our API in scope? | **Yes.** An earlier answer of "no" was a wrong inference: the owner's statement about editing happening in the database concerned **table structure**, not feature data. Schema changes on registered layers happen at the source; feature data editing goes through our API. §28's CRUD, batch editing, editor tracking and optimistic concurrency are all in scope. | 2026-08-12 (corrected same day) | [data-model.md](data-model.md) §5, [ADR-005](adr/ADR-005-api-architecture.md) §3.8 |
| Q-33 | Does hosting copy once or track the source? | **Dissolved.** Hosted data is the system of record, not a copy of anything. Derived artefacts are openly derived and have a refresh policy. | 2026-08-12 | [data-model.md](data-model.md) §1 |
| Q-21 | Does the Query AST target more than one SQL dialect from day one? | **Yes, necessarily.** Oracle Spatial and SQL Server Spatial are first-class alongside PostGIS. Capability negotiation is core, not a refinement. | 2026-08-12 | [ADR-008](adr/ADR-008-query-engine.md), [research/multi-database-consequences.md](research/multi-database-consequences.md) |
| Q-22 | Is a PostgreSQL-free deployment profile a goal? | ~~**Yes.**~~ **REVERSED 2026-08-12 by Q-70.** No: a mandatory PostGIS datastore makes PostgreSQL a hard dependency of every deployment. The original answer and its reasoning are kept rather than deleted — the reasoning was sound and was outweighed, not refuted. Original text: *Yes. PostgreSQL is not mandatory. Reframed into Q-29 (which stores ship when). A-009 invalidated.* | 2026-08-12 | [product-context.md](product-context.md), [ADR-002](adr/ADR-002-primary-data-architecture.md) §4a |
| Q-05 | Does platform metadata live in PostgreSQL, in files, or both? | **A relational store is the source of truth; files are an export/import format.** Amended same day: the store is portable across SQLite, PostgreSQL, SQL Server and Oracle. The platform database is logically distinct from data source databases, which may be foreign and read-only. `ACCEPTED WITH CONDITIONS` — see the conditions in ADR-002 §9. | 2026-08-12 | [ADR-002](adr/ADR-002-primary-data-architecture.md) |
| Q-07 | Is displacing existing ArcGIS Server / GeoServer deployments a goal? | **Yes.** The compatibility layer (§51) is therefore a product requirement, not an option — still strictly outside the core domain, still strictly clean room. Raises Q-16 and Q-17. | 2026-08-12 | [product-context.md](product-context.md) |
