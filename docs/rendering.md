# Rendering

**Status:** STUB — not written
**Required by:** §34

---

Server-side cartography: symbology model, style evaluation, label placement and
decluttering, font handling, layer compositing, and the rendering cache.

Also covers the vector tile pipeline (§33) where it shares machinery with
rendering — tile addressing, MVT encoding, generalisation by zoom, and tile
invalidation.

Expands [ADR-004](adr/ADR-004-rendering-engine.md).
