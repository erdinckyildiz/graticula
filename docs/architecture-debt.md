# Architecture Debt Register

Temporary architecture must not silently become permanent (§62).

Every entry records what was compromised, why it was acceptable at the time, and
**the observable condition that makes it unacceptable**. An entry without a
trigger is not debt — it is an undocumented permanent decision wearing a
disguise.

---

| ID | Debt | Taken on | Why it was acceptable | Trigger to repay | Cost if unpaid | Status |
|---|---|---|---|---|---|---|
| D-06 | **Dependency licensing is deliberately unexamined.** ~~Every row in [DEPENDENCY-LICENSES.md](../DEPENDENCY-LICENSES.md) is `UNVERIFIED`~~ **Narrowed 2026-08-15 by the §66 licensing gate:** all six direct dependencies are now verified from their own nuspecs and the gate passes. What remains unexamined is **transitive** dependencies. | 2026-08-12 | Verifying licences by hand was deferred while the dependency set was still moving. It stopped moving: six packages, four of them test-only. | Before any distribution, and before adding a dependency with a copyleft parent | A transitive GPL component linked into a shipped assembly would make the Apache-2.0 outbound licence (Q-73) unlawful to grant. The direct set is clean and the graph beneath it has never been enumerated — `dotnet list package --include-transitive` is the missing step. | **PARTLY RESOLVED.** Direct: verified, passing. Transitive: open. |
| D-05 | ~~A-019 is validated only on PostGIS.~~ **Rewritten 2026-08-12 by Q-67 and now much smaller: no engine except PostGIS serves tiles, so the tile half of this debt is gone. What remains is that the *feature* path — the day-one workload — has never been measured on SQL Server or Oracle at all.** | 2026-08-12 | Neither engine is installed on the development machine and the owner has declined to install them. Deferring is the honest option; the alternative was to leave a benchmark permanently listed as "next" and never run. The PostGIS result is still informative — it bounds our own CPU and allocation cost, which is engine-independent — but it says nothing about the two engines' WKB output, driver materialisation cost, or spatial index selectivity. | **Before any commitment is made that Oracle or SQL Server layers serve tiles at parity.** Concretely: before ADR-008 §4.8's tile path is called decided, or before the multi-database claim appears in user-facing material. **Repayment path found 2026-08-12**, and it stands on the library rather than on where we noticed it: `Testcontainers.MsSql` and `Testcontainers.Oracle` are public packages that start both engines as throwaway containers from the test run itself. **Confirmed 2026-08-16** that a peer of our scale runs exactly this — both packages at 4.11.0 in their dependency manifest, alongside MySQL, PostgreSQL and Redis ([reference-reading-log.md](research/reference-reading-log.md)) — which makes it a used route rather than a plausible one. Nothing is installed and nothing persists. That removes the stated obstacle — what remains is only machine headroom, and the Oracle image is the heavy one. | Reduced by Q-67 but not eliminated. Registered Oracle and SQL Server layers still serve features, still return WKB into an NTS graph, and A-037's allocation ceiling was measured on exactly that machinery. If the feature path has the same ceiling on those engines — and there is no reason yet to think it does not — then the multi-database promise has an unmeasured limit on the workload the product leads with. | **OPEN, narrowed 2026-08-12** — tile measurement no longer required; feature-path measurement now required |
| D-01 | ~~RLS delegation and per-data-source pooling are incompatible.~~ | 2026-08-12 | Found by fresh-challenger review G5 | — | — | **RESOLVED same day** — [security.md](security.md) §2. Our authorization is the baseline and was always going to exist; RLS delegation becomes an opt-in provider capability using transaction-scoped identity switching, so pools do not fragment. |
| D-02 | ~~Tenant identity is not part of the cache key.~~ | 2026-08-12 | Found by G5 | — | — | **RESOLVED same day** — [security.md](security.md) §3. Authorization splits into pre-lookup (uniform) and in-key (varies), and the key carries a grant fingerprint rather than a principal, so sharing survives where it is safe. |
| D-03 | Capability reports and detailed refusals disclose provider type and internal topology to any client that can reach a layer. | 2026-08-12 | Designed for usability without an authorization dimension. Found by G7. | Before the capability report ships | Reconnaissance surface; reveals an organisation's internal database topology | **Rule stated** in [security.md](security.md) §5 — detail is authorization-scoped. Open until implemented. |
| D-04 | **Multi-tenant resource isolation is not designed.** One tenant's expensive query degrades another's in a shared request worker; §49's limits are per service, not per tenant. | 2026-08-12 | Raised by G5, not addressed by the D-01/D-02 resolution | Before multi-tenant deployments are supported | A noisy tenant becomes an availability problem for everyone on the worker | OPEN |
| D-07 | **A streaming response that fails after its first byte cannot report the failure.** The query endpoint pulls the first row before writing anything, so every failure the error classifier knows about lands while the response is still empty. A failure *after* that — the database dying mid-result — leaves a truncated JSON document, and the server aborts the connection because there is nothing better available. | 2026-08-13 | Found by running the endpoint: a forced statement timeout produced HTTP 504 whose body was a partially-written response object with an error appended into the middle of it. `Response.Clear()` resets status and headers and does **not** discard bytes already handed to the `PipeWriter`. | Before a client is expected to distinguish a truncated page from a short one | A client reading a truncated result sees fewer features and no error. For a paging client that is silent data loss, which is the failure mode this project has ruled worst. | OPEN. Correct fix is a protocol decision, not a code change — Q-91. |
| D-08 | **The statement timeout is 30 seconds because a number was needed.** Applied server-side as `statement_timeout` per pool, and not overridden when an operator has set `Options` themselves. | 2026-08-13 | Q-04 wants a measured value and has none. The default was previously *no timeout at all*, which surfaced as an unexplained HTTP 500. | When Q-04 is measured | Too low truncates legitimate large extents; too high lets one query hold a pooled connection while every layer sharing that data source waits. | OPEN — the guess is at least visible and in one place now. |
| D-09 | ~~**Publishing a layer means hand-written SQL.**~~ **RESOLVED 2026-08-14.** `POST /admin/datasources` seals the credential inside the server with the key it already holds, and `POST /admin/layers` publishes over it. The sealing step that produced a wrong port and an authentication failure days later no longer exists as a human task. Registration probes before it writes, so a source we cannot reach leaves no row behind. Original: **Publishing a layer means hand-written SQL.** *(2026-08-13: no longer blocked — ADR-018 gives ADR-017's endpoints permissions to require. The friction is unchanged; the reason for not fixing it has gone.)* There is no admin API, so registering a data source requires sealing a connection string with the server's AES key outside the server and inserting rows by hand. | 2026-08-13 | ADR-017 is designed and unimplemented. | Before anyone but us runs this | The sealing step is easy to get wrong in ways that surface far away — a wrong port produced `28P01` at query time, not at registration. There is also no reason for the key to be handled outside the process at all. | OPEN — ADR-017 §3. |
| D-10 | **ADR-015 is implemented for local passwords only.** Sessions, the first-start bootstrap, Argon2id and both rate limits are real. **OIDC, SAML 2.0, SCIM 2.0, API keys and mTLS are not**, and neither is the ArcGIS `/generateToken` surface. | 2026-08-13 | Built the identity source that air-gapped sites need first (ADR-015 §5 calls local accounts first-class, not a fallback), and stopped there rather than half-building five more. | OIDC before the first deployment with an IdP; `/generateToken` before any unmodified ArcGIS client is pointed at us | ADR-015 §5 lists four of these as free where a competitor gates them at Pro or Enterprise, so the positioning claim in Q-49 is not yet true. `/generateToken` missing means Q-17's *existing clients keep working* is currently false for any client that authenticates. | OPEN. The port shapes exist; each source is an implementation rather than a redesign. |
| D-11 | ~~**Authorization does not exist.**~~ **RESOLVED 2026-08-13, and the resolution was replaced on 2026-08-14** when the owner directed that we adopt ArcGIS Portal's matrix. Authorization now follows Portal: roles grant privileges, a user type caps them, and **reading is governed by each layer's sharing scope rather than by a privilege**. The closed default survives in a better form — a layer is private to its owner until shared — and the ownership axis, which this entry said was *not yet due*, turned out to be how reading works. Original: **Authorization does not exist.** Authentication resolves a principal; nothing consults it. Every published layer is readable by anonymous, and the `role` table ships empty. | 2026-08-13 | Q-59 has not decided what the roles are, and ADR-015 §7 and review O3 both warn against inventing them — a control resting on a term nobody has defined. | Q-59, which now blocks rather than merely being open | A server that authenticates and does not authorize invites the assumption that signing in restricts something. The startup warning says otherwise at every start. | OPEN — and it is the reason the admin API (ADR-017) is not built yet: publish and register endpoints with no authorization would be worse than the hand-written SQL of D-09. |
| D-12 | **The per-address rate limit uses the socket address, never `X-Forwarded-For`.** Behind a reverse proxy every request appears to come from the proxy, so the address limit is effectively one shared bucket. | 2026-08-13 | Trusting a forwarded header without a configured trusted-proxy list lets any caller choose their own rate-limit bucket, which makes the limit zero. Too coarse is recoverable; forgeable is not. | Before a documented deployment behind a proxy | With one bucket, 50 failures anywhere disable sign-in for everyone. The per-account limit is unaffected, and by design a correct password still works. | OPEN — needs a trusted-proxy configuration, which is ADR-014 territory. |
| D-13 | **The timing gap between a known and an unknown account name is narrowed, not closed.** An unknown name pays for a hash it does not need, but it is not the same work, so the distributions still differ under enough samples. | 2026-08-13 | Closing it properly means verifying against a fixed decoy hash with the same parameters as the real ones, which requires knowing those parameters before the account is found — and they are stored per credential. | If account enumeration is ever demonstrated against a deployment | Account enumeration is the step before credential stuffing. | OPEN, and documented in `LoginService.SpendComparableTime` rather than described as constant-time. |
| D-14 | **A server that loses its last platform administrator cannot be recovered in band.** The setup flow only runs when there are no users at all, so a store with accounts and no `platform-administrator` grant has nobody who can grant one. | 2026-08-13 | **Found by running the ADR-018 upgrade against a store set up before it.** Migration 3 seeds the roles and grants none, so the existing administrator came out of the upgrade unable to administer anything — ADR-018 §4's failure reached by a different road. | Before the first deployment that is not ours, or sooner if role revocation reaches the admin API | Reads keep working; nothing administrative does. The only fix is SQL against the platform store. | **Detected and diagnosed, not repaired.** Startup logs the exact recovery statement. Repairing it automatically means issuing a credential to a log whenever the last administrator's grant disappears, and *the last administrator's grant disappeared* is a state an attacker would like to arrange — so the safe version of this needs design, not a quick fix. |
| D-15 | **The user-type ceiling is enforced but unreachable from any endpoint.** `Authorization.Resolve` intersects the role grants with the type's ceiling, and nothing in the HTTP surface currently requires a privilege — reads go through sharing, and the admin API does not exist. | 2026-08-14 | ADR-018 §3a was adopted with the model; the endpoints that consume it are the next increment. | The first admin endpoint | The ceiling is covered by unit tests and has never run against a request. Its failure mode is *silent over-permission*, which tests can demonstrate and only a live path can disprove. | OPEN, and stated rather than demonstrated — no live walk of it was faked. |
| D-16 | **Portal's privilege identifiers and default-role assignments are structural, not verified line by line.** The shape — two axes, three privilege groups, five default roles — is right; the exact catalogue is ours reading like Portal's. | 2026-08-14 | ADR-018 condition 2. Adopting a documented model from memory is how a compatibility claim becomes subtly false. | Before any surface claims Portal compatibility | An administrator migrating from Portal finds privileges that do not correspond, and trusts the mapping anyway. | OPEN — ADR-018 §5 records which parts are inferred. |
| D-17 | **The request path reads the catalogue on every query, and that half is now deliberate.** *(2026-08-14: the table-shape half is fixed; the catalogue half is kept on purpose, and D-17's stated remedy is rejected as written — see the resolution column.)* `PostgresLayerCatalog.FindAsync` runs per feature request, so serving depends on the platform store being up. **Narrowed 2026-08-15 by [ADR-026](adr/ADR-026-serving-through-a-platform-store-outage.md):** the read still happens on every request, so revocation stays instant — but a store outage no longer stops serving outright. Public services are answered from the last catalogue entry seen; everything else is refused. The single-point-of-failure half of this debt survives for private and organisation data, deliberately. | 2026-08-14 | Found while writing [ADR-019](adr/ADR-019-portal-server-split.md) §4, which claims a seam between the catalogue domain and the runtime domain. **The claim was written down as a principle before it was checked.** | Before any performance claim about serving, and before ADR-017 §6's degraded surface is called done (ADR-019 condition 2) | Fusing the tiers spent the isolation Esri gets for free. If the catalogue store is down, serving stops. | **PARTLY RESOLVED, and the rest reclassified.** D-17 said the fix was *a resolved service context, cached and invalidated*, and that remedy is now **rejected as stated**: a `PublishedLayer` carries the sharing scope, the owner and the started/stopped status, so caching it caches the authorization decision — a layer made private stays readable, and a service stopped mid-incident keeps answering. Two servers over one store makes that unfixable locally, because the mutation happens in the other process. So `ServiceContexts` caches **only the table shape** (`information_schema` + `ST_EstimatedExtent`), which is where the cost actually was: measured 2026-08-14, **5.76 ms → 1.64 ms of fixed per-request overhead**, a 100-row query 41% faster and layer metadata 48% faster. The residual 1.64 ms is the catalogue read, kept on purpose. **The availability half is not fixed and is now [Q-95](open-questions.md)**, because it is a question about what should happen rather than a compromise anybody accepted. |
| D-18 | **`/admin/health` is anonymous and therefore redacted, which is the wrong trade and the only one available.** Sessions live in the platform store, so during the outage the endpoint exists for, nobody can authenticate — and an unauthenticated caller must not be told the store's host and port. | 2026-08-14 | Found while discharging ADR-017 condition 1: the endpoint was echoing `127.0.0.1:55432` to anyone who could reach the port. | When ADR-017 §6's break-glass path is designed (A-051) | The administrator who most needs the detail is exactly the one who cannot prove they are an administrator. The endpoint currently says *redacted* to the person it was written for. | OPEN. The break-glass path is the fix and it is the classic shape of an authentication bypass, so it needs design rather than a quick patch. |
| D-19 | **There is no published image and no release.** The compose file builds from source, so the quickstart needs the repository and a working Docker build rather than a `docker pull`. | 2026-08-14 | Packaging was built before there was anywhere to publish to; a registry, a tag scheme and a release process are Q-72's territory and Q-72 is unanswered. | The first release | The target user has a lapsed licence and a deadline. *Clone the repository and build two images* is a different proposition from *docker compose up*, and the difference is most of the adoption. It also blocks the other half of ADR-016 condition 1, which needs two published tags to test a stale image against a newer store. | OPEN. |
| D-20 | **`features:edit` is stricter here than in ArcGIS Portal, because editor tracking does not exist.** Portal's *edit* covers adding and changing *your own* features; *full edit* reaches everybody's. We map adds to `features:edit` and **all** updates and deletes to `features:fullEdit`. | 2026-08-14 | The distinction rests on knowing who created each row, which is editor tracking, deferred under Q-58. Treating an update as *probably yours* would be a guess made with somebody else's data. | When Q-58 lands | A deployment that wanted contributors able to edit only their own work cannot express it: they get everything or nothing. Narrower than Portal, and narrow is the safe direction. | OPEN, and documented in the endpoint rather than left to be discovered. |
| D-21 | **Paging by `resultOffset` is not stable.** *(2026-08-14: improved, not closed. `orderByFields` is now supported, and a client that orders by a unique key — which is what the ArcGIS SDK does — gets stable pages. A client that pages without ordering still does not.)*  The query orders by identity when an offset is given, which stops a page repeating itself — but a feature inserted or deleted between two requests still shifts every later page. | 2026-08-14 | Added because the ArcGIS Maps SDK pages by offset and refusing it refuses the client. Cursor paging needs a stable sort key exposed to the client, which is ADR-008 territory. | Before anyone syncs a large layer | A client walking a changing layer silently misses features. It sees no error; the pages simply do not cover the set. | OPEN, and documented on `FeatureQuery.Offset` rather than only here. |
| D-22 | **`where` accepts only `1=1`, and that special case is load-bearing.** Every ArcGIS client sends it to mean *no filter*, so it had to be accepted; anything else is refused. | 2026-08-14 | Parsing SQL fragments out of a request is how injection happens, and the real answer is ADR-008's query AST. | When the query AST exists | The gap between what clients expect and what this supports is large and invisible until a user filters a layer and is refused. **The risk is that this special case grows** — one more literal, then a simple comparison — until a parser exists that nobody designed. | OPEN, with the warning written into the method. |
| D-23 | **Nothing checks a password against known-breached lists.** Length is the only rule ([ADR-015](adr/ADR-015-authentication.md) §6a), so `Passw0rd!` and a random nine characters are treated alike — and the first is in every wordlist. | 2026-08-14 | Recorded when the minimum was lowered from 12 to 8. A breach check is worth more than either number, and the honest reason it does not exist is that it needs a corpus (Pwned Passwords is ~10 GB, or a k-anonymity API call that leaks a hash prefix to a third party). Neither fits a product whose baseline is *one deployable against one PostgreSQL*, and neither had been thought about. | Before this is recommended for anything internet-facing | The realistic attack on a small deployment is credential stuffing, not offline cracking — and this is the defence aimed at credential stuffing. Argon2id and the rate limit slow guessing; they do nothing about a password that is already published. | OPEN. **The lowered minimum makes this more pressing, not less**, and saying so is the point of writing it down. |
| D-24 | **`layer.is_hosted` and `layer.sharing` are columns nobody reads, and writes still reach both.** Schema 7 derives *hosted* from `data_source.is_datastore` instead; the old column remains, written `false` by `PublishLayerAsync`, ignored by the catalogue. **2026-08-15: the second one was not harmless.** Since migration 11 the serving path reads sharing from the owning *service*, but `PUT /admin/layers/{name}/sharing` was still writing `layer.sharing` — so making a layer private returned 200, changed a column nothing reads, and left the layer readable by anybody. Fixed; found while testing [Q-95](open-questions.md). The lesson is the same one `is_hosted` taught and it cost more this time: **a dead column is a live hazard while a writer can still reach it**, so dropping them is the repayment and keeping them in step is not. | 2026-08-14 | Dropping a column is a contract migration and moves `minimum_reader_version`, which would stop a version-6 node reading the store mid-upgrade. Expand now, contract later — that is [ADR-016](adr/ADR-016-packaging-deployment-upgrade.md)'s whole shape and skipping the wait is how a rolling upgrade breaks. | When no supported release reads schema 6 | **A column that lies is worse than a column that is missing.** The next person to need *is this hosted* will find `is_hosted`, use it, and get `false` for everything — which is exactly the bug schema 7 was written to fix, reintroduced by someone reading the obvious field. | OPEN, with the warning written into the schema-7 migration and the catalogue query rather than only here. |
| ~~D-25~~ | ~~**Tile cache TTL is one global number for every layer.**~~ **CLOSED 2026-08-15.** `layer.cache_seconds` (migration 13), settable at publish time and afterwards with `PUT /admin/layers/{name}/cache`. Null means *nobody has said* and takes the server default; **zero is a different answer** — never serve a cached tile — and both the cache and the `Cache-Control` header honour it. The same number governs every cache in the chain: our disk cache expires by it and `max-age` carries it to the browser and any CDN, which is the only way they can agree. For a multi-layer tile service the **shortest** of its layers wins, because one tile can only be as fresh as its most volatile part. No purge on change: freshness does not make a cached byte wrong, which is [ADR-010](adr/ADR-010-caching.md) §5.1's reasoning for sharing applied where it also holds. | 2026-08-15 | — | — | — | CLOSED. |
| D-26 | **A relationship's `composite` flag is stored, reported, and does nothing.** [ADR-013](adr/ADR-013-feature-service-data-model.md) §3 says a composite relationship cascades on delete — by the database where it already declares `ON DELETE CASCADE`, by us in the same transaction where it does not. Neither is implemented. | 2026-08-15 | Relationships shipped with declaration, validation and querying, which is the whole read path; the cascade is a write-path concern that touches `applyEdits` and needs the same-transaction guarantee ADR-013 §3 describes. Shipping the read half beat shipping neither. | Before anybody declares a composite relationship and deletes an origin feature | **A flag that is stored and reported but not honoured is worse than one that is absent.** An administrator sets it, sees it in the layer document, and concludes deleting a parcel removes its owners. It does not, and the orphans are silent — they accumulate, count against nothing, and surface as rows nobody can explain. The endpoint should refuse `composite: true` until it is honoured, and does not. | OPEN, and named in ADR-013 §6a. |
| D-27 | **Twelve of twenty-two ADRs still describe a three-database product.** [v1-scope](v1-scope.md) §3a cut Oracle and SQL Server on 2026-08-13; the ADRs carry **70 mentions** of them and **two** mention the cut. ADR-002 is the worst: fourteen mentions, and an amendment still reading *"Oracle Spatial and SQL Server Spatial are first-class"*. | 2026-08-13 | The cut was taken as a scope decision under time pressure and the propagation was deferred — recorded then as review findings A2, A5, A6, P11 and P14, and counted for the first time by [contradiction sweep 2](reviews/contradiction-sweep-2.md). | Before anybody implements from an ADR rather than from the code | **Somebody building from these documents builds abstraction nobody needs** — ADR-008's per-dialect pushdown table, ADR-007's per-provider connection budget, ADR-010's per-engine change detection. The abstraction is exactly what the cut existed to remove, so the stale text does not merely mislead, it undoes the decision. And a reader who starts at ADR-002 is told the opposite of the scope. | OPEN. **Not repairable by a sweep**: some of those paragraphs are deferred rather than wrong, and deciding which is which per paragraph is the owner's. |
| D-39 | **`/admin/services/{name}` means two different things.** `…/sharing` addresses a *system* service (the geometry server); `…/groups` and `…/style` address a *published* one. A published service named `Geometry` would make a sharing change land on the system service instead. Latent today because no published service has that name. |  |  | Before a published service can be named after a system one — or sooner, since the fix is a route rename and the cost only grows as clients bind to these paths |  | **OPEN** |
| D-29 | ~~**There is no CI, so nothing runs the checks a published promise depends on.**~~ **PAID 2026-08-15.** [.github/workflows/ci.yml](../.github/workflows/ci.yml) now builds, audits dependencies, and runs **all eight suites** — the five that need nothing, plus the two that need PostGIS, against a `postgis/postgis:16-3.4` service container seeded by [tools/ci-corpus.sql](../tools/ci-corpus.sql). **The conformance suite runs too, against a server the workflow builds from nothing**: create the schema, migrate, start, take the single-use setup token out of the log, and seed with [tools/seed-conformance.py](../tools/seed-conformance.py), which publishes every fixture through the same HTTP API a client uses rather than writing to the database. **Proved locally against an empty database before it was written into the workflow**, which is how four of the findings below were found at all. **Six things the setup found, and none of them by review.** **(1)** A fresh install fails with a raw Postgres error — D-36. **(2)** `addFeatures` answers 200 with a per-feature results array, and a seed that trusted the status code inserted nothing while reporting success; eighty conformance tests then failed with things like “no tile with any content was found”. **(3)** Two multi-layer tests walked every entry in a service document and demanded a `geometryType` from each, which crashes on a group layer — asserted two tests below as correct behaviour. They passed only because the fixture they were written against had no groups. **(4)** Two discovery suites read `/rest/services` and took the first entry, so they failed on any server whose services all live in a folder — which is every server published through the hosting API, because hosted layers always land in `hosted`. They follow folders now, as a client does. **(5)** A layer's extent is cached with its description, so features that had just landed were invisible to the two edit tests that place a feature at the centre of it — they failed once and passed on the next run, which is the worst way for a race to present; the seed calls `POST /admin/layers/{name}/refresh` and waits for the extent to appear. **(6)** The corpus discovery took the alphabetically first geometry table, so adding the CI corpus to a developer database silently switched both oracle suites from real cadastral polygons to sixty generated ones — they now take the richest table by `reltuples`. And the obvious staleness check on the status page, a regenerate-and-diff, would have failed on every run because the page stamps its own generation time; [tools/registers-check.py](../tools/registers-check.py) compares the counts instead. Original text: No workflow of any kind: every test runs because somebody remembered. [ADR-025](adr/ADR-025-governance-and-maintenance.md) now commits in writing to fixing reported vulnerabilities in 90 days, and `dotnet list package --vulnerable` is clean today with nothing to report it going dirty. **The tension worth stating:** this project's suites *fail rather than skip* when their subject is absent, so CI cannot just run `dotnet test` — it needs a PostGIS service container and a running server, or it runs a subset and says so. |  |  | Before the repository is made public — the same trigger as ADR-025's conditions, because that is when the promise starts |  | **OPEN** |
| ~~D-28~~ | ~~**Nothing structurally prevents the next service without a layer from shipping ungoverned.**~~ **CLOSED 2026-08-15.** Every route under `/rest/services` now carries a `SharingGoverned` marker naming what governs it, `GET /admin/routes` lists them with an `ungoverned` count, and a conformance test fails when that count is not zero. **Verified by breaking it**: an unmarked probe route was added, the test failed and named it, and the probe was removed. The marker enforces nothing by itself — enforcement is still `ServiceLookup` and the geometry group's filter — what it does is make the property enumerable, which is what the geometry hole needed and did not have. | 2026-08-15 | — | — | — | CLOSED. [ADR-018](adr/ADR-018-authorization-and-roles.md) condition 5 discharged. |
| D-33 | **`layer.sharing`, `layer.status` and `layer.owner_principal_id` still exist and mean nothing.** Migration 11 moved sharing, status and ownership to the `service` table and left the layer's columns in place, carrying whatever they held at the moment of the migration. | 2026-08-15 | Expand-and-contract ([ADR-016](adr/ADR-016-packaging-deployment-upgrade.md) §5): the expand half must not break a reader running the previous build, so the columns stay until a later release drops them. | The release after the one that ships migration 11 | **This file already contains the cautionary tale.** `is_hosted` stayed writable after its meaning moved, drifted to false on every insert, and silently disabled every vector tile service — nobody noticed because nothing errored. These three columns are the same shape of hazard aimed at the sharing model, which is a worse place for it. The defence today is that the catalogue's select list reads `s.sharing` and never `l.sharing`, so a stale value cannot be believed — a defence made of one SQL string and a comment. The contract migration is what makes it structural. | OPEN, and named in [ADR-013](adr/ADR-013-feature-service-data-model.md) §4g. |
| D-30 | ~~**The feature query path has never been measured.**~~ **PARTLY PAID 2026-08-16 — [benchmarks/feature-query](../benchmarks/feature-query/RESULTS.md), run 5.** The path is instrumented in process and decomposes into lookup, prepare, driver, decode and serialise; nothing is timed unless the `query` logger is enabled for Debug, so it costs a level check in production. **Two findings.** The per-request catalogue read is **1.8 ms flat on every query** — 70% of a one-feature request, and 2.6× the cost of the data query it precedes. That read is deliberate (D-17: it carries the sharing scope and the started/stopped status) and what was missing was its price, which is now a number rather than a feeling. At a thousand features **serialisation is 43% and the driver is 16%** — so the cost is in this repository's own code and not in the database, which is the question F1 could not answer from outside. Decode was smaller than A-037 predicted for this shape of data: 9%. **Concurrency answered 2026-08-16 (§3b).** A C# generator replaced the Python one, speaks TLS, and reads the server's own GC and CPU counters from `/admin/health`. The feature query path scales **4.75× from concurrency 1 to 8** (198 → 942 req/s) and is **not what stops scaling after that** — the do-nothing control saturates in the same place. **It is not allocation-bound:** GC pause is 0.2% at concurrency 1 and never exceeds 2.5%, against the 80.9% a tile round measured, and allocation is flat at 142–164 kB per request across all levels. **A-037 does not transfer to this path**, and that transfer was the assumption D-30 was opened on. Not CPU-bound either: 3.6 of 16 cores at the peak. **What remains unseparated** is which of three shared costs is the ceiling — TLS, the per-request authentication, or the generator — which needs a second machine or a server with authentication off. **And the fifth wrong harness:** the server appeared to saturate at 130 req/s until that turned out to be .NET's console logger blocking the request thread on a full queue, because stdout had been redirected to a file to read the phase lines back. Without redirection the same path holds 1.15 ms flat. One command from being written up as a server defect. Original text: **The feature query path has never been measured.** Four benchmark rounds covered tiles and overlay; the word *query* appears in neither results document. It runs the same WKB-into-an-object-graph mechanism those rounds found to be allocation-bound at 18% CPU, and it is the most-used surface in the product. A black-box probe ([performance-gate-1.md](reviews/performance-gate-1.md) F1) shows throughput plateauing at 5–7× for 24× concurrency and cannot say why — from outside, an allocation ceiling, a pool limit and a contended host look identical. Needs the in-process counters the tile path already had. |  |  | The concurrency half is still what ADR-007 condition 3's connection budget needs, and they still need the same harness |  | **PARTLY PAID.** The phase breakdown exists; the load generator does not. |
| D-34 | **A layer or group cannot be moved, re-parented, renamed or removed without unpublishing it.** Publishing puts a layer in a service and under a group; nothing takes it out again, moves it to another group, or reorders it. Groups cannot be deleted at all. | 2026-08-15 | The service and group model shipped with the read path and the create path, which is what makes multi-layer services usable at all. Rearranging one has its own correctness questions — indices are referenced by saved web maps and must never be reused, and re-parenting is the one operation that could introduce a cycle, which is currently impossible by construction ([ADR-013](adr/ADR-013-feature-service-data-model.md) §4g-i) — and shipping the half that works beat shipping neither. | The first time somebody puts a layer in the wrong service or the wrong group | **The workaround is destructive and will not look it.** Unpublishing a hosted layer may drop its table; an administrator correcting a mistake in the service or group name would reasonably expect to be moving a pointer. Until there is a move operation, the admin API should say that unpublishing is the only route and what it costs, and it does not. **Re-parenting in particular needs a cycle guard before it exists** — today a cycle cannot be written because a parent must pre-exist and nothing moves, and adding the operation removes that argument without replacing it. | OPEN. |
| D-31 | **Overlay under concurrency is still unmeasured.** Every figure in [benchmarks/geometry-overlay](../benchmarks/geometry-overlay/RESULTS.md) is a single request on an idle machine, and the pool that now bounds overlay has never been measured with both its workers busy. | 2026-08-15 | Q-97's answer bounds the worst case by construction — two workers, 1 GB and ten seconds each — which makes the unmeasured case much smaller than it was. Smaller is not measured. | Before the overlay worker count is raised above two, or before anyone claims a throughput figure for overlay | **The bound is arithmetic, and arithmetic about allocation has been wrong here before.** A-037 measured an 80.9% GC pause at 18% CPU, and A-042 was an argument about asymptotics that measurement overturned. "Two workers at 1 GB is 2 GB" assumes the ceiling is respected under real concurrent load, that a killed worker's memory is returned promptly, and that process launches under contention stay inside the start-up allowance. Each is likely and none is measured. | OPEN, and named in [ADR-022](adr/ADR-022-geometry-server.md) §9. |
| D-35 | **Planar area was computed by a shoelace sum with no local origin, and it shipped that way for a day.** In EPSG:3857 the coordinates are around 3.2 by 5.0 million, so each product in the sum is about 1.6×10¹³ and its rounding error near 0.003 — while the answer for a parcel is a few hundred. **Fixed 2026-08-15** by subtracting the first vertex, which is what PostGIS's `ptarray_signed_area` does and why `ST_Area` agreed with itself to eleven digits where we disagreed at five. **Measured before and after:** worst relative disagreement across forty real polygons was 1.58×10⁻⁵ and is now under 10⁻⁸. **The absolute error did not depend on the feature's size**, so a 2 m² utility box was out by about 0.1 per cent while a hectare was out by nothing visible. **How it was found is the part worth keeping:** not by review and not by a test of `areasAndLengths`, which had none — a cut's pieces failed to add up to their target, and the pieces were innocent, because both sides of that sum were this function. `Area_matches_PostGIS_on_real_polygons` now covers it and fails by three orders of magnitude when the fix is reverted. | 2026-08-15 | — | — | **The rest of the tree was swept the same day and there were two more.** `ShapefileReader.SignedArea` had the identical plain shoelace, used to rank rings by size when deciding which contains which — fixed the same way; the consequence there was small, because one part in a hundred thousand does not reorder rings that differ by orders of magnitude, and it is corrected because the correction is free and because leaving one known-wrong shoelace in the tree invites the next one. `LinearRing.SignedArea2` uses the trapezoid form, which subtracts the large x values before multiplying and is therefore accurate to about 10⁻¹¹ — far more than the sign it exists for needs, and less than an area deserves; it is now documented as an orientation test that must not be used to measure. `Length` subtracts before multiplying and has no equivalent cancellation. | CLOSED, but **`areasAndLengths` shipped 2026-08-14 with no oracle test at all** — that is the gap that let this live, and it is worth remembering that three of the four first operations still have none. |
| D-36 | ~~**Nothing in the repository says how to install against an empty database, and the first command fails with a raw Postgres error.**~~ **CLOSED 2026-08-15, partly.** Pointed at a brand-new store, `migrate --apply` printed the whole plan and then threw `Npgsql.PostgresException 3F000: no schema has been selected to create in` — Postgres saying that `SearchPath` names a schema that does not exist, which on a new database is the normal state rather than a fault. **The migrator still does not create it, and that is deliberate:** creating a schema is a privileged act, and doing it silently would turn a typo in a connection string into a second, empty installation instead of an error. What changed is that it now says `CREATE SCHEMA gisserver;` and exits 1. **Found by making CI build a server from nothing, which was the first time anything in this project had installed the product against an empty database** — every run before that inherited a schema somebody had made by hand a week earlier. | 2026-08-15 | — | — | *(The documentation half was closed the same day: [deployment.md](deployment.md) §0 now carries the whole sequence — database, schema, the two required settings, the explicit migration, and the one-time setup token — and every step of it is exercised by the conformance job on every push, so drift turns that job red rather than turning this page quietly wrong.)* | CLOSED. |
| D-37 | ~~**A test that had been written off as flaky twice was not measuring its own claim.**~~ **CLOSED 2026-08-16.** `A_worker_is_reused_rather_than_launched_per_request` counted processes named `Graticula.Overlay.Worker` **across the whole machine** before and after five overlays. That also counts the pools other test classes are running in parallel and any server the developer happens to have up, so it failed intermittently and was dismissed as flaky twice in this project's history without anybody reading it. It now asserts the pool's own launch counter, which is what the sentence in its name actually claims. **Worth keeping as a pattern rather than as an incident:** an intermittent failure is a defect in something, and 'flaky' is a description rather than a diagnosis — this one had been true for as long as the test had existed. | 2026-08-16 | — | — | It surfaced the day after CI was built, which is the point: a flaky test in CI is a test that teaches people to ignore CI. | CLOSED. |
| D-38 | ~~**The service lookup's unique index disagreed with the query that needed it, in meaning as well as in form.**~~ **CLOSED 2026-08-16, migration 15.** Migration 11 created `unique (coalesce(folder, ''), lower(name))` — case-insensitive on the name and case-*sensitive* on the folder — while every read asks `coalesce(lower(folder), '') = coalesce(lower(@folder), '')`. Two consequences. **The correctness one:** `Hosted/parcels` and `hosted/parcels` could both exist and the lookup matched both, returning whichever row came first — and since the two can carry different sharing scopes, the same URL could resolve to the public service or the private one depending on row order. Folders are taken from the administrator's request rather than generated, so the pair needed no trick; **reproduced against a real Postgres before the fix was written.** **The performance one:** an expression index is only an index for the expression it holds, so the predicate could not use it and the lookup sequentially scanned `service` on the most-used path in the product — 85 µs at 33 services, linear at the stated target of 1,000. **How it was found:** not by review. The D-30 instrumentation put the per-request catalogue read at 1.8 ms, and reading the query to find out why turned up the mismatch. | 2026-08-16 | — | — | The migration refuses rather than half-applies: it names any colliding pair and stops, because two services that were legal yesterday and ambiguous today need an administrator to say which survives. | CLOSED. Four tests, two of which fail when the migration is removed from the registry — the other two assert behaviour migration 11 already had, and correctly stay green. |
| D-32 | ~~**A transformation PROJ could not do accurately is reported as though it were accurate.**~~ **PARTLY PAID 2026-08-16 on the GeometryServer surface.** The accuracy is still unreachable — `ST_Transform` does not name its pipeline and PROJ's operation database is not queryable from SQL (Q-100) — but **whether a datum was crossed is readable from the two references' own WKT**, and that is the line between a transformation that is exact by construction and one that can be metres out. `project` now answers with `datumShift` and, when it is true, a caution naming both datums and saying in plain words that PROJ falls back to a ballpark without failing. 4326 to 3857 is not flagged, which is what keeps the flag worth reading. An unreadable reference is reported as unknown rather than as safe. **Still open on two surfaces:** FeatureServer's `outSR` transforms in SQL inside `PostGisFeatureSource` and never touches the projector, and the tile path does the same in `PostGisTileSource` — a protobuf tile also has nowhere to carry a caution, so for tiles it has to go on the service document instead, which is a different change. Original text: **A transformation PROJ could not do accurately is reported as though it were accurate.** Layers now keep their own reference and the tile path transforms per request (Q-96). PROJ picks the pipeline; when the shift grids for the accurate path are absent it falls back to a ballpark transformation — **without failing** — and the tile is drawn metres from where the data is. | 2026-08-15 | Q-96's third option was *transform anything and report the accuracy PROJ actually used*. The transform shipped; the reporting did not, because `ST_Transform` does not say which pipeline it chose and getting that out means `ST_TransformPipeline` or a PROJ query per layer, which is a design rather than a line. | Before anybody publishes cadastral or survey data in a national grid and reads a tile as authoritative | **Metres, silently, on exactly the data where metres are legally significant.** 4326 to 3857 is a closed formula and safe; EPSG:5254 or a British/Dutch national grid to WGS 84 is a datum change, and the grids are a separate download PROJ does not ship. The failure has no error, no log line and no visual signature — the map looks right and is in the wrong place. [geometry-crs-policy](geometry-crs-policy.md) §3 already asks for a pinned pipeline and this is the same gap on a second surface. | PARTLY PAID. The FeatureServer's `outSR` and the tile path both still have the identical exposure and the same absence of reporting. |
| D-47 | ~~**The publish endpoint accepted three documented fields and discarded them, so multi-layer services were unreachable through the admin API.**~~ **CLOSED 2026-08-16.** `POST /admin/layers` takes `serviceName`, `parentLayerId` and `cacheSeconds`. `TryReadPublication` built its `LayerPublication` with the first ten arguments and stopped; all three of the record's remaining parameters default to `null`, **so the short call compiled** and the endpoint answered `201` having thrown the operator's intent away. Naming an existing service produced a layer in a service of its own, and the response's `service` field reported that new name, so the answer looked correct. **The catalogue underneath was right the whole time** — `ServiceName ?? Name`, `parent` and `cache` are all bound in the SQL — and `PostgresAdminCatalogTests` asserts the multi-layer behaviour at that level. So the owner's 2026-08-15 correction that *"a service is a combination of layers"* was implemented, tested, and unreachable. `POST /admin/featureservices` was meanwhile answering with a note instructing the operator to *"publish with serviceName set to this service"*, which is an API documenting its own broken path. | 2026-08-15 (shipped), found and closed 2026-08-16 | Nothing. Optional parameters on a record turned a missing argument into valid code, and no test spanned the request-to-record mapping. | — closed | — closed | **CLOSED.** The three fields are passed through, with `serviceName` trimmed and blank treated as absent. Verified end to end against a running server: publishing into an existing service now answers `service: probe_svc, layerId: 1`, and the service document shows that layer nested under the group at 0. **What is still missing is the test, and its absence is the finding:** there is no in-process host harness anywhere in `tests/`, so nothing exercises an admin endpoint's mapping — the layer below is covered and the layer above is not. That gap is [D-48](#)'s neighbour and the reason this survived. |
| D-48 | **A service and a group layer can be created through the admin API and cannot be removed, and unpublishing a layer leaves its service behind.** `POST /admin/featureservices` and `POST /admin/services/{name}/groups` have no `DELETE` counterpart — the only deletes on the surface are `/admin/layers/{name}` and a style. Measured 2026-08-16: publishing four layers and deleting all four left **five empty services** in the catalogue, three of them created implicitly by the layers themselves. They remain in the services directory, so a client sees a FeatureServer with no layers, and the only way to remove them is SQL against the platform store. | Pre-existing; measured 2026-08-16 while building the console's create surface | Not chosen. Creation was built for the case the owner asked about — several layers in one service — and removal was never the question in front of anybody. Single-layer publishing hid it, because deleting the layer left a service nobody looked for. | **Before the console's create surface is used on a real deployment.** An operator who can make structure and not unmake it either stops using it or edits the database, and the second is worse. Concretely: `DELETE /admin/featureservices/{name}` refusing while layers remain, `DELETE /admin/services/{name}/groups/{id}` refusing while children remain, and unpublish removing a service it implicitly created. | Empty services accumulate in every deployment that experiments, and each one is advertised to clients. It also makes the create surface untestable in a suite that has to clean up after itself, which is part of why D-47 had no test. | **OPEN.** The console can now create both, and its own copy does not claim they can be removed. |
| D-53 | **The importer accepts invalid geometry, and nothing on the publish path checks.** Measured 2026-08-17: `hosted.tr_ilce_511f6767`, imported from OpenStreetMap by our own importer, holds **18 invalid geometries out of 25,280** — `ST_IsValidReason` says *"Too few points in geometry component"*, so they are degenerate rings rather than self-intersections. Neither `PostGisImporter` nor `POST /admin/layers` looks: import writes what it parsed, and publish records a table it never validates. **Found by another server refusing to publish it** — the peer's publish path validates the source table first and answered *"Table validation failed (invalid-geometries): Source table contains invalid geometries"*, which is a capability we do not have, exercised against our data. | Pre-existing; the OSM import ran 2026-08-16 and the layer has been served since | Not chosen. Validity was never asked about: the importer's job was read GeoJSON and write PostGIS, and PostGIS accepts an invalid geometry happily — `geometry` has no validity constraint, so nothing refuses on the way in. | **Before the publish surface is used on data nobody inspected**, which is the case it was built for. Two separate repairs: the importer reports invalid counts (and decides whether to refuse, repair with `ST_MakeValid`, or record), and publish validates the source the way the peer does. The second matters more for registered tables, where the data is somebody else's and we did not import it. | An invalid geometry is not a stored problem, it is a query-time one: `ST_Intersects` against a degenerate ring can error mid-scan, so a filter that touches one of those 18 rows fails a request that succeeds everywhere else — the hardest class of fault to reproduce. It also silently propagates into every tile and every export. | **OPEN, and the count is measured rather than estimated.** 18 of 25,280, verified with `ST_IsValid` directly against the table rather than taken from the other server's word. |
| D-52 | **The startup compatibility check says "compatible" to a build that cannot read the store, and then the server does not start.** Found 2026-08-17 while adding migration 16. A build whose reader selects a column the store does not have yet passes the check — it printed *"server at schema 15 is compatible with the platform store (schema 15, minimum reader 1)"* — and then died with an unhandled `Npgsql.PostgresException 42703: column s.serves_features does not exist`, having bound no port. **The check is one-directional:** it asks whether a store's *minimum reader* is new enough for this build, which protects an old reader from a newer store, and never asks whether this build needs a schema newer than the store has. | Pre-existing; any column-adding migration has had this property. Made visible by migration 16, which is the first one whose column is read on the service path that runs at startup | Not chosen. The compatibility model was written for the direction that loses data — an old build writing to a store it does not understand — and the direction that merely fails to start looked harmless until the failure was a crash with no port bound and a message about a column. | **Before a deployment upgrades a binary without migrating**, which is the ordinary order of operations for anyone who does not read release notes. Concretely: compare `PlatformMigrations.ComponentSchemaVersion` against the store's applied version at startup and refuse with a sentence naming `migrate --apply`, instead of selecting a column and letting Npgsql explain. | An operator upgrades, restarts, and gets a stack trace about a column name in a table they did not know existed, with the server down. The correct action — run the migration — is not mentioned anywhere in the failure, and the line immediately above it says *compatible*, which sends them looking for a connection problem instead. | **OPEN, and cheap.** The information is already in hand at startup: the migrator prints exactly the right sentence when run by hand (*"1 migration(s) to apply … Re-run with --apply"*), so this is a check in the wrong place rather than a check that does not exist. |
| D-51 | **A session exists in two forms and the console can only clear one, so a failed sign-out looks like a dead button.** Login returns a bearer token *and* sets a `gis-session` cookie — `httponly`, `secure`, `samesite=strict` — carrying the same session id, and `/rest/whoami` accepts either. The console keeps the token in `sessionStorage`, which it can clear, and cannot touch the cookie by design. So **only the server can end a session**, and the console's sign-out used to swallow the failure of the one request that does it (`catch { /* already gone */ }`) and reload anyway: the cookie then signed the operator straight back in and the button appeared to do nothing. Reported by the owner 2026-08-16 — *"it does not sign out, it comes back to the same page."* | Pre-existing; the cookie was added for the browser sign-in flow, the token for clients | Reasonable separately: a form post needs a cookie, an API client needs a header. What was never decided is which one the console's own state is a copy of. | **Whenever a client holds a copy of session state.** The rule that follows: local state is cleared only *after* the server has confirmed, never before and never regardless. | A sign-out that silently does not sign out is worse than one that refuses — the operator walks away from a live session believing it closed. | **Two measured faults fixed 2026-08-16; the structure is still two-formed.** The console now asks first, stays and reports if refused, and leaves with `location.replace` so the console is not behind the back button. And `LogoutAsync` clears the cookie on its early-return path too — arriving there means the session is already gone *and the browser is still holding a cookie for it*, which was the one path that left a stale credential in place. **Server-side sign-out is verified correct by measurement:** a valid logout revokes the session and returns `gis-session=; expires=Thu, 01 Jan 1970`. Whether that closes the reported symptom is not yet confirmed by the owner. |
| D-49 | ~~**A hosted layer's VectorTileServer declares Web Mercator on an extent that is in degrees.**~~ **CLOSED 2026-08-16.** Measured 2026-08-16 on `hosted/tr_ilce`, stored in EPSG:4326: the tile service document reports `fullExtent` `{xmin: 24.7, ymin: 34.9, xmax: 45.6, ymax: 42.8}` with `spatialReference {wkid: 102100, latestWkid: 3857}`. The numbers are degrees; in Web Mercator that box is a few tens of metres off West Africa. **The FeatureServer for the same layer is correct** — the same extent, declared `4326`, and `/query` returns `4326` coordinates — so this is isolated to the tile service's metadata, which appears to stamp Web Mercator regardless of what the layer is stored in. Tiles themselves serve correctly at every zoom tried. | Pre-existing; found 2026-08-16 | Not chosen. Hosted layers were expected to be stored in Web Mercator — the console's own copy says *"stored in Web Mercator so the layer can serve tiles"* — and the imported `tr_*` layers are in 4326, so the assumption and the data disagree and only the tile document repeats the assumption. | **Before any client is told to trust a tile service's `fullExtent`.** Either the document reports the layer's actual spatial reference, or hosted layers are genuinely always stored in Web Mercator and the import path is what needs fixing. Which of the two is the real decision has not been taken. | A client that zooms to the declared extent goes to the Gulf of Guinea instead of Turkey. **This is the same error, on the server side, that this project made in its own viewer on 2026-08-16** — a centre in degrees read as metres — which is the argument for fixing the class rather than the instance. | **CLOSED on the second attempt, and the first attempt is the part worth keeping.** The first fix made `fullExtent` declare the reference it was actually in — honest, and **non-conformant**: a vector tile service's tiling scheme *is* Web Mercator, and a client reads `fullExtent` in the scheme's reference. Handed a document whose `fullExtent` said 4326 while `tileInfo` said 102100, the ArcGIS JS `VectorTileLayer` fetched the service document, the style and the sprites — and then **requested no tile at all**, settling nothing and reporting nothing. Found only by counting what the server log received, after the owner said the Tiles button did nothing for that layer. **So the extent is projected, not relabelled**: `VectorTileEndpoints` transforms it to Web Mercator through `IProjector` — the datastore's PROJ, per ADR-022 §4 — and the writer now **refuses** any other reference rather than declaring it, so the next caller is told instead of producing a document that loads nothing. A failed projection falls back to no extent, which the writer reports as the whole world: safe for a client, unlike degrees labelled as metres. Verified numerically against a running server — all four corners of `tr_ilce`'s extent match the Mercator arithmetic exactly, and the 3857-stored `look_buildings` is unchanged. **Two silent-hang sites closed with it:** `layer.when()` now races a clock the way `view.when()` does, because a client that stops after the metadata never rejects, and until that clock existed no `catch` could have reported this. |
| D-50 | **The capability probe cannot inform the one publish field the operator must decide.** `POST /admin/layers` requires `identityColumn`, and Q-57 makes that a nomination rather than an inference — *"declared, not inferred"*. The probe reports `objectIdColumn` and nothing else about candidate keys: no primary key, no unique constraints, no column list. So a UI can offer exactly one suggestion, and only when an ArcGIS-shaped object id already exists. | Pre-existing; found 2026-08-16 | Reasonable while publishing was done by whoever knew the table. It stops being reasonable the moment a console offers a table list, because the list implies the server knows enough to help. | **When the console's publish form is used against a table nobody present designed.** Concretely: the probe reporting each table's primary key and unique integer columns, so the nomination is a choice among named candidates rather than a typed guess. | The operator types a column name from memory and a typo becomes a `400`, or worse a wrong-but-existing column becomes the feature identity — which decides which row an edit lands on. | **OPEN.** The console asks for it explicitly, empty and required, and offers the object-id column as a one-click nomination rather than prefilling it — because prefilling would present the server's inference as the operator's decision, which is what Q-57 refuses. |
| D-46 | **A behaviour is fixed in one of the several places that carry it, and the others keep the bug.** Six instances on 2026-08-16, all found by the owner rather than by us, and a seventh the next day found by looking at a screenshot of my own work. **(1)** [D-44](#)'s Content-Security-Policy fix moved `index.html`'s script to a file; `map.html` stayed inline, so the same policy killed the same way one edit later. **(2)** The rule *imported tiles replace the vendored ground* was written into `view.js`; the console's map panel and the SDK page kept drawing Natural Earth, so "it is still showing" was true in two of three places somebody might look. **(3)** The ground picker persisted a choice; the `?ground=` link that documented it did not, so following the documented route left the setting empty. **(4)** A URL was built from `layer.ServiceName` in one place after the same class of guess had just been fixed in another ([D-45](#)). **(5)** `map.js` waits for `view.when()` before calling `goTo`, with a comment saying why — *"after the view is ready, and the failure is said out loud"* — and `console.js` never did, so clicking **Tiles** raced the view's initialisation and failed with *"Cannot read properties of undefined (reading 'animation')"*, intermittently, which is the worst version of it. Reported by the owner on 2026-08-16 with the detail that made it diagnosable: *"sometimes on the first click."* Holding the view in a variable rather than a promise made a second click return the half-built view as though it were ready, so the guard that looked like protection was part of the fault. **(6) The fix for (5) then produced the same button doing nothing at all**, and this one is the clearest instance in the list: `#view` sits inside a panel that is `display: none` until shown, and both callers revealed the panel *after* asking for the view. Awaiting `view.when()` on a hidden, zero-sized container never settles, and memoising the promise made it permanent — every later click joined the same pending promise. No error, no status, nothing logged. Reported as *"I press it and nothing happens."* The tiles branch of the delegated click handler was also the one branch with no `try`, while its sibling had one, so even a rejection would have been silent. Fixed as a class rather than an instance: the panel is revealed inside `ensureMap` where every caller passes, the whole handler is guarded so no branch can fail quietly, and `view.when()` is raced against a clock — because **a hang is not a rejection**, and until that race existed there was no arrangement of `catch` that could have reported this. **(7) The console's stylesheet, 2026-08-17, and this one is D-44's own fix reaching one resource kind and not the next.** The `<style>` block moved out of `index.html` into `console.css` so the console and its new layer page could not drift apart — and the console's policy read `style-src 'unsafe-inline' https://js.arcgis.com`, with no `'self'`. So the server sent every rule and the browser applied none. It did not look like a refusal: unstyled, `.view { display: none }` never arrived, so all five screens stacked into one document and the tab strip lost its layout — a layout bug, which is where I went looking. Both existing guards passed, because both name the resource kind they were written for: one asks whether `script-src` permits `'self'`, the other whether any page carries inline script. **The fix is the enumerating form**: `Every_file_a_console_page_asks_for_is_permitted` walks whatever a console page references — script, stylesheet, whatever is added next — and requires the governing directive to permit this origin and the file to be served. It needs no editing when the next kind arrives, which is the property the other two lack. **The shape is always the same:** a behaviour lives in N places, a fix is applied to the one being read, and the reader who finds the others cannot tell a partial fix from no fix. | Recurring; four instances recorded 2026-08-16 | Not a compromise anybody chose — an artefact of how a fix is scoped. Each individual instance looked complete from inside the file being edited. | **Whenever a behaviour is about to be fixed**, and the fix is not done until the question *what else carries this?* has an answer. Not a trigger to wait for: it is a step in the work. | Every instance costs a round trip with whoever is using the thing, and it erodes their reason to believe the next fix. Three of the four were reported by the owner in consecutive messages, which is the cost made visible. | **OPEN, and it is a process debt rather than a code one.** Two mitigations exist so far and both were built the same day: shared decisions moved into one file (`ground.js` decides which ground for all three pages), and tests asserting the **invariant** rather than the instance — `No_console_page_carries_an_inline_script` is a `[Theory]` over pages precisely because naming one page protects one page. |
| D-45 | **`/admin/layers` does not say which service holds a layer, or at which id, so no client can build its URL without guessing.** The listing carries name, table, status, sharing, owner and `arcGisServable`. It does not carry the service or the layer index. Guessing `/rest/services/{folder}/{layerName}/FeatureServer/0` is right only when a layer sits alone in a service named after it — and **wrong for every member of a multi-layer service.** `look_EarlyAlert` holds sites at 0, routes at 1, a group at 2 and reports at 3, so the console had been asking for services that do not exist for three of six layers. **What hid it:** the server answers its deliberate *"no layer is visible to you… the same for absent and forbidden"* 404, which is the correct answer to the wrong question and reads to an administrator holding every privilege as a permission problem. Found 2026-08-16 when a new drawer panel displayed the response instead of swallowing it. | Pre-existing; found 2026-08-16 | Not chosen. The listing was written for a catalogue view — *what exists and who can see it* — and nothing noticed that a UI also needs *where is it*. Single-layer services made the guess work for the common case, which is how it survived. | **Before another client is written against this listing**, and before the layer list grows a link anywhere. Concretely: whoever adds a service and index to `/admin/layers` closes this; the console's resolver can then be deleted. | Every consumer re-derives the mapping by walking the services directory — one request per service — or guesses and silently fails on multi-layer services. A UI that cannot address half a service is a UI that gets replaced rather than fixed. | **OPEN in the API; worked around in the console.** The console resolves name → (service, id) from the services directory, cached until the catalogue changes, and skips group layers. Guarded by `Every_catalogued_layer_is_findable_in_the_services_directory`, which asserts the catalogue and the directory agree — an invariant between two documents that nothing checked before. |
| D-44 | ~~**The security gate's own fix disabled the console for a day, and the console then leaked a password into a URL.**~~ **CLOSED 2026-08-16.** The §66 security gate of 2026-08-15 found *"not one security header on any response"* and added them. Its `HtmlPolicy` — `default-src 'none'` with **no `script-src`** — was derived, correctly, from the three server-rendered pages, and its own comment said they *"contain no script at all, which is asserted by a test rather than believed."* **The console is a fourth `text/html` surface and it is nothing but script.** So from that day the console ran **no JavaScript at all**: it rendered, because `style-src 'unsafe-inline'` was permitted, and then every button was inert. **And the sign-in form fell back to a native GET** carrying `name="username"` and `name="password"`, writing the password into the address bar, the browser history and any proxy on the way. Found by the owner opening it, not by anything we run. | 2026-08-15 | Nothing. The policy was right for the pages it was written for and was applied to one more. The comment that made it feel checked — *asserted by a test* — was true of those three pages and never covered the console. | — closed | — closed | **CLOSED.** The console gets its own policy by **path**, not by content type, so the three document pages keep the strict one. Its script moved out of the page into `console.js`, so the policy is `script-src 'self' https://js.arcgis.com` with **no `'unsafe-inline'`** — the fix is also the stricter policy. The map SDK is no longer a blocking CDN script in the head: it is fetched on demand, so a blocked CDN or an air-gapped deployment (Q-15) costs the map and not the console. The form now posts and its inputs carry no `name`, so a native submit has nothing to serialise even with zero script, and a `<noscript>` says what is wrong. Two regression tests in `SecurityHeaderConformanceTests`. **The lesson is the shape of the failure:** a blocked script is refused by the browser after the response left, so there is no log line, no error status and no failed request — every existing header test passed throughout, because they assert the headers are present and none asked whether the page still worked with them. |
| D-43 | **The datastore suite fails three tests on the first run after a build, and passes on every rerun.** Observed twice on 2026-08-16 with the same shape: `dotnet build` then `dotnet test` gives 243 of 246 in ~1 m 15 s, with `WkbAgainstPostgisTests`, `ArcGisWindingAgainstPostgisTests` and `PostGisFeatureSourceTests` each taking **31–33 s**; an immediate `--no-build` rerun gives 246 of 246 in ~32 s, and each of the three passes in isolation. **What is known:** the durations sit on Npgsql's 30-second default `CommandTimeout`, so these are queries not returning rather than assertions disagreeing. PostgreSQL had 6 of 200 connections in use, so it is not server-side exhaustion. `PostgresFixture` builds its own data source without our `statement_timeout`, so D-42's control is not involved. **What is not known:** the mechanism. Candidates not yet separated — xunit's parallel collections each creating and migrating a schema at once, cold page cache in the container on first touch, or contention on the catalogue during concurrent DDL. | Observed 2026-08-16 | Not a compromise anybody chose. It is recorded because D-30 already says this project's harness has been wrong three times, and a suite that fails only in the configuration CI uses is the fourth candidate. | **Before trusting a red datastore job.** Concretely: before anybody treats a CI failure in these three tests as a regression, or reruns the job to make it green. Also before adding retry to CI, which would hide this rather than fix it. | **CI runs `dotnet build` and then `dotnet test` in the same job** — the exact configuration that reproduces this. So the failure mode is not a local-machine curiosity; it is what CI does every run, and the reason it has not been seen there may only be that the service container is fresh. A flaky red build teaches people to rerun, and rerunning is how a real regression gets dismissed. | **OPEN, characterised but not diagnosed.** Reproducible on demand: build, then test. |
| D-42 | ~~**A mandatory statement timeout was removable by an unrelated setting.**~~ **CLOSED 2026-08-16.** ADR-007 §4.8 makes the per-connection `statement_timeout` mandatory, and `LayerConnections.BuildPool` applied it only when the registered connection string's `Options` was empty — deferring, the comment said, to *"an operator who has already set it"*. But `Options` carries every PostgreSQL server setting, not that one: registering a data source with `Options=-c application_name=qgis` silently removed the timeout for every layer on that source, forever, with nothing logged and nothing visibly different. Without it a single expensive query holds a pooled connection until the client gives up, and enough of them exhaust the pool for every other layer sharing the source. | 2026-08-12 (shipped), found and closed 2026-08-16 | Nothing. The intent was right and the test was wrong — *is this option set* was written as *is any option set*. | — closed | — closed | **CLOSED.** The check now asks whether `statement_timeout` itself is present, case-insensitively, and appends beside any other options. Extracted to `LayerConnections.WithStatementTimeout` so it is testable **without a database** — the old form could only be checked by reading it, which is most of why it stayed wrong. `StatementTimeoutTests`: seven cases, including the three that used to lose the timeout and the three where an operator's own value is still honoured. Found by the D-41 sweep, not by a failure. |
| D-41 | ~~**`havingClause` was SQL injection, anonymously reachable.**~~ **CLOSED 2026-08-16, the same hour it was found.** The FeatureServer query surface accepted ArcGIS's `havingClause`, checked only that it was non-empty and that `outStatistics` was present, and `PostGisFeatureSource.StatisticsAsync` appended it to the statement verbatim: `sql.Append(" having ").Append(query.Having)`. No parser, no allow-list, no bound parameters. `/query` is governed by service sharing, so on a layer shared to public the caller was **anonymous**. In `HAVING` position a subquery is legal, so this was at minimum blind read of everything the server's role could see. **What kept it alive is the part worth keeping:** a comment above it said the clause got *"the same treatment as `where`, which this server also passes through, so it is no wider a door; it is the same door"* — and `where` is **not** passed through. `WhereClause` parses it and re-emits our own SQL with every literal bound, which ADR-008 §4a states outright. The comment asserted an equivalence that made the hole look already-reasoned-about, and the §66 security gate on 2026-08-15 tested the where-clause parser and held, one parameter away. | 2026-08-15 (shipped), found and closed 2026-08-16 | Nothing about it was acceptable. It was not a trade; it was a wrong sentence in a comment that nobody had reason to doubt. | — closed | — closed | **CLOSED.** Refused at the boundary with a message saying why; `FeatureQuery` throws if one is constructed internally; `supportsHavingClause` is now `false` so the document stops advertising it; the query page's input is gone. Three regression tests: the boundary refusal, the constructor refusal, and `SqlStaysOutOfTheQueryModelTests`, which fails the build if any query-model member carries SQL again. **The capability itself is now absent** — that is [Q-109](open-questions.md), and it is a feature gap rather than debt. |
| D-40 | **ADR-008 states a rule it also documents breaking, and the shipped code follows the break.** §4.1 defines the query model by an exclusion — *"It must contain no SQL concepts. The moment it does, it stops being able to target GeoParquet, FlatGeobuf, or anything that is not a database"* — and §4a describes the parser it built as emitting parameterised SQL. `FeatureQuery.Where` is a `ParsedWhere(string Sql, IReadOnlyList<object?> Parameters)`, so the attribute filter enters the domain model already compiled. **The debt is the disagreement, not the missing abstraction:** an ADR that contains both sentences cannot be used to decide anything about the next provider. | 2026-08-15, recorded 2026-08-16 | The parser was the right call and the argument for it is in §4a: `where` accepted `1=1` and nothing else, which is not a FeatureServer with a gap but a FeatureServer nobody can use. Building a provider-neutral expression tree at the same time would have been an abstraction with one hypothetical consumer, which §82 forbids. What was skipped was not the abstraction but the paragraph reconciling the two sections. | **Before the first provider that is not a database is designed** — GeoParquet or FlatGeobuf as serving providers (Q-52), or DuckDB as the file-query engine (Q-81). Not after: the seam has to exist before something needs to read across it. | A provider design that assumes it can consume a `FeatureQuery`, discovers the filter is PostGIS SQL, and either grows a SQL parser of its own or refuses attribute filters entirely. Both are worse than the seam, and both get chosen under schedule pressure if this is not written down. Meanwhile every reader of ADR-008 has to work out unaided which of its two rules is live. | **RECORDED, deliberately unpaid.** Reconciled in ADR-008 §4a-i; `ParsedWhere` is pinned as the single exception by `SqlStaysOutOfTheQueryModelTests`, so a second one fails the build rather than quietly setting a precedent. |

---

## What belongs here

- A decision taken for schedule reasons that we already believe is wrong.
- A simplification that is correct at the current scale and known to break at a
  larger one.
- A dependency adopted knowing it must be replaced.
- A missing capability that downstream design is quietly assuming exists.
- A criticism from adversarial review that was accepted as valid but deferred
  (§85 requires every material criticism to be resolved *or documented* — this
  register is the "or documented" half).

## What does not belong here

- Deliberate scope decisions. Clustering is deferred by design (§79), not owed.
  It lives in [architecture-completeness.md](architecture-completeness.md).
- Unanswered questions. Those go in [open-questions.md](open-questions.md).
- Unvalidated assumptions. Those go in
  [architecture-assumptions.md](architecture-assumptions.md).

## Review cadence

Reviewed at every phase gate (§65). At each review, for every open entry, ask
whether the trigger has fired — and whether the entry is still honest, or has
quietly become the permanent architecture while nobody was looking.
